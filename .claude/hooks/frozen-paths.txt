# Frozen surface. One path per line, relative to the repo root.
# A path is added or removed here only via ruling — lifting the freeze IS
# the ruling path. The orchestrator is not exempt.
#
# The lift is the ORCHESTRATOR's step, in the MAIN CHECKOUT: lift before
# dispatch, restore after merge, both committed with the ruling's provenance.
# Ruling: James, M5 plan review, question 2. A lane cannot perform a lift —
# this hook resolves its list relative to the main checkout, so the lift a lane
# commits is invisible to the hook that enforces it. Measured by lane-o1. That
# resolution is privilege separation and is deliberate.
#
# Lifted once, by advisor ruling 2026-08-15, for exactly one change: the output
# contract gained a structured `adjusted` field, with §8 amended in the same
# commit. Restored in that commit. The freeze continues, and the next change
# needs its own ruling.
# (This block stood twice, verbatim. One copy removed by the M5 opening-1 lift,
# which is the first commit to own this file since the duplication appeared.)
#
# Lifted once more, by James's ruling on BLOCKED 2 at the M4 close, for exactly
# one change: crates/kicli/src/route/report.rs gained the joined-net contract
# field, with research/wire-routing.md §8 amended in the same lane commit and
# T18/T19's sibling-key surface migrated to the contract field. The entry is
# tasks/M5/opening-1-joined-net-contract.md. The window was d4c0eb8 (lift) to
# the merge at e4449ed, and it is CLOSED by this commit. The freeze continues,
# and the next change needs its own ruling.
crates/kicli/src/route/report.rs
