# M4 — Wiring

Cut from `spec/SPEC.md` §6, §9, §15, §18, §19 plus `research/wire-routing.md`
(R9) and `research/style-rules.md` §4. Code standards and the gate list come
from `ENGINEERING.md`. The mutation write path M3 froze — the atomic write, the
invariant pass, the mutation result — is the floor every verb here builds on.

**Goal of M4.** kicli draws a wire the way a person would, and says what the
wire cost. `wire connect` runs a deterministic orthogonal router and reports the
route with its cost breakdown, so an agent can accept the route or move a symbol
instead. `wire draw` takes explicit vertices and refuses an illegal one. Every
route the router writes reads in KiCad as the connection kicli claimed.

**Rules for this milestone.**

- Constitution §11: every task below names an executable completion check. A
  task is not done until that exact command passes.
- `ENGINEERING.md`: **`cargo xtask check` must pass at every commit.**
- `CLAUDE.md`: the orchestrator runs the full check, corpus included, at every
  lane merge. Corpus and environment gates do not run in a lane worktree.
- `ENGINEERING.md` "Testing pyramid": write the test before the implementation.
  Fixture expectations are verified against KiCad, never hand-asserted.
- Constitution §4: the router holds **no floating point**. Every cost is `i64`,
  every coordinate is `Iu`. The rule has an executable twin in T8.
- Every wire write goes through M3's path: a `kicli-sexpr` tree edit, the
  invariant pass, the atomic write, one delta fragment. No new way to write
  bytes appears in this milestone.
- **The weights are not retuned in this milestone.** `w_turn`, `w_cross`,
  `w_text`, `w_near` and `label_threshold` are shared with M5's rules, so
  changing one changes what the linter scores. If the re-route calibration gate (T20)
  falls outside 15 %, the task reports the measured deviation and parks it for
  James. It does not move a weight until the gate passes.
- No scoring, no rendering, no bus routing, no rip-up-and-reroute.
- **A task is named by its role, with the number in parentheses**: "the
  calibration gate (T20)", not "T20". Task numbers drift as a milestone is
  edited — this file's goal statement still named the calibration gate T19 after
  it had become T20 — and a ruling that carries only a number can be pointed at
  the wrong task by a later edit. The hedge that made this drift cost nothing
  was writing each trigger into the entry that owns it, so a task carries its own
  obligations rather than being told about them from elsewhere.

---

## Carried in from M3 — recorded so they are not re-opened

**The netlist oracle gate is open.** 35 of 35 demo hierarchies match KiCad, so
the extractor the router stands on is measured rather than assumed. The gate
stays in the exit criteria: a router that changes connectivity must be seen to.

**The oracle pattern is standing policy.** A check that matters asks KiCad about
the file kicli wrote, not about kicli's arithmetic. Every wiring task that
changes connectivity carries an oracle check.

**The invariant checks what the mutation wrote; the lint judges the drawing.** A
wire, a junction and a wire end are standalone connectable geometry, so the
on-grid invariant covers every segment this milestone writes. A symbol's
resolved pins remain `KI-GRID-001`'s jurisdiction.

**The delta and the mutation result answer different questions.** A route's own
report says what the route changed. `@last-write` is for everything else.

**One M3 debt is cleared here rather than left to age.** `sch view --view delta`
is listed in `spec/SPEC.md` §6, was asked for by no M3 task, and its semantics
are ruled. It is T17, in the lane that touches no router module.

**The other M3 debt is paid.** T23's two GUI observations were made on
2026-08-14: Eeschema shows nothing when kicli writes underneath it, and nothing
when its own save overwrites that write. `research/notes/eeschema-external-changes.md`,
`spec/SPEC.md` §14.4 and `AGENT.md` are written from the measurement.

## Settled before the milestone starts

`research/wire-routing.md` §10 asks James four questions. `spec/SPEC.md` §9
records all four as accepted, so no task below re-opens one:

| Question | Ruling |
|---|---|
| Q1 `w_turn = 6` | accepted. The calibration gate is the objective check on it |
| Q2 four-way junctions | refuse and offset by 1 G, reporting the adjustment |
| Q3 calibration | re-route a known-good sheet, total cost within **15 %** |
| Q4 `routing.margin` | 8 G |

One naming conflict is already resolved and needs the words fixed, not a
ruling. `research/style-rules.md` §4 calls `KI-LBL-001`'s knob
`labels.distance_threshold`. `spec/SPEC.md` §15 and C14 call it
`routing.label_threshold` and say it is one key shared with the router. C14 is
the resolution of that exact contradiction, so the spec's name wins and the
research doc's name is stale. T5 corrects it.

## Resolved — one term, one vocabulary

**Ruled by James, from the ergonomics pass.** The label kinds were written two
ways: `label add --kind` took `local|global|hierarchical`, and the layout view's
`T` line printed `label|global|hier`. An agent read `T hier PORT_A …` and had to
know to write `--kind hierarchical`.

**The view adopts the flag's words.** The view is what an agent copies from, so
its vocabulary is the load-bearing one. The eight bytes a hierarchical-label
line grows are noise against the §7.4 budget, and an alias on the flag would
leave two right answers, which is the thing one-term-per-concept exists to
prevent. `view_layout.golden` was refreshed in the same commit — the golden
change is the change — and `AGENT.md`'s mapping paragraph shrank to the single
canonical list.

**Proposed while implementing, affirmed on review: the snapshot keeps KiCad's
record names.** The file under `.kicli/` writes `label`, `global_label` and
`hierarchical_label`, beside `no_connect`, `bus_entry` and `sheet_pin`. Three
legs hold it up: these are KiCad's record names rather than a third synonym; the
file must round-trip a kind this version has never seen, which `Other(token)`
does by keeping the file's own word; and it is machine-keyed rather than read by
hand. The vocabulary rule governs the **agent-facing surface** — views, verbs,
documentation — and a snapshot is a wire format, which keeps its source-of-truth
names. Half-translating three of fifteen tokens would stale every snapshot
already written and leave the set in two vocabularies at once.
`ObjectKind::token`'s documentation states this where the tokens are defined, so
it is not re-argued.

Nothing about this is open.

---

## Phase 0 — the probe harness (sequential, orchestrator, no parallel lanes)

The measurement scaffolding the connectivity work grew is the instrument every
router calibration probe will use. It currently lives in test files: one builder
in `net_probe_rules.rs`, nine copies of `kicad_cli()`, five of
`export_netlist`, three each of `kicad_partition` and `node_label`, and a
scratch-copy module included by thirteen test binaries. A router probe built on
copies of copies measures the copy.

Promote it first. Nothing in Phase 1 starts until Phase 0 lands.

### T1 — The probe crate, and the drawing builder ✅

A new workspace member, `crates/kicli-probe`, holds the drawing builder that
`net_probe_rules.rs` grew: the `Probe` item-by-item writer, `Placed`, the
library-symbol builders (`symbol`, `pin`, `hidden_pin`, `power`, `resistor`),
the sheet builders that take a port angle, the strand and bundle shorthands,
`millimetres`, and `check_precision`.

Two properties of the builder are the reason it is worth a crate. A port whose
angle disagrees with its position is moved by KiCad, which takes it off the wire
the probe drew — so the sheet builders take the angle and justify from it. And a
coordinate with more than four decimals is not a coordinate KiCad writes, so
`write` refuses the drawing before any tool sees it. Both were paid for by a
probe that measured its own defect and called it a finding.

**Dependency direction.** `kicli-probe` depends on `kicli` and `kicli-sexpr`;
`kicli` dev-depends on `kicli-probe`. The cycle is dev-only, which cargo
resolves, and it is what lets the builder own `partition_with` — the helper all
16 probes call. `ENGINEERING.md`'s structure section gains the crate and the
statement that the edge is dev-only, in the same commit. The licence allowlist
does not govern a dev-dependency; advisories still do (`ENGINEERING.md`,
Dependencies).

The crate carries the same lints as the others: `deny(missing_docs)`,
`deny(unsafe_code)`, `warn(clippy::pedantic)`.

- **Check:** `cargo test -p kicli-probe` — the builder's own unit tests:
  `a_number_kicad_would_not_write_is_refused` plants a coordinate with five
  decimals and the writer refuses it; `two_runs_write_one_drawing` writes the
  same probe twice and the bytes are equal, because the identifiers come from a
  counter and not a random source.
- **Check:** `cargo test --test net_probe_rules` — the 16 probes pass with the
  builder deleted from the test file.
- **Check:** `cargo test --test probe_crate_is_dev_only` — a workspace-reading
  test: no crate names `kicli-probe` under `[dependencies]` or
  `[build-dependencies]`. The shipped artefact carries no test helper.

**Fallback, if the dev-only cycle fights the tooling:** keep `kicli-probe` on
`kicli-sexpr` alone and leave the four `Nets`-shaped helpers in a `probe` module
under `crates/kicli/tests/`. Take it only on a measured failure, and record the
failure.

### T2 — One home for the oracle invocation, and the record types ✅

The other half of the scaffolding is how a test asks KiCad. Promote it with its
record types, so a probe holds a typed answer rather than a string it parses
again: `kicad_cli()` and the skip convention, `export_netlist`, the netlist
readers (`kicad_partition`, `kicad_nets`, `node_label`), the rule-check report
reader and `millimetres_to_units`, and `without_the_run_specific_lines`.

An oracle record is never hand-written. It is what KiCad answered, read by one
reader, and a reader that read nothing is not an answer.

- **Check:** `cargo test -p kicli-probe` — `a_netlist_with_no_nets_is_not_an_answer`:
  the reader refuses an empty reading rather than returning an empty set a
  caller would compare against successfully. Its control,
  `a_netlist_with_nets_is_read`, reads the same file with its nets.

**Corrected while implementing.** This task first asked for
`a_report_with_no_items_is_not_an_answer` beside the netlist check. The two
readings are not alike, and the second sentence was wrong. A rule check that
finds nothing is a **drawing with no violation**, which is a true answer; a
netlist with no nets is a file no schematic produces. So the report reader
accepts an empty report — `a_report_with_no_items_is_a_real_answer` states it —
and the failure it must refuse is a report the tool never wrote, which the
runner refuses by panicking. The control an empty report needs belongs to the
caller comparing before against after, and lands in T4.
- **Check:** `KICLI_TEST_KICAD_CLI=1 cargo test` — every test that invokes
  `kicad-cli` passes with its own copy of the invocation deleted.
- **Check:** `cargo test` — the default run still needs no KiCad, and each
  skipped test still says which environment variable turns it on.

### T3 — The scratch copy moves in ✅

`crates/kicli/tests/support/mod.rs` becomes part of the crate: `fixture`,
`scratch`, `copy_file`, `copy_directory` and the two that combine them.

Two compile-time paths do not survive the move and must become parameters.
`CARGO_TARGET_TMPDIR` is set for test targets only, not for a library, and
`CARGO_MANIFEST_DIR` inside the crate names the crate rather than the caller.
Each test binary passes its own, once, at the top of the file.

- **Check:** `cargo test` — the suite passes with `tests/support/mod.rs` gone
  and no test file declaring `mod support`. Two more copies went with it:
  `net_oracle` and `fixture_oracles` each made their own scratch directory under
  the system temporary directory rather than under `target/`, which the `clean`
  gate cannot see. `Fixtures::copy_project` is the shared form, because copying
  a project before handing it to KiCad is one concept — the tool writes a
  `.kicad_prl` beside anything it opens.
- **Check:** `cargo xtask check` — the `clean` gate passes, so no test writes
  outside `target/`.

### T4 — The discipline, written where it is enforced ✅

The crate's module docs state what the harness encodes, because the next
milestone's author reads the crate and not this file: **probes are instruments,
and an instrument fails silently**; **a negative result needs a control that
must fire, or it is not evidence**; **an oracle record is KiCad's answer, never
a hand-written expectation**.

Two conventions become code rather than comments, because both already exist as
copied shapes: a before-and-after comparison whose before-reading is empty is
refused, and the delete-and-re-measure control — remove the item that is
supposed to cause a merge, measure again, and require the merge to go — gets one
helper. The mid-span junction case in M3 T14 is its first caller.

- **Check:** `cargo test -p kicli-probe` — `a_comparison_with_an_empty_control_is_refused`
  and `removing_the_cause_removes_the_merge` exercise both helpers, the second
  on a probe drawing built for it: two crossing wires with a resistor pin on
  each end, measured with the junction and without it. `Change::measured` is at
  its real call site in the mutation loop, which is where the empty-control
  hazard was found.
- **Check:** `cargo test --test probe_harness_has_one_home` — a
  workspace-reading test: no file under `crates/*/tests/` defines a function
  named `kicad_cli`, `export_netlist`, `kicad_partition`, `node_label` or
  `scratch`, and none declares a `Probe` of its own.
- **Check:** `cargo doc --no-deps` — the crate documents every public item, and
  the crate-level docs name the three disciplines above.

### Phase 0 status, 2026-08-14 — complete

Four of four tasks, each against the check it names. `crates/kicli-probe` holds
the drawing builder, the oracle invocation with its record types, the scratch
copy, and the two controls. Measured on the main checkout:

| Command | Result |
|---|---|
| `cargo xtask check` | all six gates pass: fmt, clippy, test, doc, deny, clean |
| `cargo test` | pass |
| `cargo test --features corpus` | pass |
| `KICLI_TEST_KICAD_CLI=1 cargo test --features corpus` | pass, netlist oracle still **35 of 35** |

What the sweep found and removed, beyond what the task asked: nine copies of the
tool lookup, five of the netlist export, three each of the netlist reader and
its node label, two of the rule-check report reader, and two test binaries that
made scratch directories under the system temporary directory rather than under
`target/`, where the `clean` gate cannot see them.

Two decisions worth carrying forward:

1. **`Report::parse` accepts an empty report and `Netlist::parse` does not.** A
   rule check that finds nothing is a true answer about the drawing; a netlist
   with no nets is a file no schematic produces. The control an empty report
   needs belongs to the caller comparing two readings, which is `Change`.
2. **`text_metrics` plots with `-n -e` and prints a refusal rather than
   panicking**, so `Kicad::try_svg` carries options and returns the reason. One
   seam, two callers, and that call now honours `KICLI_KICAD_CLI` as the rest
   do.

---

---

## Phase 1 — the router's foundations (sequential, orchestrator)

Three tasks that every later task reads. They land in order, in the new `route`
module, and nothing runs beside them.

`ENGINEERING.md`'s module list is not closed but is amended by the change that
grows it. `route` is added there in T6's commit, with its concern in one
sentence: **`route` turns two terminals and a sheet into an ordered list of grid
points and the cost of reaching them.** It knows nothing of files, the CLI, or
`kicad-cli`, which makes it pure and cheap to test.

### T5 — `[routing]` becomes a typed section, and the threshold is one knob ✅

`kicli.toml`'s `[routing]` keys are accepted by the validator today and read by
nobody. Give them a typed home: `label_threshold`, `w_len`, `w_turn`, `w_cross`,
`w_text`, `w_near`, `margin`, and `u_max` — which `research/wire-routing.md`
§4.2 defines (6 G) and `spec/SPEC.md` §15 does not list, so §15 gains the key in
the same commit.

`routing.label_threshold` is the router's "emit labels instead" boundary and
`KI-LBL-001`'s long-wire boundary. One key, one field, read twice. M5 reads this
field; it does not add one of its own. `research/style-rules.md` §4's stale
`labels.distance_threshold` is corrected here, citing C14.

- **Check:** `cargo test config_reads_every_routing_key` — each key parses to
  its documented default, a length accepts both `"30G"` and a millimetre form,
  and a misspelled key is still an error.
- **Check:** `cargo test --test the_label_threshold_has_one_name` — a
  workspace-reading test over `crates/`, `spec/`, `research/` and `AGENT.md`:
  the string `distance_threshold` appears nowhere, and exactly one config field
  carries a threshold. Its control asserts the canonical name **is** found, so a
  sweep that read nothing cannot pass. `tasks/` is out of scope: a task file
  records what a name was corrected from, which is how it explains itself.

**Added beyond the brief, and accepted on review: a negative weight or distance
is refused where it is read.** A negative weight does not merely produce an odd
route — it breaks A*'s admissibility, because the heuristic's guarantee rests on
every cost term being non-negative, so the search stops finding the cheapest
path at all. Config-read is the cheapest catch point: the alternative is finding
it in a route nobody can explain.
`a_cost_that_would_break_the_search_is_refused` holds both, with zero accepted
as the control. `spec/SPEC.md` §15 states the rule beside the keys.

**`AGENT.md` does not list `[routing]` yet**, because no command reads it. It
gains the section in T16, with the verbs that read it. What `AGENT.md` says now
is the thing that is true today: every known section is validated, so a typo is
an error the moment it is written.

### T6 — Terminals, and the escape rule ✅

A terminal is a symbol pin, a sheet pin, or a point on an existing net. Resolve
one the way the M3 verbs resolve an object: by reference designator on the sheet
path being edited, or by identifier.

A route must step at least 1 G along the pin's own direction before it turns,
at both ends. A wire that approaches a pin from the side reads wrong and
overlaps the marker on a clock or inverted pin. This is a hard constraint and
not a cost: a terminal whose escape point is blocked is `blocked`, with the
blocking object named.

- **Check:** `cargo test a_route_leaves_a_pin_along_its_own_direction` — a probe
  symbol carries one pin on each of its four edges, placed upright and mirrored
  both ways; in every case the escape point is one grid step **outside the
  symbol's body box**, and the step the other way lands on the body.

**Corrected while implementing.** This check first said the first vertex is
"1 G along the pin direction". A pin's stored angle points from its connection
point **towards the body**, so a route leaves along the reverse of it. KiCad's
own `Device:R` settles the direction rather than memory: pin 1 at `(0, 3.81)`,
angle 270, length 1.27, and a body rectangle whose top edge is at 2.54 — so the
root lands exactly on the body and the angle points inwards. `SCH_PIN::GetPinRoot`
in `eeschema/sch_pin.cpp` gives the same rule for every orientation, with the
file angle mapped at `sch_io_kicad_sexpr_parser.cpp:1673`.

The test states the rule against the **body box** rather than against the sign
in the code, so an inverted sign fails it. That was checked by inverting the
sign and watching it fail, which is the control this check needed.

**Two additions to the harness, built here and needed by T7:** a `Placed` may
carry an angle and a mirror, and `rectangle()` gives a probe symbol a body. A
symbol that occupies no space cannot obstruct a route, so the obstacle map has
no probe to measure without them.

**A sheet pin's angle means something else, and the difference is a trap.** It
names the **edge the pin sits on**, counting anticlockwise from the right with
y upwards: 0 right, 90 top, 180 left, 270 bottom
(`sch_io_kicad_sexpr_parser.cpp`, `parseSchSheetPin`). A wire leaves outwards
from that edge, so the heading is the angle reflected into the schematic's
y-down sense — 90 means *up the page*. `a_sheet_pin_leaves_the_edge_it_sits_on`
pins the mapping. It is established from KiCad's source and **not yet measured
against the running tool**; the first task that routes to a sheet pin owes it a
probe.
- **Check:** `cargo test a_blocked_escape_is_reported_and_not_routed_around` —
  a pin boxed in by another symbol's body yields `blocked` naming that symbol,
  and writes nothing.

### T7 — The routing window and the obstacle map ✅

The window is the bounding box of the two terminals, inflated by
`routing.margin`, clipped to the page. Obstacles are looked up in a hash grid
keyed by grid cell, built once per request.

`research/wire-routing.md` §3.2's table becomes a closed enum with an exhaustive
match, so a new schematic object type is a compile error at the one site that
must decide about it. The two rows that carry the whole design: a **collinear or
overlapping wire of another net is a hard block**, because a wire drawn along
another net's wire renders as a connection; a **perpendicular crossing is
allowed and costed**.

**A foreign wire's treatment depends on the route's heading, so the map cannot
be a set of blocked points.** Collinear and crossing are not two kinds of wire;
they are one wire met two ways. A cell holding a horizontal wire blocks a route
travelling horizontally through it and costs `w_cross` for one travelling
vertically. The query is therefore `entering(point, heading)` rather than
`blocked(point)`, and a cell stores the **axis** of any wire on it. Every other
row of the table is heading-independent and answers the same either way.

- **Check:** `cargo test every_obstacle_kind_is_classified` — one case per row
  of the table, each on a probe drawing that contains only that object. The
  match that classifies them is exhaustive over a closed enum, so a new
  schematic object type is a compile error at the one site that must decide
  about it.
- **Check:** `cargo test a_collinear_wire_blocks_and_a_crossing_does_not` — the
  **same cell** of the **same map**, queried with two headings, answers two
  verdicts: blocked along the wire's own axis, costed across it. Querying two
  drawings would test two maps and leave the heading dependence itself
  unmeasured, which is the one thing this check exists for.
- **Check:** `cargo test the_window_is_clipped_to_the_page` — a terminal near
  the page edge yields no lattice node outside the border.

**Two cases are required rather than optional**, both because they cover
decisions that would otherwise pass silently against convenient fixtures:

1. **The heading dependence, on one cell.** One map, one cell holding one
   foreign wire, two queries, two verdicts. The check above carries it.
2. **The window-relative lattice.** `points_in` aligns to the window's own
   origin rather than to absolute zero, so a body box whose corner is off the
   grid still marks the grid points it covers. A fixture whose boxes happen to
   start on grid agrees with either behaviour and measures neither.

#### Done, 2026-08-15 — the measuring half

The computing half landed in the previous session: `route::window` and
`route::obstacles`. What this session added is the adapter and the checks.

**The adapter is `route::sheet`.** `SheetObjects::read(file, path, routed)`
turns one placement of one loaded file into the lists the search borrows, and
`SheetObjects::geometry()` lends them. Symbol bodies come from `symbol_boxes`,
pins from `resolve_pins` with the escape the terminal rule gives them, label and
text boxes from `text_box`, and the wires, junctions and no-connects as the file
draws them. It owns its vectors and `SheetGeometry` borrows, so the search
copies nothing while it runs. T17 and T18 call the same function.

**Two more items in the harness, built here:** `Probe::no_connect` and
`Probe::free_text`. The table has a row for each and there was no way to draw
one.

**The seam that keeps connectivity's work out of the search is `Routed`.** Two
lists — the wires of the net being routed, and the terminals the route ends on.
Whose a wire is, is not a question geometry can answer, so the caller states the
answer and the adapter records it: a named wire becomes `Feature::OwnWire` and
ends the route, an unnamed one blocks along its own axis. A pin the route ends
on is a terminal rather than an obstacle and is left out of the list.
`a_sheet_becomes_the_lists_the_search_reads` measures both directions on one
drawing.

**The page came with the adapter, because the window is clipped to it.**
`route::sheet::page_area` reads the `(paper …)` list. The sizes are
`PAGE_INFO::standardPageSizes` (`common/page_info.cpp:46-67`) in mils, scaled by
`IU_PER_MILS = 254` (`include/base_units.h:69-86`), so A4 is 11693 × 8268 mils
and not 297 × 210 mm exactly — KiCad's own rounding, kept rather than corrected.
A file naming no paper draws on A4, as KiCad's default does; `portrait` swaps
the sides; `User` carries its own size and is clamped to the parser's limits.

**Ruled on review. Four self-resolutions, proposed while implementing and
promoted, all recorded where the code makes them:**

1. **A bus is laid down as a wire of another net.** Drawing along a bus reads as
   a connection exactly as drawing along a wire does, and this milestone routes
   no buses, so the conservative treatment is the right one: foreign is the
   treatment that guarantees a route is neither laid along a bus nor across one
   for free. A **bus entry** is left out: it is a diagonal stub and the lattice
   has no way to describe one, and its two endpoints are guarded by the bus and
   pin cells it joins. **The trigger to revisit is the re-route calibration gate
   (T20)**: if calibrated routes thread unrealistically close to entries, that
   gate reopens this.
2. **The page is the paper.** The border and the title block KiCad draws inside
   it are not taken off, because a wire over them is legal in the file and §3.1
   says "clipped to the page". kicli does not invent a harder boundary than the
   editor's. **The trigger to revisit is the re-route calibration gate (T20)**,
   and the reopened answer is a cost rather than a wall — recorded in that
   gate's own entry, which is what makes the number a convenience rather than a
   dependency.
3. **A hidden pin obstructs.** It is drawn by nothing and still connects, so a
   route that met one would join a net the reader cannot see. A hidden pin is a
   real connection point, usually a power input, and the obstacle map describes
   what is there rather than what is inked.
4. **`Uuid::short` is now a method on the type**, with the eight-character rule
   and its rustdoc. Four private copies of the same eight characters remain in
   `edit::net`, `edit::mark`, `view::connectivity` and `view::snapshot`; two of
   them shorten keys that are not identifiers, so the sweep is a task of its own
   rather than a side effect of this one. **It is a chore with an owner rather
   than a note**: see "Chores" below.

**The task text yields to measured reality on one point, and the ruling R3 asked
for is half unobservable.** `points_in` does align to the window's own origin,
but no probe drawing can tell that apart from aligning to absolute zero: every
window a route request can build **starts on the grid**, because a terminal off
the grid is refused (`Terminal::is_on_grid`) and the page starts at the origin.
Measured: replacing the window origin with `Point::new(0, 0)` in `points_in`
leaves all five drawing-based checks passing. So the required case is split, and
both halves are checked:

- The half a drawing can see is `a_body_off_the_grid_still_marks_the_points_it_covers`
  — a symbol whose body box is 1.5 grid steps from its anchor, so no edge lands
  on a grid point, and every lattice node the box holds is marked and no other
  is. Rounding the box's start corner **down** instead of up marks 99.06,99.06,
  which the box does not cover, and the check fails.
- The half it cannot is `route::obstacles::tests::the_lattice_belongs_to_the_window`,
  a unit test on a window that starts a third of a step off the grid. It is the
  one test in this task built on a hand-made rectangle, and it is there because
  the alternative is not measuring the rule at all. With the absolute-zero
  alignment it finds nothing at all and fails.

**The exception this granted, with the condition that makes it one.** Hand-built
geometry is permitted **only where no drawable request can distinguish the
behaviour**. The ban exists because a fixture and the code that reads one share
their assumptions and agree vacuously; that reason does not apply where no
drawing can produce the input at all. Citing this case without its condition
erodes the ban, so the condition is written beside it here and in the test's own
module docs.

**Carried to the lane that owns the search — the terminal-cell exception.**
§3.2's first row ends "except the grid point at a target pin", and
`Obstacles::entering` has no notion of the target: it answers about a point and
a heading. So T9, T10 and T18 must except their own two terminal cells rather
than expect the map to. The case that makes it load-bearing is a **sheet pin**:
its cell is on the sheet's border, which the sheet body covers, so a route to a
sheet pin is blocked at its own terminus until the search excepts it.

**Every check was shown capable of failing**, each by breaking the code it
watches and watching it fail: the wire's axis comparison inverted (both wire
checks fail), the page clamp dropped from `Window::around` (the clipping check
fails), the no-connect pass dropped from `Obstacles::build` (the classification
check fails, naming the empty cell), every wire called the route's own (three
checks fail), and the two alignment experiments above.

Measured on the main checkout, 2026-08-15:

| Command | Result |
|---|---|
| `cargo xtask check` | all six gates pass: fmt, clippy, test, doc, deny, clean |
| `cargo test --features corpus` | pass |
| `KICLI_TEST_KICAD_CLI=1 cargo test --features corpus` | pass, netlist oracle **35 of 35** |

### T8 — The cost model, in integers ✅

```
cost = w_len·steps + w_turn·corners + w_cross·crossings
     + w_text·steps_in_text + w_near·steps_within_1G_of_a_body
```

The breakdown is the point of the exercise, so it is computed as parts and
summed, never as a total with parts reconstructed for display. An agent reads
the parts to decide whether to move a symbol instead.

- **Check:** `cargo test the_cost_breakdown_sums_to_its_total` — over generated
  paths, the five terms sum to the reported total.
- **Check:** `cargo test the_weights_keep_their_documented_order` — one crossing
  costs more than a twenty-step detour and less than two; one corner costs more
  than a five-step detour. These are the rationale sentences in
  `research/wire-routing.md` §6, made executable.
- **Check:** `cargo test --test the_router_holds_no_floating_point` — a
  workspace-reading test: no `f32` or `f64` appears under `src/route/`.
  Constitution §4 says determinism is the whole game; clippy has no lint for it.

#### Done, 2026-08-15

`route::cost` holds three things: `Tally`, the counts a path collected;
`Cost`, the five parts those counts are priced into; and `Tally::of_path`, the
walk that produces one from a path and a map. The counts are facts about a
drawing and the weights are configuration, kept apart so a report can say what a
route **did** as well as what it cost. `Cost` has no total field — `total()`
sums the parts, and `parts()` names them the way the output contract does
(`length`, `turns`, `crossings`, `text`, `proximity`), so a report is the
structure rather than a translation of it.

**The walk is where the target-cell exception now lives, and it has one home.**
The points handed in are the polyline's vertices; the walk expands them into
grid steps, because a cost is charged per step and an obstacle is met at a
point. The first point is where the route starts and is never entered. The last
is the terminal it was asked to reach — which sits on its own pin, inside its
own symbol's body box — so a **block** there is the route arriving rather than
colliding, and only a block is excepted: what the last step costs is still
counted, because another net's wire through the target pin is a real crossing.
T9, T10 and T18 carry the note; a search that queries the map directly must
except the same two cells, and none of them may grow a second rule.

**Measured, and it corrected a test I had written wrongly:** a route to a pin is
blocked by that pin's **halo** — the step it escapes along — one step before it
reaches the pin. That is not a hole in the exception; it is the caller's answer
arriving late. A pin the route ends on goes in `Routed::terminals`, and then
neither the pin nor its halo is in the map. `the_route_arrives_at_a_terminal_that_would_otherwise_block_it`
now names the terminal and keeps the closed map as its control.

**The task text yields to the measurement on the weight order.** "One crossing
costs more than a twenty-step detour" is false at the documented weights:
`w_cross` is 20 and `w_len` is 1, so a crossing and a twenty-step detour cost
exactly the same. The break-even is **at** twenty, which is what
`research/wire-routing.md` §6's "detour up to 20 grid steps to avoid one
crossing" says. The check asserts the break-even: a crossing beats nineteen
steps and loses to twenty-one, and a corner beats five (the measured median
segment) and loses to seven. Two more sentences of §6 became executable in the
same test: a step through text is dearer than a corner and cheaper than a
crossing, and a crowded step is dearer than a plain step and far cheaper than a
corner.

**Generated paths, by enumeration rather than by sampling.** `staircases()`
produces every monotone path from one corner of a six-by-four field to the
other — 210 of them, one per arrangement of the down steps — over a drawing
holding a wire to cross, a label to run through and a symbol to crowd. Half of
them (105) are refused, because a path that turns onto the foreign wire's column
runs along it; the other 105 are costed. Every path walks the same distance, so
length alone tells them apart not at all, which is the argument for reporting
the parts. `proptest` is **not** a dependency yet: T11 is the task that adds it,
and an enumeration covers this property completely rather than sampling it.

**Every check was shown capable of failing**: a float added under `src/route/`
(the sweep names the file), the text part priced with `w_near` (the breakdown
check and the weight order both fail), the terminal exception removed (the
arrival check fails), and corners counted as zero — which fires the
anti-vacuity control, "no path turned", rather than an arithmetic assertion,
which is what that control is for.

| Command | Result |
|---|---|
| `cargo xtask check` | all six gates pass |
| `cargo test --features corpus` | pass, 66 binaries |
| `KICLI_TEST_KICAD_CLI=1 cargo test --features corpus` | pass, netlist oracle **35 of 35** |

### Phase 1 status, 2026-08-15 — complete

T5, T6, T7 and T8, each against the check it names. The router now has its
configuration, its terminals and the escape rule, its window and obstacle map,
the adapter that fills them from a loaded sheet, and its cost model. Nothing
searches yet: that is T9 and T10.

**The signature Lane B builds against is frozen — `route::report`.** Phase 1
froze the router's **inputs**; its **result** type did not exist, because T9,
T13 and T16 are what shape it, so Lane B had nothing to compile against on the
day the lanes split. Ruled on review: writing §8's shape is transcription rather
than design, and one orchestrator commit before any split is the minimum that
gives Lane B a signature. Done, and it is not lane work.

`Status` is `routed | labels | blocked | invalid`, with `is_failure()` — a
proposal is a result and only `blocked` and `invalid` are failures. `Report`
carries the terminals, the path, the tally, the five-part cost, the crossings,
what was added, the label pair, what blocked it, a reason and
`alternatives_considered`. **Nothing derivable is stored**: `segments()` is the
path, `corners()` is the tally, `length(grid)` is the steps walked, and the cost
total is the sum of its parts. The module names no exit code, because nothing
below the command layer may depend on one; T16 maps `is_failure` to its column
of the table and the check for that mapping is T16's.

- **Check:** `cargo test route::report` — the contract's own worked example
  reads back: three segments, two corners, 38.10 mm and cost 62 from one place
  each; a proposal writes nothing and draws no wire; every status writes the
  word §8 gives it.

**PROPOSED, one place §8 asks for something the router cannot know.** A crossing
is reported with `{ net, at }`, and the map knows the **wire** it crossed rather
than the net that wire carries. `Crossing` therefore holds the wire's handle and
`net: Option<String>`, filled by the caller on the same seam that sorts a wire
into the route's own or another's. The alternative — having the router ask
connectivity — would put the extractor inside the search, which is the thing
`Routed` exists to prevent.

**Phase 2 is not started. The split itself is escalated**: go/no-go and timing
are James's, because the two-lane structure is his workflow design and he
launches the sessions.

---

## Phase 2 — two lanes

The dependency shape here is not M2's or M3's. Everything that routes is one
module owned by one lane, and the only work that is genuinely independent of it
is the two verbs that do no searching. **Two lanes, honestly, rather than five
that would queue on each other.**

| Lane | Owns | Tasks | Depends on |
|---|---|---|---|
| **A — the router** | `crates/kicli/src/route/**`, `crates/kicli/tests/route_*` | T9, T10, T11, T12, T13 | Phase 1 |
| **B — the verbs** | `crates/kicli/src/edit/wire.rs`, `crates/kicli/src/cli/edit/wire.rs`, `crates/kicli/src/cli/view.rs`, `crates/kicli/tests/edit_wire*`, `tests/delta_view.rs`, `tests/command_surface.rs` | T14, T15, T16, T17 | Phase 1 |

**`tests/command_surface.rs` is held by one task at a time**, on the same rule as
`AGENT.md`. It was missing from this table until the delta view's tick review
found it — see that task's entry. Both the delta view (T17) and the verb surface
(T16) name a `--test command_surface` check, so both must write it; the order is
T17 first, T16 after it merges.

Lane B builds against the router's signature, frozen at the end of Phase 1, and
calls it in Phase 3. `Cargo.toml`, `lib.rs`, `edit/mod.rs` and the fixture
`MANIFEST` stay with the orchestrator. **`cli/args.rs` is designated to Lane B**
for this milestone, because every verb here is Lane B's.

### Phase 2 status, 2026-08-15 — INTERRUPTED by a token limit, one task of nine merged

The session that opened Phase 2 stopped on its token limit rather than on a task
boundary. What holds on `main`:

| Task | State |
|---|---|
| the delta view (T17) | implemented and merged, `71bb1d6`. **Not ticked** — no reviewer ran |
| the candidate shapes (T9) | not started as far as the record goes; uncommitted draft tests only |
| `wire draw` (T14) | not started as far as the record goes; uncommitted draft, **and the sheet-pin measurement it owes was not made** |
| T10, T11, T12, T13, T15, T16 | not dispatched |

Three coordination calls were made before the stop and are recorded in
`tasks/reports/M4-phase2.md`. **All three were PROMOTED by the advisor's ruling
on the interrupted stop (R4, R5, R6)** and now bind:

1. **`AGENT.md` is held by one task at a time**, so the delta view (T17) and the
   verb surface (T16) are sequenced rather than parallel. A document merge
   conflict is pure waste and sequencing costs nothing.
2. **`edit/mark.rs` is designated to whichever of the four-way task (T12) and
   `wire delete` (T15) runs first, and the two never run concurrently.**
   `edit::mark::wire_ends_at` is private, and one implementation called by all
   three consumers is the point of the four-way task; two lanes editing it at
   once guarantees the divergence the task exists to prevent.
3. **The label proposal (T13) is sequenced last in Lane A**, its implementer
   holding both the `route/` half and the `--auto-labels` wiring, after Lane B's
   verbs merge. A task split so that neither half can run the task's own check
   is not a parallelisation; it is two blocked tasks.

**The first act of the next session is the tick review of the delta view
(T17)** — the first reviewed tick, against this entry and commit `71bb1d6`.

### Phase 2 resumed, 2026-08-15 — checkpoint 1

The session reopened under the advisor's rulings on the interrupted stop. Three
amendments to `CLAUDE.md` landed before any dispatch: environment-gated checks in
a lane never count toward done but may be run to MAKE a measurement (R9); the
session runs under `/goal` to a checkpoint stop rather than ending at a task
boundary (R10); and a parked lane's uncommitted draft is reference, not
resumption (R2). The pre-commit hook's refusal (R3) and the promotions into this
file (R4–R8) were already applied in `47174b8` and `1030aab` and needed nothing.

**A fourth coordination call, which the lane table does not settle: the probe
crate.** `crates/kicli-probe/**` belongs to no lane, and both resumed tasks want
it — the candidate shapes (T9) needs a probe drawing per shape, and `wire draw`
(T14) already had 130 lines of draft changes there. It is a merge hotspot, so
CLAUDE.md's rule applies: touched by the orchestrator, or by exactly one
designated lane. PROPOSED, and cheap to reverse: **`crates/kicli-probe/**` is
designated to `wire draw` (T14) for this pairing**, and the shapes task builds on
the existing probe API or stops and reports what it needs. Recommendation:
accept. The alternative — both lanes adding to one shared crate concurrently — is
the merge conflict the designation rule exists to prevent, in the one crate whose
breakage stops every lane at once.

**A fifth, forced by the same measurement.** `wire draw` owes the sheet-pin
probe, and the entry requires the measured result be recorded **beside the rule**
in `route::terminal` — a Lane A file. PROPOSED: T14's implementer makes that one
amendment, restricted to the comment and its provenance label, touching no logic,
and quotes the diff back for checking at merge. Recommendation: accept. The
alternative is a measurement recorded away from the rule it measures, which is
how a rule's provenance goes stale.

### T9 — Candidate shapes, in the fixed order (Lane A) ✅

I, L(h-first), L(v-first), Z(vertical-mid, `m` ascending), Z(horizontal-mid,
`m` ascending), U(offset ascending). Each candidate is validated against the
obstacle map and costed; the cheapest wins; ties break by fewer corners, then
shorter length, then the lexicographically smallest vertex sequence.

Shapes come first because A* with a corner penalty finds *a* minimal route and
often not the one a person would draw, and because half of all real segments are
under 7 mm — the fast path is the common path, not an optimisation.

**Carried in from T7, and answered in T8 — the target-cell exception.** §3.2's
first row ends "except the grid point at a target pin", and `Obstacles::entering`
has no notion of the target: it answers about a point and a heading. `Tally::of_path`
excepts the two ends, so a shape costed through it inherits the rule and **must
not grow a second one**. Two things this task still owes: the terminals of the
route go in `Routed::terminals`, or the target pin's own halo blocks the last
step but one; and the case that makes it load-bearing is a **sheet pin**, whose
cell sits on the sheet's border, which the sheet body covers.

- **Check:** `cargo test each_shape_is_drawn_when_it_is_the_cheapest` — one
  probe drawing per shape, and the route is that shape.
- **Check:** `cargo test equal_cost_shapes_break_the_documented_way` — a drawing
  where two shapes cost the same yields the earlier one in the enumeration, and
  the report's `alternatives_considered` counts every candidate tried.

#### Interrupted, 2026-08-15 — not started as far as the record is concerned

The session hit its token limit mid-task. **Nothing is committed and nothing is
merged.** The state, so the next session resumes from this record rather than
from an archaeology of a worktree:

| | |
|---|---|
| Branch | `worktree-agent-a47c8bcd90b50118f` — no commits ahead of `main` |
| Worktree | `.claude/worktrees/agent-a47c8bcd90b50118f` |
| What exists | `crates/kicli/tests/route_shapes.rs`, 616 lines, **uncommitted and unverified** — the tests, written before the implementation as the rule requires |
| What does not exist | the implementation module; no shape enumeration, no candidate validation, no tie-break |
| Gates | never run on it |

**Treat the uncommitted test file as a draft with no standing.** It was never
compiled, never run, and never shown capable of failing, so it is not evidence
of anything. The next session may read it for its shape and may equally start
again; it must not assume any assertion in it is right. The task's own checks
and the obligations above are the source of truth.

#### Tick review, 2026-08-15 — APPROVE, with six of twelve rows measured

Merged as `a0a4e03`; merged check green, corpus and environment-gated run
included, oracle at 35/35. **Verdict: APPROVE. This is the tick.**

The reviewer took a `git archive` copy of `a0a4e03` to `/tmp`, established a
green baseline (`route_shapes` 6 of 6, the same 6 under
`KICLI_TEST_KICAD_CLI=1` with `kicad-cli` genuinely present, three unit checks,
five gates), and then **made six of the twelve recorded breaks itself**, one at a
time, reverting between. Each reproduced the entry's row. The other six it took
on the entry's word and said so.

Two of the six settle the claims this entry rests on:

- **The tie-break correction is measured, not argued.** The reviewer's own
  objection going in was that the tie-break drawing's lexicographically smallest
  L is *also* the enumeration-first L, so the assertion would pass under either
  theory. Reversing the lexicographic rung alone moves the winner to
  `LVerticalFirst` while enumeration order is untouched — so the drawing does
  separate the two rungs, and the correction to the task text stands on a
  measurement rather than on reasoning.
- **The one-home constraint holds, and the two readings are not blurred.** No
  second implementation of the target-cell exception exists in `route/shapes.rs`;
  the one home is `cost.rs:185`. With the exception removed there, all five
  drawing checks fail. With both `polyline` guards dropped, the drawings stay
  green and the unit check `a_route_that_draws_over_itself_is_not_a_route` goes
  red — so "unreachable from any drawable request" and "untested" are held apart
  in the record, and the second is closed by the unit check rather than assumed
  away.

**Scope clean**: four paths, `kicli-probe` untouched as the designation required,
frozen `report.rs` untouched.

#### Done, 2026-08-15 — implemented fresh, on branch `worktree-agent-a554f83be40bd0b4a`

`route::shapes` holds three things: `Shape`, the six silhouettes in §4.2's
declaration order, so the derived `Ord` **is** the evaluation order and nothing
carries an index beside it; `Candidate`, one route with its tally and its
five-part cost; and `Shapes`, everything one request produced — the feasible
candidates in enumeration order, the count of every candidate tried, and the
handles that refused the rest.

The predecessor's draft was read and **not resumed**. Its module shape was
adopted; every assertion in it was rebuilt from the drawing's own geometry, and
two of its numbers were wrong (below).

**Both Phase 1 obligations are discharged, and neither grew a second home.**

1. **The target-cell exception is not in `route/shapes.rs`.** Every candidate is
   costed through `Tally::of_path`, which excepts the two ends, so nothing in
   the module knows what a target is. Measured: with `arriving` forced false in
   `route::cost`, all five drawing checks fail and the sheet-pin one names
   `child` — the exception is load-bearing and it is reached through the one
   home it has.
2. **The terminals go in `Routed::terminals`**, and the check that makes it
   load-bearing is `a_terminal_the_caller_did_not_name_blocks_its_own_route`:
   the same drawing routes when the target is named and is refused by `U2.4`'s
   own halo when it is not.

**The sheet-pin case is now measured against the running tool, which discharges
the debt the terminal rule (T6) recorded.** T6 established the angle-names-the-
edge mapping from `parseSchSheetPin` and wrote that "the first task that routes
to a sheet pin owes it a probe". `kicad_reads_the_route_to_a_sheet_pin_as_the_connection_it_claims`
is that probe: the router's own route is drawn as a wire into the probe file,
`kicad-cli sch export netlist` reads it back, and KiCad puts `U1.3` and the
child sheet's `R1.1` on one net through the port `IN` at 127,101.6 on the left
edge. Run under `KICLI_TEST_KICAD_CLI=1` in this worktree **to make the
measurement the task owes**, which is what the amended rule permits; it counts
towards done only on the orchestrator's merged run.

**The task text yields to measured reality, twice.**

1. **The equal-cost drawing did not tie.** With the terminals four grid steps
   apart, a Z whose middle column cleared a body's crowded ring beat both Ls,
   **26 against 34** — the L's vertical leg runs down the ring's own column and
   pays `w_near` four times. That is the router working, not a tie, so the
   drawing was narrowed to a one-column-by-one-row span rather than the
   expectation being adjusted to whatever came out. The two Ls then tie at
   **22** and the Z families can only redraw them.
2. **"the earlier one in the enumeration" is not the whole rule.** §7 breaks a
   tie by fewer corners → shorter → lexicographically smallest vertex sequence,
   and only an exact tie on all of those falls through to the enumeration order.
   Two equal-cost shapes with **different** paths are therefore separated by the
   chain, not by the order. The check measures both halves: the two Ls tie and
   the chain picks one, and the **twins** — a Z whose free coordinate lands on
   the L's own line draws the identical vertex list — are what leaves the
   enumeration order as the only thing that can decide.

**PROPOSED: the U is the middle line placed outside the span, one of four
sides, offset ascending.** §4.2 says "four segments, needed when both terminals
face the same way; free coordinate is the outward offset" without fixing the
sides. Recommendation: the four sides in the order §4.3 already fixes for the
search's expansion — `+x, −x, +y, −y` — so the router carries one order rather
than two. Evidence that it is the right shape: in the U drawing, every
three-segment route either turns back on itself at the target or turns on the
column a no-connect stands in, the shapes that stay inside the span turn four
times, and the U turns twice — 40 against 46. The count it implies,
`4 × u_max/G`, is asserted independently in the tie-break check.

**PROPOSED: T7's hand-built-input exception is applied to two rules of
`polyline`, and the condition is met rather than cited.** Measured, 2026-08-15:
with **both** the turns-back-on-itself refusal and the vertex-distinctness
refusal removed, all five drawing checks still pass. The reason is structural —
a route only reverses at one of its two stubs, and a terminal's own cell is
always covered by the body it belongs to, so the leg that would reverse steps
through that cell first and the map blocks it anyway. Recommendation: keep both
rules, because what makes them unreachable is another module's invariant rather
than this one's, and measure them in `route::shapes::tests` where they can be
seen. Same for the corners and length rungs of §7's chain: no drawing here
reaches them, because a cost tie in these drawings is a tie in both, so they are
measured on stated tallies in the same module.

**A defect in `kicli-probe`, found by this task and NOT fixed — the crate is
designated to the `wire draw` lane (T14).** `Probe::label_of_kind` writes its
`shape` argument verbatim, so `strand_of_kind("hierarchical_label", "input", …)`
emits `(hierarchical_label "IN" input …)`. KiCad writes `(shape input)` —
`tests/fixtures/sch/nets/nets_channel.kicad_sch:384-385`, a file KiCad wrote —
and **does not read the bare token**: the oracle above first reported
`unconnected-(R1-Pad1)` and no join at all. kicli's own reader is lenient, so
every kicli-only test using this helper passes over a file KiCad reads
differently. Worked around at this task's own call site by passing
`"(shape input)"`. **Recommendation: the probe crate should write `(shape …)`
itself, and the callers in `tests/net_probe_rules.rs` should be re-measured
against KiCad** — five of them name hierarchical labels this way.

- **Check:** `cargo test each_shape_is_drawn_when_it_is_the_cheapest` — six
  probe drawings, one per shape, each asserting the silhouette, the exact vertex
  list, and that re-walking the chosen path reproduces the tally and the cost it
  was reported with. The coverage assertion compares the shapes drawn against
  `Shape::EVERY`, whose completeness is held by an exhaustive match in
  `route::shapes::tests`, so a seventh silhouette is a compile error and then a
  failing coverage assertion.
- **Check:** `cargo test equal_cost_shapes_break_the_documented_way` — the tie
  is asserted before anything is concluded from it; the chain picks the winner;
  the twins prove the enumeration order decides an exact tie; the whole
  enumeration order is asserted as a sequence, families then U offsets
  ascending; and `alternatives_considered` is checked against a count worked out
  from the terminals rather than read back from the router — 30 here, `2 + 2 + 2
  + 4 × 6`.
- **Check:** `cargo test every_candidate_is_a_polyline_a_wire_can_be_drawn_from`
  — every offered candidate is orthogonal, keeps no vertex mid-run, and obeys
  the escape rule at both ends, with controls that candidates were both offered
  and refused.
- **Check:** `cargo test a_route_arrives_at_a_sheet_pin_the_sheet_body_covers`
  and `cargo test a_terminal_the_caller_did_not_name_blocks_its_own_route` — the
  two Phase 1 obligations.
- **Check (environment-gated):** `KICLI_TEST_KICAD_CLI=1 cargo test
  kicad_reads_the_route_to_a_sheet_pin_as_the_connection_it_claims`.

**Every check was shown capable of failing.** Each break was made in the source,
watched, and restored:

| Broken | What failed |
|---|---|
| `best()` keeps the last of an exact tie (`<` → `<=`) | four checks; the tie-break reports `ZHorizontalMiddle`, and even the straight run and the sheet pin change shape |
| `lines()` yields `m` descending | the enumeration-order sequence in the tie-break check |
| the four U sides reordered to `+x, −x, −y, +y` | the sides-of-one-offset assertion |
| the collinear-run collapse dropped from `polyline` | four checks, including "keeps a vertex mid-run" by name |
| the turns-back-on-itself refusal dropped | **nothing** — see the PROPOSED above; caught by `route::shapes::tests` |
| the vertex-distinctness refusal dropped | **nothing** — same; caught by `route::shapes::tests` |
| `rank()` puts length above corners | the corners rung, "fewer corners beats shorter" |
| `rank()` compares vertex sequences in reverse | the lex rung, and the Z drawing picks `m = 85.09` instead of `82.55` |
| `considered` counts only feasible candidates | the count assertion, 18 against 30, and the refused-candidates control |
| the U family dropped from the enumeration | the coverage assertion, and the U drawing has no route at all |
| the target-cell exception removed from `Tally::of_path` | all five drawing checks; the sheet-pin one names `child` |
| the pin halo dropped from `Obstacles::build` | the unnamed-terminal check |

Measured in this worktree, 2026-08-15:

| Command | Result |
|---|---|
| `cargo xtask check` | all six gates pass: fmt, clippy, test, doc, deny, clean |
| `KICLI_TEST_KICAD_CLI=1 cargo test --test route_shapes` | 6 of 6 pass, oracle included |

Corpus-gated and the merged run are the orchestrator's, per CLAUDE.md.

### T10 — A* over turn-aware states (Lane A) ✅

States are `(x, y, dir)`; expansion order is fixed at `+x, −x, +y, −y`; the
queue orders on the total order `(f, g, x, y, dir)`, so no tie is ever resolved
by heap internals. The heuristic is Manhattan distance times `w_len`, plus
`w_turn` when a turn is unavoidable, which is admissible because every term is
non-negative.

A failure is never a bare failure. `blocked` carries the handles of the objects
that blocked it, because an agent given a list can act.

**Carried in from T7 — the target-cell exception, and A\* is the one that must
repeat it.** A\* expands states against `Obstacles::entering` rather than
walking a finished path, so it cannot inherit `Tally::of_path`'s exception and
must except its own goal cell **to the same rule**: blocks are excepted at the
two ends, costs are still counted. A\* meets it first at a sheet pin, whose cell
is under the sheet body it sits on.

**This is a known twin, so it owes a check that fails when the twins diverge.**
Two homes for one rule drift silently, and the drift shows up as a route the
search finds and the cost model then refuses, which reads as a router bug rather
than as a rule in two places.

- **Check:** `cargo test the_search_and_the_walk_except_the_same_ends` — a
  terminal whose cell is blocked by its own symbol's body: A\* reaches it, and
  `Tally::of_path` costs the very path A\* returned. Both must accept it, and
  with the exception removed from either home, both must refuse. A path the
  search accepts and the walk cannot cost is the divergence itself.

- **Check:** `cargo test a_star_routes_what_no_shape_can` — a drawing whose
  obstacles defeat every shape, and the path found is orthogonal, on grid, and
  free of hard blocks.
- **Check:** `cargo test a_blocked_route_names_what_blocked_it` — the handles
  are real identifiers of real objects, and nothing is written.
- **Check:** `cargo test the_queue_order_is_total` — over generated state pairs,
  the comparator is antisymmetric and never reports two distinct states equal.

#### Tick review, 2026-08-15 — APPROVE. TICKED

**The contamination question is settled, and settled the right way.** This task
reported unprompted that an early falsification sweep had stacked its breaks,
because `git checkout --` restores nothing when one pathspec fails. The reviewer
was told not to accept the entry's assurance that it was caught. It did not: it
extracted the commit to a scratch tree, verified it file-for-file by checksum
against `git show`, applied each break alone, ran the whole workspace, and
restored from a pristine copy with the restore checksum-verified before the next.

**Twelve of eighteen rows measured, and the proof is the shape of the results
rather than any assurance.** Six rows that claim *nothing happens* produce a
fully green workspace. Four rows that claim a *narrow* failure fail exactly one
test in the entire workspace. **A stacked sweep can produce neither** — with
several breaks applied, nothing is green and nothing fails alone. The table
records isolated breaks.

**The twin check is a real twin, verified from both homes.** Forcing the search's
guard true fails the check at line 259, "A* reaches a pin its own symbol's body
covers". Forcing `arriving = false` in `Tally::of_path` fails the *same check* at
line 282, "the walk costs the path the search returned". Two breaks, two lines,
two messages. The reviewer noted it could tell this apart from T9's
"I-broke-it-and-nothing-happened" outcome **because six other breaks in the same
sweep did produce nothing** — the discriminating power was demonstrated, not
assumed.

**Two recording defects, both UNDER-claims, corrected in the table above.** Two
rows named the wrong assertion inside the right check: the queue-order row fires
at `route_search.rs:590` on "the order is not (f, g, x, y, dir)", which runs
before the assertion named; the heuristic row fires at `:312` on
`route.tally.corners`, one assertion before the cost. Two further rows
under-report co-failures. **Nothing over-claims anywhere, and that asymmetry is
itself evidence against contamination** — a fabricated table does not err only in
the direction that makes it look weaker.

**The second PROPOSED item draws T9's distinction on the correct side.** It does
not conclude the corner cost is redundant; it records what the checks here cannot
see — "never decides a route *this task can draw*" — recommends keeping the
weight, and names the calibration gate (T20) as the revisit trigger with the
symptom to watch for. The reviewer reproduced the underlying measurement itself.

#### Done, 2026-08-15 — on branch `worktree-agent-ab3fce95dc0125eb4`

`route::search` holds four things: `State`, a grid point and the direction it
was reached from, whose **derived** comparison is `(x, y, dir)`; `Queued`,
whose derived comparison is the total order `(f, g, x, y, dir)` §4.3 fixes, so
nothing carries a tie-break beside it; `Route`, one answer with its tally and
its five-part cost; and `Search`, what one request produced — the route, the
handles that refused a step, and how many states were expanded.

**The brief's premise was wrong about the base and it was corrected rather than
worked around.** It said the worktree branched from the candidate shapes (T9)
merge; it branched from `47174b8`, three commits earlier, and `route::shapes`
did not exist in it. Measured: `git log --oneline -1 main` gave `a0a4e03`, the
T9 merge, and the branch was rebased onto it before a line was written. Nothing
else in the brief depended on the difference.

**The target-cell exception now has two homes, and the twin check fails from
both sides.** That was the one thing this task could not take on trust, so it
was measured rather than argued:

| Home broken | What the twin check did |
|---|---|
| `route::search` — the goal cell no longer excepted | FAILED: A\* reaches no pin its own body covers |
| `route::cost` — `arriving` forced false in `Tally::of_path` | FAILED: the walk cannot cost the path the search returned |

Both breaks were made in the source, watched, and restored. The check is a
twin and not decoration: it accepts only when both homes accept, and it refuses
when either one is broken. **The narrowing matters as much as the exception**:
a block is excepted only where the step **ends** the route, which is what the
walk excepts, so a route through its own target is refused in both homes.

**The search's tally is the search's own.** It is counted over the search's own
states as they are traced, rather than read back from `Tally::of_path` — a
tally copied from the other home would make the twin check agree with itself.
The check compares the two and asserts they agree on every field and on the
cost. The two ends are symmetric in both homes without a second rule: the walk
never enters the first point, and the search starts there rather than stepping
onto it.

**One order in the router rather than two.** `Heading::EVERY` is new, in
`route::terminal` where `Heading` lives: `+x, −x, +y, −y`. The search expands
successors in it, the U-shaped candidates take their four sides in it (the
literal T9 wrote inline is gone), and it is the order the derived comparison
gives — which is the last rung of the queue order, so the two must agree.
`every_heading_is_in_the_expansion_order` holds them together with an
exhaustive match, so a fifth heading is a compile error.

**The window's edge is not a handle.** `Obstacles::entering` answers
`"page border"` for a point outside the window; the search asks
`Window::cell` first and drops the step silently instead. A refusal names
objects an agent can act on, and the window is the search's own limit rather
than something in the drawing. The check asserts every handle in `blocked_by`
is a reference designator, a pin of one, or the identifier of an item the file
holds.

**PROPOSED: `Search::expanded()` is what a report carries as
`alternatives_considered` when the search rather than the shapes produced the
route.** §8 gives a report one such field and the shapes fill it with candidates
tried; the search has no candidates, and states popped is the same quantity —
what it looked at, not what it could use. Recommendation: the verb surface (T16)
or the join (T18) decides, because that is where a report is filled; nothing
below it depends on the choice.

**Four rules of this module are unreachable from any drawing, and they are
recorded rather than deleted.** Each was broken and watched; the table below is
what fired and what did not. The structural reason is the same one T9 recorded
for `polyline`, and it is worth stating once: **a terminal's own body box covers
every neighbour of its own cell except the escape point.** So the escape rule at
both ends, and the width of the goal-cell exception, are enforced by the
obstacle map on every drawing this milestone can build. They are kept because
what makes them unreachable is another module's invariant rather than this
one's, and they are measured in `route::search::tests` where the state can be
stated — the condition T7 attached to its hand-built-input exception.

**PROPOSED: the corner cost never decides a route this task can draw.**
Measured, 2026-08-15: with `w_turn` taken out of **both** the step cost and the
heuristic, every check still passes — the queue's total order settles the ties
the corner cost was meant to settle, and the reported cost is priced by
`route::cost`, which is falsified in its own entry. Recommendation: keep it (it
is §5's weight and the reported cost's own term) and let the **re-route
calibration gate (T20)** be the trigger: if calibrated routes zig-zag where a
person would turn once, the corner cost is not reaching the search's decision
and this is the note that says so. The unit test that looked like a check for it
is named for what it actually watches — the tally and the vertices agreeing.

**An inadmissible heuristic is caught, but not by a small margin.** Measured:
at three times the distance every check still passes; at a hundred times, the
twin check fails on its cost assertion. The exact costs the drawings assert are
what catch it, and they are derived from the geometry rather than read back
from the router.

- **Check:** `cargo test the_search_and_the_walk_except_the_same_ends` — three
  probe symbols in a row. Both terminals' cells are covered by their own bodies
  (asserted as controls before anything is concluded), A\* reaches the target,
  and `Tally::of_path` costs the very path A\* returned to the same tally and
  the same cost. The second half is the other direction of the same rule:
  passing **through** a body is refused in both homes, and the route round it
  is cheaper than the one shape that fits, which hugs two crowded rings.
- **Check:** `cargo test a_star_routes_what_no_shape_can` — two staggered walls
  of no-connects, each spanning the whole routing window, open on different
  rows. Every silhouette draws at most one jog between the escape points, so
  all 51 candidates are refused; A\* threads it in 32 steps and four corners,
  through both gaps, orthogonal, on the lattice and free of hard blocks.
- **Check:** `cargo test a_blocked_route_names_what_blocked_it` — the one cell a
  route can arrive from is closed by a no-connect. Every handle is checked
  against the identifiers the drawing holds, the file's bytes are compared
  before and after, and the control is the same drawing without the marker.
- **Check:** `cargo test the_queue_order_is_total` — 144 generated entries, all
  pairs: antisymmetric, never equal for two distinct states, and equal to the
  documented key `(f, g, x, y, dir)` written out independently of the derive.
  Transitive over every triple of a slice, with a control that the entries
  differ in every field.
- **Check (environment-gated):** `KICLI_TEST_KICAD_CLI=1 cargo test
  kicad_reads_the_a_star_route_as_one_net` — the five wire records of the maze
  route are read back by `kicad-cli`, which puts `U1.3` and `U2.1` on one net;
  the control, the same route one grid step short, leaves them apart. Run in
  this worktree **to make the measurement**, which is what the amended rule
  permits; it counts towards done only on the orchestrator's merged run.

**Every check was shown capable of failing.** Each break was made in the source,
watched, and restored. The rows that caught nothing are recorded as they
happened, not dropped:

| Broken | What failed |
|---|---|
| the goal cell no longer excepted in `route::search` | the twin check, the maze, the blocked check, and the unit test on the exception |
| `arriving` forced false in `Tally::of_path` | the twin check, and six of T8's and T9's |
| the goal cell excepted **however** it is entered | the unit test on the exception — **nothing a drawing can ask** |
| `Heading::EVERY` reordered away from the declaration order | the queue-order check and `every_heading_is_in_the_expansion_order` |
| `Queued`'s order drops the heading | the queue-order check, at `route_search.rs:590`, "the order is not (f, g, x, y, dir)" — citation corrected by the tick review; the named assertion would also catch it but is never reached |
| a refusal records no handle | the blocked check, "a bare failure" |
| a step off the window lands on the cell it came from | the blocked check |
| a corner is counted but no vertex is written | the twin check, the maze, and the empty-window unit test |
| the arriving step excepted from the **count** as well as the block | the twin check, the maze, and two unit tests |
| the heuristic at a hundred times the distance | the twin check, at `route_search.rs:312`, on `route.tally.corners` (left 8, right 4) — citation corrected by the tick review; it fires one assertion before the cost |
| `turns_again` answers on the heading it **is** travelling | the unit test on the estimate's corner term |
| the heuristic at three times the distance | **nothing** — see the PROPOSED above |
| `w_turn` dropped from the step cost, and from the estimate too | **nothing** — see the PROPOSED above |
| the retracing guard removed, with and without the corner cost | **nothing** — a retrace is never cheaper while a step costs something |
| a stale queue entry expanded anyway | **nothing** — with a consistent heuristic it relaxes nothing |
| a state keeps the first cost that reached it | **nothing** — same reason |
| the escape rule dropped at the target end | the unit test on the exception — **nothing a drawing can ask** |
| the escape rule dropped at the source end | **nothing** — the source's own body covers its other three neighbours |

Measured in this worktree, 2026-08-15:

| Command | Result |
|---|---|
| `cargo xtask check` | all six gates pass: fmt, clippy, test, doc, deny, clean |
| `KICLI_TEST_KICAD_CLI=1 cargo test --test route_search` | 5 of 5 pass, oracle included |

Corpus-gated and the merged run are the orchestrator's, per CLAUDE.md.

**Carried, for whoever owns the probe crate.** `tests/route_shapes.rs` and
`tests/route_search.rs` both carry `quad()` and `pin_terminal()`, because
`kicli-probe` is another lane's and the brief says to report rather than add.
The natural home for a four-pin probe symbol is beside `resistor()` in
`kicli-probe`. T9's `Probe::label_of_kind` defect stands; nothing here touches
a hierarchical label, so it was not met again.

### T11 — Determinism, as a property test (Lane A) ✅

For every fixture and every pair of terminals: routing 100 times, and across a
shuffled item order — KiCad reorders items, so the file's order is not a stable
input — yields byte-identical output.

`proptest` becomes a dev-dependency of `kicli`; it is already one of
`kicli-sexpr`, and the justification goes in the commit message per
`ENGINEERING.md`.

- **Check:** `cargo test --test route_determinism` —
  `routing_is_identical_across_a_hundred_runs` and
  `routing_is_identical_across_a_shuffled_item_order`.
- **Check:** `cargo test --test the_router_iterates_no_hash_map` — a
  workspace-reading test over `src/route/`: no `HashMap` or `HashSet` iteration
  feeds a decision. `BTreeMap` and sorted vectors are the tools.

#### Tick review, 2026-08-15 — APPROVE. TICKED

Merged `986daab`. The reviewer pinned the state first
(`git log 986daab..HEAD -- <lane files>`, empty — nothing landed underneath the
review), worked in a `mktemp -d` scratch copy taken with
`rsync -a --exclude target --exclude .claude/worktrees --exclude .git`, and
**verified the copy with `rsync -n -ci` before reading it**. Five gates passed
there and `clean` skipped, which is the scratch-copy artefact the amended
definition names rather than a finding. It confirmed by `diff` after every
restore that the real checkout was never written to.

**The entry's centrepiece claim was reproduced rather than read.** Both
"green check, faulty control" rows were re-made by the reviewer, each against
*both* the current control and a reconstruction of the old one:

| Break the reviewer made | Against the current control | Against the old control |
|---|---|---|
| `reordered()` ignores its `order` argument — the shuffle permutes nothing | **fails** at "the shuffle moved an item" | **passes vacuously**, as the entry claims |
| `answer()` returns a constant | **both arms fail** | with the baseline's class counts removed, the shuffled arm alone **passes vacuously**, as the entry claims |
| `keep_smallest` reverted to first-wins | hundred-run arm passes, **shuffled arm fails** at the answers comparison | — |

**The row that caught nothing was judged on its reasoning, and the reasoning
holds.** No `HashSet` exists in `search.rs` to re-break, so the reviewer read the
source instead: `State` derives `Ord` over `(at, dir)`, `Queued` over
`(f, g, state)`, and `BinaryHeap::pop` returns the maximal element by that total
order whatever the insertion order — so no tie is resolved by iteration order,
and successors of one pop are built one per `Heading` and never share `(at, dir)`.
Verified by direct reading, and the verdict says so rather than claiming a break.

Configuration confirmed against the source, not the prose: 56 ordered pairs × 2
drawings, `RUNS=100`, `PERMUTATIONS=64`, `RngAlgorithm::ChaCha`, fixed seed,
`failure_persistence: None`.

**Scope: not exceeded.** `keep_smallest` is nine lines inside Lane A's own
module, honestly labelled PROPOSED with its rationale rather than landed
silently, and follows from what the property test itself found — which is a
defect a determinism test cannot pass around.

**One discrepancy found by measurement and not blocking the tick**: the
"Carried" paragraph's claim about probe handles, stale at merge time. Corrected
above.

#### Implemented, 2026-08-15 — NOT TICKED, awaiting tick review

Branch `worktree-agent-a216a44225205e17c`, on base `3563339`. Two commits:

| Commit | What |
|---|---|
| `e2d662e` | both checks, and the blame rule that stopped depending on the file |
| `2d1724f` | two controls the falsification runs found missing |

**The worktree started at `3aa4803`, two commits behind the base the brief
named.** `3aa4803` is a strict ancestor of `3563339`, so the branch was
fast-forwarded onto the named base before any work — the stale-worktree defect
(C5) again, and the brief's own base-commit check is what caught it.

**What "routing" means here, because no verb routes yet.** There is no
`route::connect`. The composition the check drives, per request, is: resolve the
two terminals with `Terminal::of_pin`, read the sheet with `SheetObjects::read`
under a `Routed` that names both terminals as the route's own, build
`Window::around` at the configured margin, fill `Obstacles::build`, ask
`Shapes::of`, and **fall through** to `Search::of` only where no silhouette fits.
That is the join Phase 3 will put behind a verb, driven directly.

**What is compared, byte for byte.** One string per request, holding: the two
terminal names, the window's corners and grid, `Shapes::considered` and
`Shapes::blocked_by`, the winning candidate and **every** feasible candidate —
each as shape, vertex list, the five tally counts, the five cost parts and the
total — then `Search::expanded`, `Search::blocked_by` and the route A\* found in
the same form. Nothing derived is left out, so a difference anywhere in either
engine's answer moves the string.

**The drawings, built through the probe harness.** Two, each two symbols of the
four-pin `quad()` instrument, every ordered pair of their pins routed (56 pairs
each). `walled` puts a no-connect on one pin's escape cell and a column of
eleven junctions between the symbols, which produces all three answers: routes
the shapes draw, routes only A\* finds, and refusals. `crowded` overlaps its two
symbol bodies and adds a foreign wire, a junction, a no-connect, free text and a
label — an item of every kind the shuffle can move, and the overlap that makes
the blame ambiguous.

**The shuffle is applied to the written file.** The file text is parsed, the
root's drawable children are permuted into their own slots, and the document is
emitted and reloaded. Every item keeps its own record and its own identifier, so
only position moves — which is what KiCad's save does. Two controls guard the
instrument: the token count must be unchanged, and a non-identity permutation
must produce a file different from **the same file through the same writer
unshuffled** (see the falsification table for why that reference and not the
drawing's own text).

**PROPOSED, and it is a source change: the verdict names the smallest handle on
the cell rather than the first laid down.** `Obstacles::entering` blamed the
first `Block` feature it met, and the order features are met in is the order the
file lists its items in. Measured, 2026-08-15: with two symbol bodies
overlapping, the shuffled arm fails on `blocked_by` alone — same routes, same
tallies, same costs, different names, and sometimes a name present in one answer
and absent from the other, because a cell is blamed once. `keep_smallest` in
`route::obstacles` now takes the smaller of the names offered, for `blocked_by`
and `terminates_on` alike, which makes the name a function of the cell's
contents. Recommendation: keep. Every candidate name is equally true, the change
is nine lines, and the alternative is a report that blames one symbol today and
its neighbour tomorrow for one drawing. It is recorded as PROPOSED because it
changes Phase 1 behaviour that no ruling covers.

**The proptest configuration, and the trade taken.** 64 cases,
`RngAlgorithm::ChaCha`, `RngSeed::Fixed`, shrinking on, failure persistence off.
**The seed is fixed**: a seed from entropy makes a gate that fails on somebody
else's machine and passes on the next run, and the coverage a varying seed buys
is bought here instead by the case count and by each case being a whole sweep —
one case routes every pair of the drawing it picks, not one request. Shrinking
stays on because a permutation that breaks the router shrinks towards the file's
own order, which names the item that moved. Persistence is off because a fixed
seed already replays the case, and a check that writes a regression file into
the source tree when it fails is a check that edits the repository.

**Measured, and it decided the size of the drawings.** A hundred runs over every
pair costs what the pairs cost: two three-symbol drawings measured 18.4 s, and
four symbols 33 s, against a suite that is ~20 s without this check. The engines
are the cost, not the rendering (166 ms of a 193 ms pass). Two two-symbol
drawings hold all three answer classes and cost 11.5 s, which is what is
committed. The claim is unchanged — every drawing, every pair, a hundred times.

**Every check was shown capable of failing.** Each break was made, watched and
restored; the rows that caught nothing are recorded as they happened.

| Broken | What failed |
|---|---|
| `Obstacles::entering` blames the first `Block` laid down (the state before the fix above) | the shuffled arm, at the answers comparison — and **not** the hundred-run arm, which is right: first-wins is stable within one file order |
| `Shapes::of` enumerates its silhouettes out of a `HashMap` | **both** arms; the hundred-run arm on run 1 of the first pair |
| a `HashMap` named in `route::window` | the sweep, `["window.rs: HashMap"]` |
| the sweep pointed at `src/geometry` instead of `src/route` | the presence control, "the router keys its maps in a BTreeMap" — while the ban itself passed **vacuously**, which is the reason the control exists |
| the shuffle permutes nothing | **nothing, first time.** The control compared the shuffled text against the drawing's own text, and the writer lays a file out its own way, so the comparison passed on layout alone. The control now compares against the same file through the same writer unshuffled, and the rerun fails at "the shuffle moved an item" |
| every answer replaced by a constant | the hundred-run arm's class counters, at "some request fell through to A\* and it routed" — and **not** the shuffled arm, which had no such control. Every baseline now has to hold a shape route, an A\* route and a refusal, and the rerun fails both arms |
| the search expands its successors out of a `HashSet` | **nothing.** A state carries the direction it was reached in, so two successors of one pop are never the same state, and the queue's total order fixes every pop. Successor order cannot reach a decision here — which is why the sweep bans the **name** rather than the iteration |

Measured in this worktree, 2026-08-15:

| Command | Result |
|---|---|
| `cargo test --test route_determinism` | 2 of 2 pass, 11.5 s |
| `cargo test --test the_router_iterates_no_hash_map` | 2 of 2 pass |
| `cargo xtask check` | all six gates pass: fmt, clippy, test, doc, deny, clean |

Corpus-gated and the merged run are the orchestrator's, per CLAUDE.md. No oracle
run: determinism is a property of kicli's own arithmetic and nothing here
changes connectivity or writes a file KiCad reads.

**Carried, for whoever owns the probe crate.** `quad()` is now copied in a third
test file, for the reason T10 recorded: `kicli-probe` is another lane's. And
**every item a probe draws answers to the handle `00000000`**, because
`uuid.short()` takes the first eight characters and the probe's identifiers all
start with eight zeros — so no check here can tell two probe wires apart by
name. That is C5's defect met from a new direction: it is the reason the
ambiguous-blame case had to be built from two **symbols**, which carry reference
designators, rather than from two overlapping wires, which would have been the
commoner drawing.

**Correction, found by this task's tick reviewer at merge time — the paragraph
above was true when this lane branched and is false in the merged tree.** It is
left standing rather than edited, because what makes it worth reading now is
*how* it went stale. C5 was implemented and merged in the chore lane while this
lane was working, and `278c678`/`c31de4b` are ancestors of this task's merge
`986daab`. **Probe uuids vary per object in the merged tree** — the reviewer read
`01000001`, `01000006` and others directly from it. So no later task inherits the
workaround this paragraph describes, and the two-symbol construction of the
ambiguous-blame case is now a free choice rather than a forced one. The test is
not wrong and needs no change; only its stated justification does.

**The general lesson, which is the reviewer's and is worth more than the
correction.** A "Carried" note describes **another lane's state**, and a lane
reads that state at **branch time** while the next owner reads the note at
**merge time**. Between those two moments the other lane can land. So a carried
claim about a neighbouring lane is checked against merge-time `main`, not
branch-time `main`, before the next owner trusts it.

### T12 — Four-way avoidance, on the refusal that already exists (Lane A) ✅

M3 T14 built the four-way junction refusal in `edit::mark`, with the wire-end
detection behind it. The router does not grow a second one. Where a route would
terminate such that four wire ends meet, it offsets by 1 G and **reports that it
did**, so the drawing never needs the linter's `KI-JCT-001` to notice.

- **Check:** `cargo test the_router_never_makes_a_four_way_junction` — a drawing
  whose natural terminus is a three-end point: the route lands 1 G away, the
  report names the adjustment, and `mark::add_junction` at the terminus is still
  refused afterwards, which proves the offset avoided the shape rather than
  hiding it.
- **Check:** `cargo test --test the_four_way_rule_has_one_home` — a
  workspace-reading test: one implementation of the four-end detection, called
  by both the mark verb and the router.

**Correction to the first check's last clause, 2026-08-15 — it cannot mean what
it says, and the implementer's substitution was confirmed sound at tick review.**
Written above and left standing: "`mark::add_junction` at the terminus is still
refused afterwards". After a **correct** offset the terminus keeps **three** ends,
and three ends *take* a junction — so the clause as written asks for the opposite
of what a correct implementation produces, and an implementation that satisfied
it literally would be broken. What the clause was reaching for is real: that the
offset **avoided** the shape rather than hiding it. That is proved by a
**counterfactual** — a drawing carrying the two segments the router declined to
draw, on which `add_junction(P)` refuses with `FourWayJunction` — with the
control that on the drawing the router really produced, `add_junction(P)`
**succeeds**. Corrected here, beside the clause rather than over it, so the next
reader does not re-derive it: the verb surface's implementer spent a substantial
share of the task's reasoning on this, and said so.

#### Tick review, 2026-08-15 — APPROVE. TICKED

Merged `8f86e87`. The reviewer pinned the lane files at the start, and **re-pinned
during the review** because unrelated commits (the verb surface, T16) landed on
`main` underneath it — none touched T12's files. Scratch tree from `mktemp -d`,
`rsync -a` with the three exclusions, verified with `rsync -an -ci` (zero
differences) before reading; breaks made and reverted in the scratch tree only,
each revert re-verified with `diff -q`. Five gates passed there with `clean`
skipped — the method's artefact, named as such.

**Eight breaks re-made by the reviewer rather than taken on the entry's word**,
each matching the entry's row exactly:

| Break | Caught at | Verdict |
|---|---|---|
| `CROWDED = 4` | `:240` | matches row A3 |
| `CROWDED = 2` | `:295` | matches row A4 — **a genuinely different assertion**, which is the claim that mattered |
| `adjusted.push` suppressed | `:266`, `left: []` | matches A1 — applied but not reported |
| `settle` returns the unmoved terminal | `:240`, `left: (1143000, 889000)` | matches A2 — reported but not applied |
| `mark::FOUR_WAY = 5` | `:335` "four wire ends refuse a junction" | matches A6 |
| `mark::FOUR_WAY = 3` | `:345` "three wire ends still take a junction" | matches A7 |
| `wire_ends_at` renamed everywhere | `:122` "…where it is documented to be: []" | matches B3 |
| `workspace()` pointed at an empty directory | all three sweep tests, `:78` | **the presence control is real** — confirmed red, not a silent clean pass |

`edit/mark.rs` confirmed byte-for-byte unchanged, so no second implementation of
the four-end count exists. The oracle arm ran for real with `kicad-cli` present
(0.40 s against 0.02 s without) and passed.

**One discrepancy, found and resolved rather than waved through.** Row A2b said
that with the `:240` assertion removed, `:248` also catches the break. Removing
*only* the literal four-line `assert_eq!` leaves it caught by an adjacent
`assert_ne!` instead; removing that too reproduces the entry's claim exactly,
line, message and both values. The reviewer judged this an imprecision in naming
"the `:240` assertion" — most plausibly the two-line terminus check as one unit —
rather than a fabricated result, and confirmed the substantive claim by direct
re-measurement. **Recorded because a review that silently absorbs a mismatch is
not a review.**

**The task-text override was checked against the rule that permits it.**
`task-entry-recording` requires the citation in the entry; the citation here is
the geometric fact itself — P keeps exactly three wire ends after a correct
offset — which the reviewer verified rather than read. The counterfactual is a
sound proof that the offset avoided rather than hid the shape.

**Taken on the entry's word and said so**: A5/A5b (file order against
`Heading::EVERY`), A9/A10 (the oracle control arms), B1/B2/B5. And A8, where the
reviewer's own attempt at the break was caught by a different assertion than the
entry names — the entry already discloses that row as caught elsewhere and not
claimed for the oracle, which is the correct disclosed form.

**Scope: not exceeded.**

#### Implemented, 2026-08-15 — as reviewed above

`crates/kicli/src/route/terminal.rs`, `crates/kicli/tests/route_four_way.rs` and
`crates/kicli/tests/the_four_way_rule_has_one_home.rs`. Commits `2c6eb45` (the
rule and both checks) and `d0fb86c` (a rustdoc tidy) on branch
`worktree-agent-a0d627a3133d92259`, base `9b010b3`. The lane's own
`cargo xtask check` passes: fmt, clippy, test, doc, deny, clean. **No file
outside the lane's scope is touched**, and the frozen `route/report.rs` is not
touched at all — the contract's `adjusted` field was already there, added by
`dd4f659`.

**The rule, and where it runs.** `Approach::of(source, target, schematic, grid)`
settles **both** terminals against the drawing **before the search sees them**,
and answers with the terminals a route may really use plus the `Vec<Adjusted>` a
caller copies into `Report::adjusted`. A terminal standing on a point that
already carries `CROWDED` wire ends moves one grid step **along a wire that
already meets that point**, so the terminus stays on the net the route was drawn
to reach; a step into empty space would move the wire off the thing it was
asked to reach, which is a worse drawing than the four-way one. The direction is
taken in `Heading::EVERY` order rather than in the order the file lists its
wires, because KiCad reorders items when it saves. A step that would land on
another crowded point is passed over for the next direction.

**The boundary tested, and which side.** `CROWDED = 3`, not 4.
`edit::mark::FOUR_WAY = 4` counts the ends that **are** there, because a junction
is added to a point as it stands; this counts the ends there **would be** once
the route's own end arrives, so it is that boundary less the one end the route
brings. Neither constant is written in terms of the other — C6's correction, in
the case it was written for. **Both sides of the off-by-one are measured, not
argued:** the three-arm drawing must adjust (rows A3/A4 below move `CROWDED` to
4 and to 2, and each break is caught by a different assertion), and the two-arm
drawing must not — the check carries that second drawing precisely because
getting the boundary wrong in the safe direction leaves every other assertion
green.

**The composition driven.** No routing verb exists yet, so
`tests/route_four_way.rs::route` drives the composition the way
`tests/route_determinism.rs` established it, with `Approach::of` as the new first
step: settle the terminals, `SheetObjects::read`, `Window::around`,
`Obstacles::build`, `Shapes::of`, then fill a `Report` from the winning
candidate and from `approach.adjusted`.

**The drawing.** A probe drawing, built through the harness rather than by hand.
Three wires meet at `P = (114.3, 88.9)` — north to `(114.3, 76.2)`, south to
`(114.3, 101.6)`, east to `R2.1` at `(127, 88.9)` — so `P` is a three-end point
and a junction there is legal today. `R1.1` at `(115.57, 101.6)` faces north and
is the source. The route's own end would be the fourth at `P`, so the terminus
moves east one grid step to `(115.57, 88.9)`, on the east wire's interior, and
the route reaches it as a straight run with no corner. Two further drawings: the
same with the north arm removed (the boundary control) and the same with the
three arms **written in a different order** (the file-order control).

**Task text yielded to the drawing on one clause, and the reading is recorded.**
The check says "`mark::add_junction` at the terminus is still refused
afterwards". With the offset applied, `P` keeps three ends, so a junction there
is *permitted* by construction — `add_junction` refuses at four. The clause is
therefore implemented as the counterfactual it must mean: a fourth drawing
carries the two segments the router **declined** to draw, ending at `P`, and
`add_junction(P)` on it refuses with `MarkError::FourWayJunction`. The control on
that refusal is in the same test — on the drawing the router really produced,
`add_junction(P)` succeeds — so the refusal is shown to be about the fourth end
rather than about the point, and a `mark` that refused everything would not pass.

**The oracle, measured 2026-08-15 with `kicad-cli 10.0.5.`** The offset moves the
wire's end off the point that was asked for, so the question that matters is not
the arithmetic but whether KiCad still reads the connection kicli claimed.
`the_offset_terminus_still_joins_the_net_kicad_reads` writes the route through
`edit::wire::draw` and the junction the landing point needs through
`mark::add_junction`, then exports a netlist from those bytes:

| Arm | KiCad's nets |
|---|---|
| the route written, junction at the landing point | `Net-(R1-Pad1)` = `{R1.1, R2.1}`, `unconnected-(R1-Pad2)`, `unconnected-(R2-Pad2)` |
| the same drawing, **junction withheld** | four `unconnected-` nets: `R1.1`, `R1.2`, `R2.1`, `R2.2` |

kicli's own partition equals KiCad's on both arms (`differences` returns
`None`). So the offset preserves the connection, **and the junction is what
makes it** — a wire end on another wire's interior is not a connection on its
own, which is the new thing this offset introduces and is now measured rather
than assumed. Environment-gated per CLAUDE.md, so it counts at the orchestrator's
merge and not here; it was run in the lane to make the measurement the task owes.

**Falsification table.** Every row was watched failing and restored; the good
state was committed as `2c6eb45` **before the first break**, and every restore
was verified by checksum against it (`shasum -c`, four files, all OK at the end).

| # | What was broken | What caught it |
|---|---|---|
| A1 | `settle` moves the terminal but pushes no `Adjusted` — offset **applied, not reported** | `route_four_way.rs:266` `report.adjusted` equality: `left: []`. The path assertions passed, which is the point of the row |
| A2 | `settle` pushes the `Adjusted` but returns the terminal unmoved — offset **reported, not applied** | `route_four_way.rs:240` the terminus assertion: `left: (1143000, 889000)` |
| A2b | The same break, with the `:240` assertion removed, to show the later one is not decoration | `route_four_way.rs:248` "the route ends where the terminal was moved to" |
| A3 | `CROWDED` moved to 4 — the safe-direction off-by-one | `:240`: no adjustment at all, terminus stayed on `P` |
| A4 | `CROWDED` moved to 2 | `:295` the two-arm boundary control: it adjusted a drawing that must not move |
| A5 | Direction taken in `wire_ends_at` order (file order) rather than `Heading::EVERY` | `:240`: chose north, `(1143000, 876300)` |
| A5b | The same break, with the two drawings' arm orders swapped so the base arm passes | `:316` the file-order control: `by` was `(0, -12700)` on one order and `(12700, 0)` on the other |
| A6 | `edit::mark::FOUR_WAY` moved to 5 — `mark` stops refusing four ends | `:335` `expect_err("four wire ends refuse a junction")` |
| A7 | `FOUR_WAY` moved to 3 — `mark` refuses three ends too | `:345` the control on that refusal: "three wire ends still take a junction" |
| A8 | The offset stepped **perpendicular** to every wire, into empty space | Caught, but by `edit::wire::draw` refusing the path as `Blocked` at `P`, **not** by the oracle: an off-net landing beside this meeting point is not reachable without running along an arm. Recorded as caught-by-a-different-assertion rather than claimed for the oracle |
| A9 | The oracle's measured arm run **without** its junction | `:359` "KiCad joins the routed pin to the net the terminus was moved along": `{["R1.1"], ["R1.2"], ["R2.1"], ["R2.2"]}` |
| A10 | The oracle's control arm run **with** a junction | `:375` "without the junction KiCad reads no connection": `{["R1.1", "R2.1"], ...}` |
| B1 | A second `fn wire_ends_at(` added under `route/terminal.rs` | `the_four_end_count_is_implemented_once`: `left: 2, right: 1`, naming both files |
| B2 | The router counts the ends itself with a local closure instead of calling the one implementation | **Two** assertions: `the_mark_verb_and_the_router_call_the_same_one` (`terminal.rs` no longer calls it) and `nothing_else_counts_the_ends_at_a_point_for_itself` (`terminal.rs` now carries the shape) |
| B3 | The canonical function renamed to `ends_meeting_at` everywhere — the **presence control** | `the_four_end_count_is_implemented_once:122` "the one implementation is where it is documented to be: `[]`". A sweep for a name nothing has cannot pass silently |
| B4 | The workspace walk pointed at a directory with no sources — the **sweep control** | All three tests, at `:80`: "the sources of this workspace were found, not 0" |
| B5 | `SHAPE` changed to a pair of fragments nothing has — the **shape presence control** | `nothing_else_counts_the_ends_at_a_point_for_itself:164`: `mark.rs` is not written in the shape the sweep forbids elsewhere |

**PROPOSED — a terminal that is a pin is moved off the pin, onto a wire of the
pin's own net.** The rule is written for any terminal, because both ends of a
route are wire ends and a pin carrying three of them makes the fourth just as a
free point does. The offset lands on the interior of a wire that has an end at
that pin, so the pin's net is preserved and the drawing gains a junction rather
than a four-way one; the adjusted terminal fixes no direction, because a point
on a wire may be met from any side, so the pin's escape rule stops applying at
that end. The alternative is to leave a pin terminal alone and let the router
draw the four-way. **Recommendation: adjust, as implemented** — Q2 says the
router must not make a four-way, and connectivity is preserved by construction.
Not exercised by a check: the drawing here adjusts a free point, which is the
case the task names. **Revisit trigger:** the first calibration or corpus run
that reports an adjusted terminal whose name is a refdes-and-pin.

**PROPOSED — the count includes bus ends, so the offset could step along a
bus.** `wire_ends_at` counts wire **and bus** ends, which is right for the
junction refusal it was built for and is what makes the router's count the same
count. The consequence here is that a terminus beside a bus could be stepped one
grid step **along the bus**, putting a wire's end on a bus segment.
`WireEnd` does not carry the line's kind, so filtering would mean changing a
struct `edit::wire` also reads. Bus routing is out of scope for this milestone
by the milestone's own rules, and no check reaches the case. **Recommendation:
leave, and record it here rather than in code**, because the fix belongs with
the bus model. **Revisit trigger:** the first task that gives the router a bus.

**A carried gap, recorded where it is owed.** `Approach` is not yet called by
anything but the check, because the routing verb is Phase 3. The join that wires
it into the verb owes two things this task cannot do: copying `approach.adjusted`
into `Report::adjusted` at the real call site, and writing the junction the
landing point needs into `Report::added.junctions`. The oracle above shows the
second is load-bearing — without it KiCad reads no connection at all.

### T13 — The label proposal, at the shared threshold (Lane A) ✅

#### Tick review, 2026-08-15 — APPROVE. TICKED

Pinned at **both ends**, per the clause ruled this session. One commit landed
mid-review — `43211f5`, the orchestrator's own BLOCKED escalation — touching only
`tasks/`, no lane file. Scratch copy verified 0-diff with `rsync -n -ci` before
reading; breaks restored by copying the real file back (no `.git` in the scratch
tree) and confirmed byte-identical with `diff` each time.

**Three of the ten rows were re-made by the reviewer**, the two flagged highest
risk plus one:

| Break the reviewer made | Result |
|---|---|
| `Trigger::of` ignores the passed weights and reads `Config::default()` | `the_threshold_is_the_configured_one` fails with the entry's **exact** panic text: `30.48mm is over 12.70mm, so it is proposed` |
| the reachable form of "a proposal writes" — a legal L wire added inside `perform()` | `auto_labels_writes_the_pair_and_says_so` fails on the wire list, **four records where the contract expects two**, matching the entry verbatim |
| the stub-drawing loop removed from `perform()` | both `auto_labels_writes_the_pair_and_says_so` and `a_named_net_keeps_its_name` fail, the net showing only `["U1.1"]` — the label alone does **not** join the pins |

That last one is the measurement behind "a label 2 G off a pin with nothing
between is electrically nothing", re-made rather than believed.

**The case-1 classification was checked structurally and upheld.**
`route::propose` imports only `geometry`, `model::config::Routing`,
`route::report` and `route::terminal` — **no filesystem or edit path at all** — so
"a proposal writes" is a no-op forced by the module boundary, not a blind check.
The reviewer confirmed the live control arm is real, and made the reachable form
of the defect at the command layer, where it was caught.

**Scope: not exceeded.** `route/report.rs` has zero diff across the range — the
freeze held. `tests/command_surface.rs` gained one line. `AGENT.md`'s changes are
the `--auto-labels` section plus a disclosed incidental correction (see below).
`AGENT.md` and `command_surface.rs` were confirmed free at this task's start,
the verb surface (T16) having been ticked before its implementation commit.

**Taken on the entry's word, and said so**: the full six-gate run (the reviewer
was instructed not to run it in the live checkout, to avoid the `clean`-gate
race), seven of the ten falsification rows, and the oracle's numbers.

#### BLOCKED, 2026-08-15 — the recorded default is two different numbers

**Raised here because this task could not proceed past it without choosing a
value. Nothing changed; escalated to James.** Full options and recommendation in
`tasks/reports/M4-phase2.md`.

`spec/SPEC.md` §9 and `research/wire-routing.md` §5.5 both write the default as
**"30 G ≈ 381 mm"**. Measured from `crates/kicli/src/geometry.rs`:
`UNITS_PER_MM` is `10_000` and `GRID` is `Iu(12_700)`, so **30 G is 38.10 mm**
and **381 mm is 300 G** — a factor of ten apart.

The code takes the grid-step reading (`config.rs:175`,
`label_threshold: Iu(30 * GRID.0)`; `config.rs:593` asserts `Iu(381_000)`,
"30 grid steps"), and `AGENT.md:701` publishes `"30G"`. The **rationale** in both
documents takes the millimetre reading: `research/wire-routing.md:15` frames the
tension as "the router emits a wire at 250 mm and the linter penalises wires over
381 mm", which only argues for itself against a 381 mm boundary.

Likely origin, and it is the reason this is not a typo: `Iu(381_000)` is internal
units, and reading that constant as millimetres yields exactly "381 mm". The
gloss appears derived from the constant rather than from the length.

Load-bearing: at 38.10 mm the boundary sits **2.54 mm above the wire the `routed`
golden draws** (35.56 mm).

BLOCKED rather than PROPOSED for three reasons, any one sufficient:
`routing.label_threshold` is the one knob **shared with M5's `KI-LBL-001`** (C14);
this milestone's Rules say the weights are **not retuned in M4**; and it is a
value-level call.

**This task is correct under either ruling.** `the_threshold_is_the_configured_one`
proves the configured value moves the boundary, so what is blocked is a default,
not a behaviour.

#### RULED, 2026-08-20 — option (a): the default is 300 G = 381 mm

**Provenance: James's ruling, checkpoint-2 review session.** The BLOCKED item
above is closed.

The millimetre gloss is right and the grid-step constant was wrong. **The
default becomes 300 G = 381 mm** — exactly, not approximately — restoring the
recorded design rationale: a router that emits a wire at 250 mm while the linter
penalises wires over 381 mm argues with itself, and that sentence only argues
for itself against a 381 mm boundary.

**The 38.10 mm value descends from reading `Iu(381_000)` with the units
dropped.** Internal units are 10,000 to the millimetre; the constant's digits
read as millimetres give exactly "381 mm", which is how a gloss and a constant
came to disagree by a factor of ten while looking like each other. It was never
a chosen value, which is why ruling it does not violate the Rules' bar on
retuning weights in M4: nothing is being retuned, a transcription is being
corrected.

**Recorded beside the value, because M5 inherits it: `KI-LBL-001` scores against
this number.** The knob is shared (C14). A future session that moves it moves
the linter too, and must read this entry before it does.

Carried out as one commit — the constant, its assertion, and every gloss (SPEC
§9, `research/wire-routing.md` §5.5, `research/style-rules.md`, `AGENT.md`)
together, since a gloss corrected in a later commit is a window in which the
record lies.

**Carried out, 2026-08-20.** Lane `lane-threshold`, commit `c89c29e`, merged
`b3a53d1`. Six gates green on the merged result. Full evidence in
`tasks/reports/M4-phase3.md`. Three things from it belong in this entry, because
they are about the defect rather than about the session:

1. **The assertion is now a pair, and that is the actual fix.** The old form
   asserted `Iu(381_000)` against a comment reading "30 grid steps", and *a
   comment cannot be wrong out loud* — which is how the two numbers drifted for a
   whole milestone. The new form asserts the grid-step arithmetic and the
   millimetre length **against each other**, so neither can move without a red
   test. Falsified by moving `GRID` to `Iu(12_701)`: the grid-step arm stays
   green, being computed from `GRID`, and the millimetre arm fails. That is the
   drift the pair exists to catch.
2. **A fourth piece of evidence for option (a) was sitting in the repository the
   whole time.** `wire_contract_labels.golden`, `wire_output_contract.rs:206`,
   `route/report.rs:274` and `research/wire-routing.md:203` all already printed
   `447.04mm is over the threshold 381.00mm`. Constructed strings, so they never
   failed — but the frozen output contract had been speaking the ruled number
   since it was written, and nobody looked there while the item was open.
3. **`route/report.rs:259-262`'s `Iu(381_000)` is a coincidence of digits**, not
   a third site: the fixture is `steps: 30`, so the value is that route's own
   length of 38.10 mm. Confirmed by reading. The freeze was not touched and no
   BLOCKED arose.

**One prediction in the ruling held for one check out of four.** The ruling
expected the label proposal's own checks to be "insulated by construction" via
`kicli.toml`. That is true of `the_threshold_is_the_configured_one` and false of
the three `far_apart` checks, which read the documented default through the real
binary. Those three were fixed by **lengthening the drawing**, not by insulating
them: insulating every check would leave nothing anywhere asserting that the
default is a number a real drawing can exceed.


When the best path exceeds `routing.label_threshold`, or A* reports blocked, the
router proposes paired labels. It **proposes**; `--auto-labels` performs it and
the output says so. The name comes from the net's existing name, or
`<source-ref>_<pin-name>` when the net is unnamed.

- **Check:** `cargo test a_long_route_is_proposed_as_labels_and_not_drawn` —
  status `labels`, the reason names both the length and the threshold, and the
  file is byte-identical afterwards.
- **Check:** `cargo test auto_labels_writes_the_pair_and_says_so` — two labels
  land 2 G along each pin's direction, on grid, and the extractor joins the two
  pins.
- **Check:** `cargo test the_threshold_is_the_configured_one` — a `kicli.toml`
  that lowers `routing.label_threshold` moves the boundary, on the same drawing.

#### Implemented, 2026-08-15 — NOT TICKED, awaiting tick review

Branch `lane-t13-label-proposal`, from `a16333c`. Commits `3638d1d` (the
proposal, the flag, the checks, `AGENT.md`) and this entry.

**What was built.** `route::propose` is the router's half: `Trigger::of` compares
the best path's length against `routing.label_threshold` in internal units,
`anchor` steps two grid steps along a terminal's own direction, `label_name`
answers the net's own name or `<reference>_<pin name>`, and `Proposal::report`
fills the frozen `Report` with status `labels`, the pair and the sentence. It
writes nothing and reaches no file — `route/` cannot, which is the point.
`route::sheet::pin_terminal` was added beside it: one home for "the terminal
this pin makes", used by the command layer and by the checks.

`wire draw --auto-labels` is the command half. It resolves both ends, judges the
vertices the caller gave, and — when the answer is a proposal — writes a stub
from each pin to its anchor and a label on each anchor, and no wire between
them. Without the flag the verb is exactly what it was.

**The composition the checks drive** is `tests/route_determinism.rs`'s and
`tests/route_four_way.rs`'s: `Approach::of` settles the terminals,
`SheetObjects::read` fills the lists, `Window::around` and `Obstacles::build`
make the map, `Shapes::of` answers and `Search::of` is behind it. The new step is
`Proposal::of` on the length that comes back. The drawing is two symbols with
**named** pins, `U1.1` leaving upwards and `U2.2` leaving downwards — opposite
directions on purpose, so that "both labels went the same way" is a break rather
than a no-op.

| Command | Result |
|---|---|
| `cargo xtask check` | all six gates pass |
| `cargo test --test route_labels` | 5 pass |
| `KICLI_TEST_KICAD_CLI=1 cargo test --test route_labels` | 5 pass, oracle included |

**The oracle, measured 2026-08-15 against kicad-cli 10.0.5.** Check 2 changes
connectivity, so KiCad is asked about the file kicli wrote rather than kicli's
extractor alone. On the drawing before the write, KiCad reads four nets, all
`unconnected-(…)`, one per pin. After `wire draw --auto-labels`, KiCad reads one
net `/U1_SCK` carrying `U1.1` and `U2.2`, and two unconnected pins. The control
is the before-arm: a KiCad that joined everything would have passed the
measurement. kicli's own partition matches KiCad's in both arms
(`differences` is `None`).

**Falsification.** Good state committed at `3638d1d` before the first break;
every break restored with `git checkout --` and the tree confirmed against the
commit (`git diff --quiet`) before the next one.

| Broken | Caught by |
|---|---|
| `Trigger::of` reads `Config::default()`'s threshold rather than the configured one | `the_threshold_is_the_configured_one`, at the lowered arm: "30.48mm is over 12.70mm, so it is proposed". Also the unit test's last arm |
| `Trigger::of` never fires on length (`if false && …`) | all three drawing checks. Check 1 at "a path that long is proposed as labels"; check 2 falls through to a plain draw and refuses the diagonal, which is the documented fall-through |
| `Trigger::reason` drops the threshold from the sentence | check 1 ("and the threshold"), check 3 and check 2's printed reason, plus the unit test |
| `REACH` = 1 grid step | check 1's anchor assertion and check 2's printed `labels:` line |
| `anchor` steps the **reverse** of the pin's direction | check 2, exit 1: `a wire leaves U1.1 through (50.8,100.33). This one goes to (50.8,104.14) instead.` The stub is refused by the escape rule, so a mis-directed label never reaches the file |
| `anchor` steps **both** ends `MinusY` — "both labels the same way" | check 2, exit 1, on the `U2.2` end: `a wire leaves U2.2 through (152.4,81.28). This one goes to (152.4,77.47) instead.` The `U1.1` end is unmoved, which is why the drawing's two ends face opposite ways |
| the stub is not written, so each label stands 2 G off its pin with nothing between | check 2's connectivity assertions. Measured with the earlier assertions stood down so the reading could be seen: `U1.1`'s net is `#n1` with pins `["U1.1"]` — the labels named nothing and the two pins are apart. This is the measurement behind "the stub is what makes the label the pin's" |
| a performed proposal draws the wire as well (`perform`, then a legal L between the two anchors) | check 2's wire list: four records where the contract says two. This is the "a proposal silently wrote" break, made at the layer that **can** write |
| `label_name` ignores the net it is given | `a_named_net_keeps_its_name`, which was added because of this row — see below |

**Two rows need their reasoning stated rather than their result.**

*The "a proposal writes" break cannot be made in the router.* Check 1's
byte-identity assertion watches a composition that has no file in it:
`route::propose` takes terminals and a length and answers with a value, so there
is no edit to break. That is case 1 of the falsification skill — the break would
be a no-op because the module boundary already enforces the rule — and it is
**not** evidence that the assertion is blind. The assertion is shown live inside
the same check by its control arm: the same read over a drawing that **was**
written to differs (`assert_ne!`, on the near drawing after `edit::wire::draw`).
The defect the rule is really about — a proposal that writes — is only reachable
at the command layer, and that is where it was made and caught, in the table
above.

*`label_name` ignoring the net was caught by a unit test alone, at first.* Every
probe drawing here had an unnamed net, so the rule that a net's **own** name wins
was measured on no drawing at all — the primary half of the task's naming rule,
unmeasured. `a_named_net_keeps_its_name` was added for it: a label on `U1.1`'s
own anchor names that net `SPI_SCK`, and the pair must take that name rather than
`U1_SCK`. Re-made against the new check, the break fails it.

**PROPOSED — `--auto-labels` is opt-in, and `wire draw` still draws a long
wire.** The task says the router proposes and the flag performs. It does not say
what `wire draw` does with a long connection when the flag is absent, and the
reading that `wire draw` should itself answer `labels` was rejected on
Constitution §2: with `label_threshold` at its default of 30 G = **38.10 mm**,
that reading leaves no way to draw a wire longer than 38.10 mm at all, and "the
tool can't do that" is always a bug. So the proposal is the flag's, the verb is
unchanged without it, and the router's own proposal — which is what a routing
verb will surface — is `route::propose`'s answer. Recommendation: accept.
Revisit trigger: the join (T18) gains `wire connect`, which **should** propose
without a flag, because there the caller asked kicli to choose the path.

**PROPOSED — the pair is written as two stubs and two labels, four records.**
`research/wire-routing.md` §5.5 lists only the labels, at "+2 G along pin
direction". A label two grid steps off a pin with nothing between them is
electrically nothing: measured above, the extractor reads `U1.1` alone on `#n1`.
So performing the proposal writes the stub that carries each label back to its
pin. Recommendation: accept, and it is what the oracle measured. The alternative
— the label on the pin's own anchor — connects, and draws the name over the pin.

**PROPOSED — one report over four writes.** Each of the four writes goes through
`edit::wire::draw` or `edit::label::add`, so each is verified and atomic on its
own. The command then commits once more against the snapshot taken **before the
first**, so the caller gets one delta naming all four records rather than the
last one's. The cost is one extra identical write of the file. The gap the
approach leaves is composite atomicity: a failure on the third write leaves the
first two in the drawing. Everything that can be decided is decided before the
first write. Recommendation: accept for this milestone; the join (T18) writes
more than one record too and is the place to settle it once.

**Measured, and it contradicts two governing documents.**
`research/wire-routing.md` §5.5 and `spec/SPEC.md` §9 both write the default as
"30 G ≈ 381 mm". 30 G is **38.10 mm**; 381 mm is 300 G. The config default is
`Iu(30 * GRID.0)` = 38.10 mm and `AGENT.md` says `"30G"`, so two of the three
statements agree and the millimetre gloss is the odd one — but the gloss carries
the rationale ("more than a sheet width", and an A4 page is 297 mm), which points
the other way. **Not resolved here, and nothing was changed**: the milestone
Rules forbid moving `label_threshold`, which is shared with `KI-LBL-001`. It is
recorded because it is load-bearing: at 38.10 mm the boundary sits 2.54 mm above
the wire the `routed` golden draws (35.56 mm), so nearly every real connection is
over it. **BLOCKED for a ruling**: either the number or the gloss is wrong, and
this task is not the place to pick. Recommendation for whoever rules: read
`research/style-rules.md` KI-LBL-001's own wording ("bounding-box diagonal ≥
`label_threshold` … more than a sheet width") as the intent, which makes 300 G
the value and "30G" the typo — but the linter (M5) is what has to live with it.

**The constructed `labels` golden still matches.** `wire_contract_labels.golden`
was built by hand in T16 because nothing produced the status. A real proposal now
renders the same three lines in the same shape and the same wording — the
`reason` sentence is `path length {length}mm is over the threshold {threshold}mm`
in both — so the golden is unchanged and `wire_output_contract.rs` passes
untouched. Two things about it are now stale rather than wrong, and are left for
whoever owns that file: its module comment says nothing in this build produces a
proposal, and its constructed threshold of `381.00mm` is a value the default
configuration cannot produce (see the contradiction above). A **performed**
proposal renders one line the golden does not carry, `wires added: 2`, because a
performed pair writes its stubs.

**One incidental correction, in the document this task holds.** `AGENT.md`'s
`wire draw` and `wire delete` examples printed a delta line as
`+ W 3300f00e (50.80,50.80) -> (50.80,45.72)`. The renderer writes
`+ W <handle> <detail>` where a wire's detail is `50.80,45.72..50.80,50.80`
(`view::delta::DeltaLine`, measured from a real run). Both examples now show what
the tool prints.

**Checked for the generated-identifier trap.** No check here asserts a generated
identifier — the assertions are positions, names, text and net membership — and
the whole file was re-run with its scratch directory renamed
(`route-labels` → `route-labels-elsewhere`, which changes every drawing's
absolute path and therefore every derived identifier). All five checks pass from
both directories, oracle included.

**One test-hygiene defect found and fixed while writing check 3.** A scratch
directory outlives one run, so the `kicli.toml` the second arm writes was still
there in the first arm of the next run: the check passed once and failed on every
rerun. It now removes the file before it starts, and was confirmed by running it
twice in a row.

**Files touched, against the lane's scope.** `crates/kicli/src/route/propose.rs`
(new), `route/sheet.rs`, `route.rs` (the module list — Lane A precedent: the
shapes (T9) and A\* (T10) tasks both added their module there),
`crates/kicli/tests/route_labels.rs` (new), `cli/args.rs`, `cli/edit/wire.rs`,
`AGENT.md` and `tests/command_surface.rs` (both held by this task).
`route/report.rs` was not touched and needed nothing: `Status::Labels`,
`LabelPair` and `reason` carry the whole proposal.

### T14 — `wire draw`: explicit vertices, validated (Lane B) ✅

Vertices given, no search. kicli validates — orthogonal, on grid, no hard-block
violation, escape honoured at both ends — and refuses with a structured error
rather than drawing something illegal. A valid polyline writes one two-point
`(wire (pts …))` record per segment, because KiCad wires are always two-point.

**This task owes a probe, carried from T6.** `wire draw` validates that the
escape is honoured at both ends, so it is the first task to use a **sheet pin's**
escape direction in anger. That direction is established from KiCad's parser and
has never been measured against the running tool: a sheet pin's angle names the
edge it sits on rather than the direction of its graphic, which is the reverse
of what the same number means on a symbol pin. Measure it here — draw a port on
each of the four edges, run a wire outwards from each, and let `kicad-cli` say
which ones connect — and record the result beside the rule in
`route::terminal`. A read-from-source rule and a measured-against-the-tool rule
are not the same claim, and this one must not stay the first kind by default.

- **Check:** `cargo test explicit_vertices_are_validated_before_anything_is_written`
  — a diagonal, an off-grid vertex and a segment through a symbol body are each
  refused, each names the offending vertex, and each writes nothing.
- **Check:** `KICLI_TEST_KICAD_CLI=1 cargo test a_sheet_pin_leaves_the_edge_kicad_puts_it_on`
  — the four edges, measured against the tool rather than read from its source.
- **Check:** `cargo test a_drawn_polyline_is_one_record_per_segment` — the
  record count matches the segment count, the invariants pass, and the extractor
  reports the connection the caller asked for.
- **Check:** `cargo test a_wire_command_changes_only_its_own_lines` — P3, per
  command, on M1's `changed_line_count`.

#### Interrupted, 2026-08-15 — not started as far as the record is concerned

The session hit its token limit mid-task. **Nothing is committed and nothing is
merged.** The state:

| | |
|---|---|
| Branch | `worktree-agent-ad075a64567c5a91d` — no commits ahead of `main` |
| Worktree | `.claude/worktrees/agent-ad075a64567c5a91d` |
| What exists | `crates/kicli/src/edit/wire.rs`, 581 lines, **uncommitted and unverified**; modifications to `crates/kicli-probe/src/drawing.rs`, `lib.rs` and `oracle.rs`, 130 insertions against 14 deletions |
| What does not exist | the CLI verb, the args entry, the tests, and the sheet-pin measurement |
| Gates | never run on it |

**The sheet-pin probe is still owed and is still unmeasured.** That is the part
of this task that matters most and it did not happen. A sheet pin's angle names
the **edge the pin sits on** rather than the direction of its graphic, which is
the reverse of what the same number means on a symbol pin. The rule is
established from `sch_io_kicad_sexpr_parser.cpp`'s `parseSchSheetPin` and
**remains read-from-source, not measured against the running tool**. The
provenance in `route::terminal` is unchanged and still says so. Nothing in this
session moved that claim from the first kind to the second.

**Treat the uncommitted work as a draft with no standing**, on the same reading
as the shapes task (T9): never compiled, never run, never falsified. The probe
crate modifications are the ones to read first, because the probe crate is
shared and an addition to it affects every lane.

#### The sheet-pin measurement, made 2026-08-15 — the claim is no longer read-from-source

**Measured, kicad-cli 10.0.5, `crates/kicli/tests/edit_wire_sheet_pin.rs`.** A
sheet pin's angle names the **edge KiCad puts the port on**. The rule now rests
on the tool rather than on `parseSchSheetPin`, and the provenance beside it in
`route::terminal::Terminal::of_sheet_pin` says so and says how.

The instrument is connectivity, because connectivity is what a wrong rule
breaks. One drawing: a 25.4 mm sheet body at (101.6, 63.5), four ports written
one per edge, one stub wire leaving each port outwards to a resistor pin, and a
hierarchical label of the same name in the child sheet at the far end. Measured
twice with **one variable between the arms — the angle**. Every port is written
at the same place in both arms and every wire is drawn in the same place.

| Arm | Angles | What `kicad-cli sch export netlist` reported |
|---|---|---|
| agreeing | each port's own edge: right 0, top 90, left 180, bottom 270 | all four: `/child/RIGHT = {R1.1, RC1.1}`, `/child/TOP = {R2.1, RC2.1}`, `/child/LEFT = {R3.1, RC3.1}`, `/child/BOTTOM = {R4.1, RC4.1}` |
| reflected | each angle names the opposite edge | all four parent stubs `unconnected-(Rx-Pad1)`; each child pin alone on its port net |

**The control was run before the conclusion, per `ENGINEERING.md`.** Two symbol
pins joined by a plain wire sit in the same drawing, away from the sheet, and
read as one net — `Net-(R5-Pad1) = {R5.1, R6.1}` — in **both** arms. So the
reflected arm's four breaks are the angle and not a broken instrument.

**What this settles and what it does not.** It settles that the angle decides
the edge and that a wire meets a port only where the angle puts it, which is
what the escape rule stands on: 0 right, 90 top, 180 left, 270 bottom, and a
wire leaves outwards — in the schematic's y-down sense, 90 is *up the page*.
It does not measure a port whose angle is off the quarter turn; kicli answers
no escape for one, which is unchanged and still read from the parser.

**No ERC-report reading was added**, though the draft had begun one. The
netlist arms already separate the two readings of the angle, and a second
parser in the shared probe crate is surface every other lane then carries.

Falsifications, each by breaking the thing the check watches and watching it
fail:

| What was broken | Which assertion caught it |
|---|---|
| the top edge given the bottom edge's angle (90 → 270) in the agreeing arm | `the TOP port did not carry its stub into the child sheet` — and only that port; the other three and the control stayed joined |
| the control wire moved 1.27 mm off its two pins | `the control net is not joined, so the instrument is broken`, before any port was judged |
| `Terminal::of_sheet_pin` given the reflected y mapping (90 → `PlusY`, 270 → `MinusY`) in `route/terminal.rs`, then reverted | `the router leaves the TOP port the wrong way` |

#### Tick review, re-review 2026-08-15 — APPROVE. TICKED

The gap is closed and a fresh reviewer measured **all three** of the new arm's
falsifications itself, taking none on the entry's word. It read the three
partitions off the exported netlists rather than off the assertions, and they
reproduced the table exactly.

**It went further than the entry did and killed the competing readings one by
one.** Port stays put and refuses a wrong approach: dead, because a port at
`(127, 71.12)` cannot reach a wire whose nearest endpoint is `(101.6, 71.12)`.
Port connects at both positions: dead on the reflected arm. Port connects
anywhere along the named edge: dead on the one-grid-step falsification, where the
stub still touches the sheet body's edge and still does not connect. Position
derived from the angle alone: dead, because each port's along-edge coordinate is
arbitrary and per-port preserved while the assertions are net-specific. **Two
arms could not distinguish; three can.**

It also diffed the reflected and followed files directly: they differ on the four
`(wire …)` records and four symbol anchors only, and **the four `(pin …)` records
are byte-identical** — same position, same angle. So the third arm is what it
claims: the port written in one place, the wire drawn 25.4 mm away across the
body, and the join happening anyway.

`terminal.rs` verified comment-only by diffing both revisions with doc lines
stripped: non-comment lines identical. The narrowing to "connection point" was
judged well aimed rather than a hedge — connectivity is the instrument, so where
KiCad joins the port is exactly what was measured, and the connection point is
the half a router needs.

**Three observations recorded, none gating.** The doc comment's conclusion
generalises past the measured case: only opposite-edge angles were tested, and a
*perpendicular* angle is untested with "along-edge coordinate kept" undefined
there. The claim that "on a file KiCad wrote the two agree and nothing is owed"
is an unmeasured assumption whose failure would widen the fault rather than
invent one. And C4 names the check the "report" ruling produces while the check
the "correct silently" ruling would produce lives in this entry — traceable, but
a session picking up C4 alone sees one half of the flip.

#### Tick review, 2026-08-15 — REJECT: the claim outruns the measurement

Merged as `686aa72`; merged check green, corpus and environment-gated run
included, oracle at 35/35. **Not ticked.** The reviewer made five of the recorded
breaks itself against a green baseline, each reproducing the entry's row, and
audited the measurement hardest, as the heart of the task. Most of it stood:

- **The control is real, not a formality.** Moved 1.27 mm off its pins, it fails
  at the control assertion **before any port is judged** — it sits in the same
  file as the ports, so any harness failure takes it down with them.
- **Only the angle changed between the arms**, verified by diffing the two
  written files: they differ on the four `(pin …)` records only, and the two
  child sheets are byte-identical. One confound was found and ruled out — the
  probe derives `(justify …)` from the angle, so justify differs too; swapping
  justify alone, angles untouched, still connects all four. Justify is not
  load-bearing for connectivity.
- **`kicad-cli` 10.0.5 genuinely ran**, and the reported partitions are in files
  the reviewer's own run wrote.

**The gap: two recorded claims say KiCad *relocates* the port, and two arms
cannot see that.** The provenance above `of_sheet_pin` says "because KiCad puts
the port on the edge the angle names and not where the file wrote it", and
PROPOSED 2 says "the measurement above showed KiCad moves a port whose angle
disagrees with its position". What the two arms establish is that the angle is
load-bearing and the stub at the written position no longer meets the port. A
competing reading — KiCad leaves the port where the file wrote it and only
accepts an approach along the angle — predicts the reflected arm's netlist
exactly. Nothing in the test looks at where the port ended up.

It matters because PROPOSED 2 stakes a future ruling on the relocation half, and
an entry must support a retroactive ruling from the record alone. This one
cannot.

**The claim is true — the reviewer measured the missing arm before rejecting.**
Relocating each stub to the position the angle predicts rejoins all four,
identically to the agreeing arm. So the fix is additive, not a retraction: a
third arm, re-measured by the implementer rather than adopted from the review.

**Second finding, not gating: PROPOSED 2 has no executable half and no owner.**
It recommends a lint, defers to "whichever task owns the sheet-pin lint" without
naming one, and nothing would flip if it were ruled either way.

**Rejection one of two on this task**, counted. A fresh implementer closes both.

#### Tick review, 2026-08-15 — APPROVE. TICKED

Five of thirteen rows measured, one break at a time with a green baseline first
and reverts between, plus one break the table does not contain.

**The design constraint is genuinely pinned on both halves, and the reviewer
proved it rather than reading it.** The risk was that one assertion was doing
both jobs and could not really do either. It is two assertions, and each was made
to fail with the other half intact: removing the stranded junctions *as well as*
reporting them trips `a junction was removed as well as reported` while
`stranded.len() == 2` still passes — the exact "reports but also deletes"
implementation; and silencing the report trips the reporting assertion while the
file stays correct. The over-reporting boundary is pinned separately by widening
`< JOINING` to `<= JOINING`.

**The oracle arm is gated and live, not dead**: pointed at a net KiCad does not
report, it fails only under `KICLI_TEST_KICAD_CLI=1` and passes without it. When
broken, KiCad's actual answer printed character-for-character as the first row of
this entry's netlist table — the recorded table is reproducible.

**No second implementation of the end-matching exists anywhere in the diff**,
including as a private helper: `stranded_by` filters junctions and delegates the
counting entirely. T12 will find one implementation.

**Two record defects found, both corrected rather than waived:**

1. **Two falsification rows quoted an assertion message that does not exist.**
   The test was revised mid-task from one junction to two — which this entry's own
   eleventh row records — and the two rows written before that revision were not
   re-read. Both falsifications hold; they fire on different text. Corrected in
   the table above with the reason. **A reviewer who greps for a quoted string and
   finds nothing cannot tell a stale citation from a fabricated one**, which is
   why this is worth fixing rather than noting.
2. **`JOINING`'s rustdoc claims a number it does not share.** It says the value
   "is the same one `crate::edit::mark` refuses a four-way junction against, read
   from the other side". They are two constants — `JOINING = 3` in `edit/wire.rs`,
   `FOUR_WAY = 4` in `edit/mark.rs`. Different boundaries on the same quantity,
   both fed by the one `wire_ends_at`, so the designation's purpose holds; but the
   prose asserts an identity that is false. Verified at source before recording.
   Filed as chore **C6**.

#### Implemented, 2026-08-15 — NOT TICKED, awaiting tick review

`crates/kicli/src/edit/wire.rs` with the module line in `edit.rs`,
`crates/kicli/tests/edit_wire.rs` and `crates/kicli/tests/edit_wire_sheet_pin.rs`.
Commits `c7c40ca` (the measurement) and `f915b0b` (the verb and its checks) on
branch `worktree-agent-a702dd29e3d9c98dc`. The lane's own `cargo xtask check`
passes: fmt, clippy, test, doc, deny, clean.

`draw` resolves both ends, builds the vertex list, and refuses **before it
writes**: every vertex on the grid, then the escape at both ends, then the walk
over the obstacle map for the diagonal, the repeat and the hard block. Only then
does it write one two-point `wire` record per segment through
[`model::mutate::commit`], so the invariants run on what was built and a file is
written only when all of them hold. The report is filled from the same walk:
tally, cost, the crossings **named** as well as counted, and the identifiers of
the records added.

**The end-to-end connectivity claim is KiCad's, not kicli's arithmetic.**
`a_drawn_polyline_is_one_record_per_segment` writes the wire, reloads the file
and asks the extractor; with `KICLI_TEST_KICAD_CLI=1` it asks `kicad-cli` about
the same bytes, which answered `{["R1.1", "R2.1"], ["R1.2"], ["R2.2"]}` — the
join kicli claimed. Environment-gated, so it counts at the merge and not here.

**PROPOSED — an off-grid vertex is refused rather than snapped.** Constitution
§7 says "all placement operations snap to the schematic grid"; this task's check
says an off-grid vertex is refused. Both readings, so it can be ruled from the
record: (a) a polyline is not a position — moving one corner changes the shape
of the wire the caller asked for and can turn a legal path into one laid along
another net, so refusing is the safe reading; (b) §7 read literally covers
anything that puts geometry on a sheet, so a corner should snap and the command
should report a note, as `label add` does. **Recommendation: refuse**, which is
what is implemented, on the precedent of the terminal rule (T6, ticked):
`Terminal::is_on_grid` already refuses an off-grid terminal rather than snapping,
"because moving somebody's pin is not a routing decision". The reason is in the
module's own documentation so it is not re-argued. `an_off_grid_vertex_is_refused_rather_than_snapped`
is the executable half: implementing the snap makes it fail.

**PROPOSED — kicli reads a port where the file wrote it; KiCad connects it
where the angle says.** The third arm below measured it: with a reflected
angle, KiCad puts the port's connection point on the edge the angle names,
along-edge coordinate kept, and a stub drawn there joins while a stub at the
written position does not. `SheetPin::at` is the file's value, so on a file
KiCad wrote the two agree and nothing is owed; on a hand-written file that
disagrees, kicli's terminal is not where KiCad connects the port. **Recommendation:
this is a drawing fault for the invariant or the lint to name rather than
something the router silently corrects** — a router that moved the port would be
editing the drawing to suit itself.

**Nobody owns this, and the earlier wording implied somebody did.** It said the
gap "belongs to whichever task owns the sheet-pin lint". There is no such task:
`spec/SPEC.md` §11.4's catalogue has no rule for a port whose angle disagrees
with its position, and M4 scores no drawing at all (see the milestone's closing
line). So it is written down as **chore C4** with the check that would exist —
a disagreeing port is reported rather than routed to the written position —
and it is unowned until a session picks that chore up. The executable half is
C4's check, which is what makes the recommendation rulable: ruling "report"
means C4's check exists and passes; ruling "correct silently" means the router
relocates the terminal and C4's check is replaced by one asserting the moved
position. Until then nothing in the code changes either way, which is the state
this item is in and the reason it is a chore and not a task.

**The CLI verb is not here, and this is the obstacle to report.** The brief
designated `cli/edit/wire.rs` and `cli/args.rs` to this lane, but a new
`Command` variant makes `agent_doc_covers_every_command` fail until `AGENT.md`
documents the verb, and `AGENT.md` is held by the verb surface (T16) under the
one-task-at-a-time ruling. The four checks this entry names are all library
level and none of them needs the verb, so the verb was left to T16, which owns
`kicli wire connect|draw|delete` and the output contract anyway. Nothing in
`cli/` was touched.

**Two additions to the shared probe crate**, both general and both needed here:
`Probe::sheet_of_size` takes the sheet's corner, its size and a list of ports
separately, because a probe that asks where KiCad puts a port must be free to
write the port somewhere else; and `Port` is the port record it takes.
`Probe::sheet_named` is now one call to it and behaves exactly as before.

Falsifications. Every check was shown capable of failing by breaking the thing
it watches:

| What was broken | Which assertion caught it |
|---|---|
| the on-grid pre-check deleted from `draw` | `the refusal does not name (76.835,45.72)` — the walk still refuses, but names the vertex the segment starts at rather than the one the caller got wrong |
| off-grid vertices rounded to the nearest grid point instead of refused | `the request drew 5 segments instead of refusing`, in `an_off_grid_vertex_is_refused_rather_than_snapped` |
| `escapes_are_honoured` made to pass everything | both escape checks: `the request drew 4 segments instead of refusing`, and the port refusal never came |
| one record written for the whole polyline instead of one per segment | `one record per segment, because a KiCad wire is always two points` (left 1, right 3), and the P3 check's `one record per segment was reported` |
| the write hoisted above every check | `the file was written anyway` — all seven checks failed, which is the point: nothing may reach the disk before the validation |
| `crossings_on` made to answer nothing | `and the same crossing was named` (left 0, right 1), with the count still 1 |
| the command made to edit the `(paper …)` atom as well | `every line the command did not add is byte-identical` |
| the written coordinates shifted one grid step off the pins | `kicli does not report R1.1 joined to R2.1` |
| the oracle arm pointed at a net KiCad does not report | `KiCad does not report R1.1 joined to R2.1`, with the tool; the same test passes without `KICLI_TEST_KICAD_CLI`, so the arm is gated and live rather than dead |

#### The third arm, measured 2026-08-15 — closing the tick review's REJECT

**The tick review rejected the two-arm measurement, and it was right to.** Two
arms — angle agreeing with the written edge, then angle reflected with nothing
else moved — establish that the angle is load-bearing and that the stub at the
written position stops connecting. They do **not** separate the two readings of
why: KiCad *relocates* the port to the edge the angle names, or KiCad leaves the
port where the file wrote it and merely refuses an approach that does not match
the angle. Both predict the reflected arm's netlist exactly, and nothing in the
test looked at where the port ended up. Two recorded claims — the provenance
above `Terminal::of_sheet_pin` and this entry's second PROPOSED item — asserted
the relocation half, which the record could not support.

**The claim is true; a third arm now measures it.** Same reflected angles, same
written port positions, and each stub and its resistor moved to where relocation
predicts the port went: opposite edge, along-edge coordinate kept.

| Port | Written at | Angle written | Stub in the third arm | Resistor anchor |
|---|---|---|---|---|
| RIGHT | `(127, 71.12)` | 180 | `(101.6, 71.12)` → `(91.44, 71.12)` | `(91.44, 74.93)` |
| TOP | `(111.76, 63.5)` | 270 | `(111.76, 88.9)` → `(111.76, 99.06)` | `(111.76, 102.87)` |
| LEFT | `(101.6, 78.74)` | 0 | `(127, 78.74)` → `(137.16, 78.74)` | `(137.16, 82.55)` |
| BOTTOM | `(116.84, 88.9)` | 90 | `(116.84, 63.5)` → `(116.84, 53.34)` | `(116.84, 57.15)` |

**Measured in this lane, kicad-cli / Eeschema 10.0.5** (the netlist header names
the version), `crates/kicli/tests/edit_wire_sheet_pin.rs`, read off the exported
netlists rather than off the assertions:

| Arm | What `kicad-cli sch export netlist` reported |
|---|---|
| agreeing | `/child/RIGHT = {R1.1, RC1.1}`, `/child/TOP = {R2.1, RC2.1}`, `/child/LEFT = {R3.1, RC3.1}`, `/child/BOTTOM = {R4.1, RC4.1}` |
| reflected, stubs unmoved | each port net holds its child pin alone — `/child/RIGHT = {RC1.1}` and so on — with all four parent pins `unconnected-(Rx-Pad1)` |
| reflected, stubs where relocation predicts | **identical to the agreeing arm**, all four port nets rejoined with both pins |
| control, all three arms | `Net-(R5-Pad1) = {R5.1, R6.1}` |

A port left where the file wrote it cannot reach a wire drawn 25.4 mm away
across the sheet body, so the third arm rules that reading out. **This settles
the connection point and only that** — connectivity is the instrument, so what
is measured is where KiCad joins the port, not where it draws the graphic. The
wording in `route::terminal` and in the PROPOSED item above was narrowed to say
connection point for that reason.

Falsifications of the new arm, each by breaking the thing it watches and
watching it fail:

| What was broken | Which assertion caught it |
|---|---|
| the RIGHT port's third-arm stub moved one grid step along the far edge, to `(101.6, 72.39)`–`(91.44, 72.39)`, a place relocation does **not** predict | `the RIGHT port did not meet a stub drawn where its angle relocates it` — and only that port: the other three and the control stayed joined, so the arm distinguishes the predicted position from a nearby one rather than accepting any wire on that edge |
| the third arm given the **agreeing** angles, so no relocation happens while the stubs stay at the relocated positions | the same assertion, for all four ports at once, with the control still joined — the arm depends on the angle and not on the geometry alone |
| the control wire in the third arm alone moved 1.27 mm off its two pins | `the control net is not joined in the followed arm`, raised before any port in any arm was judged |

The two earlier arms' falsifications, recorded in the section above, were not
re-run: nothing in this change touches them.

Scope: `crates/kicli/tests/edit_wire_sheet_pin.rs` and the doc comment above
`Terminal::of_sheet_pin`. No logic changed — `route/terminal.rs`'s diff is
comment-only, and `edit/`, `cli/` and the probe crate were not touched.

### T15 — `wire delete` (Lane B) ✅

Delete a segment by identifier. The command removes what it was asked for and
nothing else: it does not cascade into junctions, because a junction that now
sits on two ends is legal and removing it is a second decision. It **reports**
every junction that now joins fewer than three ends, so the caller can act.

- **Check:** `cargo test deleting_a_wire_splits_what_it_joined` — the extractor
  reports two nets where it reported one, and the invariants pass.
- **Check:** `cargo test a_deleted_wire_reports_the_junctions_it_stranded` — the
  report names them, and they are still in the file.

#### Implemented, 2026-08-15 — NOT TICKED, awaiting tick review

`kicli::edit::wire::delete`, added to the module the drawing verb (T14) created,
with `crates/kicli/tests/edit_wire_delete.rs`. Commits `5c06d74` (the verb and its
checks) and `871d61e` (the identifier rule, matched to the command layer's) on
branch `worktree-agent-ad5ebde9f056ce4cb`. The lane's own `cargo xtask check`
passes: fmt, clippy, test, doc, deny, clean.

**The lane branched from `47174b8`, before the drawing verb (T14) and the
candidate shapes (T9) merged**, so it began with no `edit/wire.rs` at all. It
was fast-forwarded to `main` at `a0a4e03` before any work — no commits were
lost, because there were none.

`delete` resolves the identifier against the sheet's segments, removes that one
record through [`model::mutate::commit`], and then reads the file back to say
what the removal left behind. The order is the point: the stranded report is
taken from the written state rather than from the arithmetic that expected it.

**The rule, as implemented.** A junction is reported when it sits on the removed
segment — either end or the interior, because a junction on the interior loses
as much as one on an end — and is now left joining fewer than three wire ends.
Nothing else on the sheet changed, so naming anything else would be noise the
caller reads past. Nothing is ever removed but the record named:
`a_deleted_wire_reports_the_junctions_it_stranded` asserts both junctions of its
drawing are still in the file, and the falsification below breaks exactly that.

**`edit::mark::wire_ends_at` was widened from private to `pub(crate)`, and
nothing else in that file changed.** Two questions now rest on it — whether a
junction being added is a four-way one, and whether a junction a delete left
behind still joins enough ends — and they must rest on the same answer. The
brief designated `mark.rs` here on the promoted coordination ruling, precisely
so the four-way task (T12) finds one implementation rather than two. The reason
is written at the function so it is not re-argued.

**The end-to-end connectivity claim is KiCad's, not kicli's arithmetic.** Both
named checks were run with `KICLI_TEST_KICAD_CLI=1` in this worktree — a lane
run makes the measurement, it does not count as done; the merge does. What
`kicad-cli sch export netlist` reported about the bytes kicli wrote:

| Drawing | Before the delete | After it |
|---|---|---|
| three segments joining `R1.1` to `R2.1` | `{["R1.1", "R2.1"], ["R1.2"], ["R2.2"]}` | `{["R1.1"], ["R1.2"], ["R2.1"], ["R2.2"]}` |
| the T, dropper removed | (one net of the three pins) | `{["R1.1", "R2.1"], ["R1.2"], ["R2.2"], ["R3.1"], ["R3.2"]}` |

The first is the split, measured against the tool: KiCad reports two nets where
it reported one. The second is the other half of the non-cascade claim — the
junction that was left on two ends still joins what it joined, and only the
dropper's pin left the net.

**The CLI verb is not here, on the same obstacle the drawing verb (T14)
reported.** A new `Command` variant fails `agent_doc_covers_every_command` until
`AGENT.md` documents it, and `AGENT.md` is held by the verb surface (T16). None
of the checks here needs the verb. Nothing in `cli/` was touched.

**The identifier rule was taken from the one kicli already has, after a first
version of it diverged.** `delete` first accepted the whole identifier or
exactly the eight-character handle, argued from `Uuid::short`'s documentation.
Running the suite surfaced `crates/kicli/src/cli/edit/address.rs`, which already
addresses every other object: the whole identifier, **or a prefix of at least
eight characters**, with an ambiguous one refused and the matches listed. The
two rules differ on a nine-to-thirty-five character prefix, which the existing
one accepts. Task text yields to measured reality, and so does a lane's own
reasoning: `segment_named` now states the same rule, `a_short_prefix_names
_nothing`'s sibling assertion is in
`a_segment_is_named_by_its_identifier_or_its_handle`, and the eight-character
floor was falsified by removing it.

**PROPOSED — one rule, stated twice, and the seam belongs to the verb surface
(T16).** `cli::edit::address` answers in the command layer's `Failure` type, and
nothing below the command layer may depend on that type, so a library verb
cannot call it. The rule is therefore written out again in `edit::wire`, with
the documentation at both ends saying it is the same rule. **Recommendation:
when the verb surface (T16) wires `wire delete`, route the caller's text through
`address` and hand the library a resolved identifier**, leaving one resolver on
the agent-facing path; the library's own is then the belt-and-braces for a
caller that did not come through the CLI. The alternative — lifting the rule
into a layer both can see — is a bigger move than either task owns.

**PROPOSED — a bus identifier is refused rather than deleted.** `wire delete`
given the identifier of a `bus` record answers "that is a bus, not a wire"
rather than removing it. **Recommendation: refuse**, on the one-term-one-concept
rule: the verb says wire, and a bundle carries several nets, so removing one is
a larger decision than this verb's name admits. The cost is one refusal an agent
may meet; the alternative is a verb whose name understates what it does.
Whichever task owns a `bus` verb inherits the other half.

**Defect in the shared probe crate, found here and not fixed because the crate
is not this lane's.** `Probe::uuid` writes `00000000-0000-4000-800{series}-{n:012}`,
so **every object of a probe drawing carries the same eight-character handle**.
No probe drawing can exercise a command that takes a handle, and the committed
`sch/` fixtures are numbered the same way. The handle check here rewrites one
identifier in the drawing's text after the harness wrote it, and says so at the
point it does. That is the permitted hand-built case (no drawable request can
distinguish the behaviour), but it is a workaround: **the fix is a probe that
varies the leading digits**, which would serve every later verb that takes a
handle.

Falsifications. Every check was shown capable of failing by breaking the thing
it watches, in the source, and watching it fail:

| What was broken | Which assertion caught it |
|---|---|
| the `< JOINING` boundary widened, so every junction on the removed segment is reported | `a junction joining three ends is doing its job`, in `a_junction_still_joining_three_ends_is_not_reported` |
| the delete made to tidy up — the junctions it reported removed as well | `a junction was removed as well as reported`, with the report itself unchanged |
| the stranded report taken before the removal rather than after it | `both junctions of the removed segment were reported: []` — left 1, right 2 (citation corrected after the tick review; the test was revised mid-task from one junction to two and this row was not re-read) |
| `segment_named` made to take the first segment whatever it was asked for | four checks: `the report names what went`, and the two refusals stopped refusing |
| the handle form dropped, so only a whole identifier matches | `one segment carries that handle: NoSuchWire { identifier: "deadbeef" }` |
| the eight-character floor removed, so any prefix names a segment | `a prefix below the floor names nothing`, with the seven-character `deadbee` deleting a wire |
| an ambiguous handle resolved to the first match instead of refused | `more than one object carries that handle`, naming the segment it would have lost |
| the bus refusal removed | `that identifier names no wire`, with the delta showing `Removed Bus` |
| `doc.remove` dropped, so the command reported without removing | four of five checks; `exactly one record was removed` left 4 right 3 (citation corrected after the tick review, same revision) |
| the write hoisted above the identifier lookup | all five: `the file was written anyway` on both refusals, before anything else was judged |
| the reported point taken from the removed segment instead of the junction | `no junction was reported at (63.5,36.83)` — **and this one first passed**: with a single junction the segment's own end *was* the junction's point, so the drawing gained a second junction at the dropper's far end, where one point cannot be right for both |
| the oracle arm pointed at a net KiCad does not report, after the delete | `KiCad does not report the two nets the split made`, with the tool; the same test passes without `KICLI_TEST_KICAD_CLI`, so the arm is gated and live rather than dead |
| the same, on the arm that runs **before** the delete | `KiCad does not start with R1.1 joined to R2.1`, which is where the before-and-after table's first row was read |

### T16 — The verb surface and the output contract (Lane B) ✅

`kicli wire … ` in `cli`, with `--output json` carrying the mutation result. The
router's report is the shape in `research/wire-routing.md` §8: path, segments,
corners, length, the five-part cost breakdown, the crossings with their nets, the
handles added, and `alternatives_considered`.

`status` ∈ `routed | labels | blocked | invalid`. The exit codes follow M3's
refusal convention and are recorded here so they are not re-argued: `routed` and
`labels` exit 0 — a proposal is a result, not a failure — and `blocked` and
`invalid` exit 1, a well-formed request that could not be completed. `Status`
names no exit code below the command layer; **this task owns the mapping from
`Status::is_failure` to the exit-code column, and the check for it** (carried
from the Phase 1 status entry).

#### Tick review, 2026-08-15 — APPROVE. TICKED

Merged `a16333c`, on the second attempt: the first merge failed the orchestrator's
six-gate check and was backed out. The reviewer pinned the lane files at the
start, **re-pinned mid-review** when `ab1a18f` landed (report-only, no lane file),
and worked in a `mktemp -d` scratch copy verified with `rsync -ni` before reading.

**The mechanism claim was the centre of the review and was reproduced
independently.** The reviewer read `edit::wire::draw` — seed
`format!("{} wire {} to {}", target.path.display(), from.name, to.name)`,
beginning with the absolute path — and `edit::insert`, where
`Identifiers::for_document` hashes `format!("{} {}", seed, next)` with SHA-256.
It then **exported the pre-fix commit `dec0c0a` to a fresh path with
`git archive` and ran the test there**: both golden tests failed, with the
expected value printing `ebb43fde-5c3c-4534-8776-e35e3f8aefaf` — the identifier
this entry names as the lane's committed golden. The post-fix tree passes 13/13
from two different absolute paths. That is a reproduction, not a re-reading.

**The numbering was measured to be load-bearing, in both directions**, which
settles the point the orchestrator got wrong:

| What the reviewer did | Result |
|---|---|
| swapped `<id-1>`/`<id-2>` in the golden | `a_drawn_route_matches_the_golden` **fails** |
| rewrote the normaliser to emit a uniform `<id>`, goldens updated to match | baseline **passes** — order is no longer distinguishable |
| then deleted one entry from that uniform golden | still **fails**, on count |

So a collapsing normaliser hides **ordering** and not a missing entry, exactly as
the correction in this entry states.

Also measured: the shape assertion runs on the **real** unnormalised values in
`what_was_added_is_named_by_identifier`; `KEYS` is a hand-written literal of 15
with a comment saying why it is not derived from the renderer; three independent
sites assert `Labels` and `Routed` exit 0 — the `exit.rs` doctest,
`exit::tests::a_proposal_leaves_the_same_code_as_a_drawn_route`, and
`command_surface::every_route_status_exits_the_code_its_row_names`; the stated
baselines (22, 13, 6, 30) match; and `route/report.rs` is byte-identical between
`9b010b3` and `a16333c`, so the freeze held.

**On question 2 the reviewer judged this entry's self-assessment honest rather
than a dodge**: it distinguishes "shown falsifiable" from "shown falsifiable
under the input that actually broke it", and proposes a skill amendment rather
than claiming its procedure was adequate.

**Scope: not exceeded.** No `wire connect` variant exists in `cli/` or `AGENT.md`;
`WireVerb` is exactly `Draw` and `Delete`. The two-line excursion in `cli.rs` and
the one new test in `tests/agent_doc.rs` match items 5 and 6 of this entry's own
PROPOSED list. `agent_doc_covers_every_command` is unchanged, correctly left to
chore C7.

**Taken on the entry's word, and said so**: the hand-computation of the hashes
(the reviewer reproduced the outcome through the test binary instead — equivalent
strength, different procedure), and the lane-worktree six-gate run.

#### Corrected before briefing, 2026-08-15 — everything this entry now owns

**Provenance: advisor ruling, checkpoint-1 review, item 5.** The entry above was
written when Lane B's verbs were expected to carry their own CLI. They could not:
a new `Command` variant fails `agent_doc_covers_every_command` until `AGENT.md`
documents it, and `AGENT.md` is held by one task at a time. Both `wire draw`
(T14) and `wire delete` (T15) therefore merged as **library verbs with no CLI at
all**, each recording that it left the verb here. Three further obligations
landed on this entry from elsewhere while it sat undispatched. Stated in one
place, so the brief is derived from the entry rather than from four other
entries:

| What this task owns | Where it came from |
|---|---|
| the `wire draw` verb in `cli`, over `edit::wire::draw` | T14 — "Nothing in `cli/` was touched" |
| the `wire delete` verb in `cli`, over `edit::wire::delete` | T15 — same, on the same obstacle |
| `cli/args.rs`'s `wire` noun and its subcommands | lane table: `cli/args.rs` is designated to Lane B |
| the §8 output contract, text and JSON, and the exit-code mapping | this entry |
| `AGENT.md`: the wire verbs, in the house style of the other write commands | T17's tick review — "it bites the moment the wire verbs land" |
| `AGENT.md`: the **`[routing]` section**, with the verbs that read it | T5 — "`AGENT.md` does not list `[routing]` yet … It gains the section in T16" |
| routing the caller's text through `cli::edit::address` for `wire delete` | T15's PROPOSED, recommendation accepted below |

**PROPOSED — `wire connect` is not in this task's surface, and the entry's own
`connect|draw|delete` is the stale line.** T18's first sentence is "The router
behind the verb"; there is no `route::connect` entry point today, and building
one here would move Phase 3's join into a Phase 2 lane. The three alternatives
and why they lose:

1. *Ship `connect` as a placeholder that parses and refuses.* It would appear in
   `AGENT.md` as an available command for two tasks' worth of time, and an agent
   reading `AGENT.md` would call it. A verb that lies about being available is
   worse than a verb that is not there.
2. *Build the router driver here.* That is T18's stated content, and the phase
   split is James's workflow design — escalated to him once already.
3. *Add the `connect` variant in T18, using this task's renderer, and document it
   in `AGENT.md` then.* `agent_doc_covers_every_command` asserts over the
   commands that exist, so nothing breaks in the gap.

**Recommendation: 3.** This task builds the §8 renderer and the exit-code mapping
in full — those are the contract, and they are the substance of the entry — and
T18 spends one variant and one `AGENT.md` section on top of them. Cheap to
reverse: if the split is rejected, `connect` is one more variant over the same
renderer.

**Does the corrected entry read as two tasks? No — under this boundary it reads
as one.** One CLI seam, one output contract, one document. It would read as two
if `connect` had to route here, which is exactly what the PROPOSED above
declines.

**Carried from T15's PROPOSED, accepted: one resolver on the agent-facing path.**
`cli::edit::address` already resolves the whole identifier or a prefix of at
least eight characters, refusing an ambiguous one with the matches listed.
`edit::wire` had to restate the rule because nothing below the command layer may
depend on `Failure`. This task routes the caller's text through `address` and
hands the library a resolved identifier, leaving the library's own copy as
belt-and-braces for a caller that did not come through the CLI. Both ends'
documentation says it is the same rule.

**Carried from T10's PROPOSED, and it moves with `connect`.** Whether
`Search::expanded()` fills `alternatives_considered` when the search rather than
the shapes produced the route is decided where a report is filled from a search —
which is now T18, not here. This task renders whatever the field holds.

**`tests/command_surface.rs` is free.** It is held by one task at a time; the
delta view (T17) has merged, so this task takes it.

- **Check:** `cargo test --test command_surface` — every wire verb parses, and
  every refusal exits the code its row names.
- **Check:** `cargo test --test wire_output_contract` — one golden per status,
  text and JSON, with the JSON keys exactly §8's. The `labels` and `blocked`
  goldens are rendered from constructed `Report` values, because the label
  proposal (T13) and the join (T18) are what produce those statuses from a
  drawing — a contract test measures the rendering, and it says so where it
  constructs.
- **Check:** `cargo test agent_doc_covers_every_command` — `AGENT.md` documents
  both wire verbs and the `[routing]` section.

#### Implemented, 2026-08-15 — NOT TICKED, awaiting tick review

Branch `worktree-agent-a8e75981ddd6a58db`, one commit `2861254` on base
`9b010b3`. **The worktree arrived on a stale base** — `d62aa69`, an ancestor of
`9b010b3` — with a clean tree and no local commits, so it was fast-forwarded
before any work started. That is the third stale dispatch of this phase.

The lane's own `cargo xtask check` passes: **fmt, clippy, test, doc, deny,
clean — all six**. Corpus and environment gates were not run; per `CLAUDE.md`
they do not count toward done in a lane.

**What was built.** `kicli wire draw` and `kicli wire delete` over the library
verbs that merged without a CLI; the `wire` noun in `cli/args.rs`; the §8
renderer in `cli/edit/wire/contract.rs`; `ExitCode::for_route` in
`cli/exit.rs`; `address::wire` in `cli/edit/address.rs`; `AGENT.md`'s two verb
sections and its `[routing]` section. `route/report.rs` was rendered and not
touched — the freeze held.

##### The calls this task had to make, each PROPOSED with its evidence

1. **`wire draw`'s ends are six flags, not one that guesses.**
   `--from-pin|--from-port|--from-at` and the three `--to-*`, each end a
   required argument group. Evidence: one `--from <END>` flag would have to
   decide from the text whether `A.1` is a pin or a port name, and a KiCad port
   name can hold a stop; `junction add --at|--pin` is the surface's existing
   answer to "two forms of one address" and it is a required group too. The
   surface rule `a_verb_that_makes_an_object_takes_no_positional` also forbids
   the research doc's `wire draw <pin> <x,y> … <pin>` positional form, so some
   flag shape was forced. Cost: seven flags to document. Recommendation:
   accept. Cheap to reverse — a single `--from` could be added later as an
   alias without moving anything.

2. **The text form of the cost uses the JSON's words: `length` and
   `proximity`, not §8's `len` and `near`.** `research/wire-routing.md` §8's
   sample line reads `cost 62 = len 30 + turns 12 + crossings 20 + text 0 +
   near 0`, and its JSON reads `"length"` and `"proximity"`. Evidence for
   deviating: `Cost::parts`'s own rustdoc, frozen in Phase 1, says "the field
   names are the ones the output contract writes"; `ENGINEERING.md`'s STE rule
   is one term per concept; and this milestone has already settled exactly this
   shape of conflict once — "Resolved — one term, one vocabulary" ruled that the
   agent-facing surface carries one word and the research doc's name was the
   stale one. Two words for one number on the surface an agent copies from is
   what that ruling exists to prevent. **The JSON key set is unchanged**; only
   §8's sample text line differs from what kicli prints. Recommendation:
   accept, and amend §8's sample line to match in a later commit —
   `research/` is outside this lane's scope so it was not edited here.

3. **The JSON carries three keys beyond §8's routed example, at every
   status: `reason`, `labels`, `blocked_by`.** §8's example is the `routed`
   case; its prose names `reason` ("`reason` continues to say the same thing in
   English for a person"), and §5.5 is where a proposal's pair and §4 is where
   the blocking objects come from. Every key is present at every status —
   empty list, or null — so a caller parses one shape and never branches on
   which keys came back. Recommendation: accept. The alternative, omitting keys
   by status, makes an agent test for presence before reading, which is what a
   stable contract exists to avoid.

4. **A refused `wire draw` answers in M3's refusal convention, not in the §8
   contract.** One sentence on stderr, exit 1, stdout empty — exactly like
   every other kicli verb, and exactly what `edit::wire` already produced when
   it merged without a CLI. §8's `blocked` and `invalid` statuses are rendered
   by the renderer this task built, and the verb that produces such a `Report`
   from a search is the join (T18). Evidence that nothing is lost today: the
   `Blocked` refusal message already names the obstacle
   (`U1 blocks the wire at (50.8,45.72), on the segment from …`), which is
   §4's "blocked plus a list of what blocked it". **Open question left to the
   join (T18): whether a blocked route should print the contract on stdout
   beside its non-zero exit.** Recommendation: decide it in T18, where a
   producer exists; changing it here would have set the precedent from the one
   verb that has no search.

5. **`cli.rs` was edited, two lines, outside the lane's listed file scope and
   unavoidable** — the `Command::Wire` dispatch arm and the `WireVerb` /
   `DrawArgs` re-export, without which `edit::wire::run`'s signature is not
   publicly reachable. Same shape as the delta view (T17)'s accepted one-line
   excursion. Recommendation: accept.

6. **`tests/agent_doc.rs` gained one test**, `agent_doc_covers_the_routing_settings`,
   outside the lane's listed file scope, about thirty lines. Evidence for
   needing it: `agent_doc_covers_every_command` checks command names only, so
   the entry's third check — "`AGENT.md` documents … the `[routing]` section" —
   has no executable twin without it. Same reasoning the delta view (T17)'s
   third excursion was kept under. Recommendation: keep.

7. **A fifth golden, `routed_adjusted`, beyond the entry's "one per status".**
   The field the contract gained today (`adjusted`) is empty in all four
   status goldens, because nothing that produces a `Report` in this build ever
   moves a terminal — four-way avoidance is the search's, and `wire draw` takes
   the corners it is given. A field seen only empty has never been seen
   rendered. Recommendation: accept.

##### The task text yields to measured reality on one point

The entry's second check says the `labels` and `blocked` goldens are
constructed. **`invalid` had to be constructed for the same reason**, which the
entry does not say: `wire draw`'s invalid requests come back as `WireError`,
which the command layer renders as a refusal, so no code path in this build
builds an `invalid` `Report` either. Measured: `WireError::status()` returns
`Status::Invalid` and `refused()` turns it into a `Failure`, never into a
`Report`. The test says so at each construction site, and the `routed` golden
**is** produced by a real `edit::wire::draw` over a probe drawing, so the
renderer is measured against what the verb hands it rather than only against
values written to suit it.

##### Falsification — every break made alone, watched, and restored

Baseline before any break, and re-measured green after every restore:
`command_surface` 22 passed, `wire_output_contract` 13 passed, `agent_doc` 6
passed, `kicli` lib `cli::` 30 passed. The good state was committed as
`2861254` **before the first break**, so every restore was
`git checkout -- <one path>` against a commit, and `git status --porcelain`
was checked empty after each.

| # | What was broken | Which assertion caught it | Observed |
|---|---|---|---|
| 1 | `ExitCode::for_route` made `Labels` a failure | `cli::exit::tests::a_proposal_leaves_the_same_code_as_a_drawn_route` | fails: `left: Operation`, `right: Success`, on "labels is an answer" |
| 1 | the same break | `command_surface::every_route_status_exits_the_code_its_row_names` | fails on "labels exits 0 (success)" |
| 1 | the same break | the `for_route` doctest | fails |
| 2 | JSON key `length_mm` misspelled `length` | `wire_output_contract::every_status_answers_with_the_same_key_set` | fails on "the key set of routed" |
| 2 | the same break | all five goldens | fail on "the JSON form of …" |
| 3 | the JSON `adjusted` key deleted entirely | `every_status_answers_with_the_same_key_set`, `the_lists_carry_the_keys_their_entries_are_read_by`, `a_displacement_is_a_displacement_and_not_a_position`, all five goldens, and both `contract::tests` adjusted checks | 8 of 13 integration checks fail, 2 of 6 unit checks fail |
| 4 | the `adjusted` **text** line printed even when empty | `contract::tests::a_route_that_moved_nothing_prints_no_adjusted_line` | fails |
| 4 | the same break | the four text goldens without an adjustment | fail on "the text form of …" |
| 5 | `adjusted.by` rendered as the path's end (a position) instead of the displacement | `a_displacement_is_a_displacement_and_not_a_position` | fails: "the displacement, not 76.2,50.8" |
| 5 | the same break | `a_moved_terminal_matches_the_golden` | fails on the JSON form |
| 6 | `wire delete` bypassing `address::wire`, passing the caller's text straight to the library | `command_surface::a_deleted_segment_is_named_through_the_command_layer_resolver` | fails: the refusal read "names 2 **segments**" (the library's words) against the expected "names 2 **objects**" (the command layer's) |
| 6 | the same break — **and this half caught nothing** | the same test's "a handle a junction shares still names one segment" arm | **passes.** Case 1, not case 2: the library judges ambiguity over segments alone too, deliberately, so that arm cannot discriminate which resolver answered. It discriminates a different break — see row 7 — and is kept for that |
| 7 | `address::wire` judging over every object rather than the segments alone | `address::tests::a_segment_is_judged_ambiguous_over_the_segments_alone`, `a_segment_that_is_not_there_is_refused_without_offering_a_reference`, and the surface test's junction-handle arm | 2 unit checks and the integration arm fail; the refusal read "01000001 names 1 objects of this sheet" |
| 8 | the `[routing]` section deleted from `AGENT.md` | `agent_doc_covers_the_routing_settings` | fails: "AGENT.md has no routing section" |
| 9 | the `kicli wire draw` **section** deleted from `AGENT.md` | `agent_doc_covers_every_command` | **passes** — see the finding below |
| 9 | the same break | `agent_doc_covers_every_verb_flag` | fails: "AGENT.md does not document --from-pin of `wire draw`" |
| 9b | every mention of `kicli wire draw` removed | `agent_doc_covers_every_command` | fails: "AGENT.md does not document `kicli wire draw`" |
| 10 | the stranded-junction notes dropped from `wire delete` | `a_deleted_segment_is_named_through_the_command_layer_resolver` | fails on "the caller is told what the removal left behind" |
| 11 | `refused()` mapping every wire refusal to `Success` | `cli::edit::wire::tests::a_refusal_takes_the_row_its_status_names` and `command_surface::every_refusal_exits_with_the_code_its_row_names` | both fail; the surface one on the off-grid vertex row |
| 12 | the contract block no longer prepended to `wire draw`'s text | `a_drawn_wire_answers_in_the_contract_and_in_the_mutation_result` | fails on "the contract answers first" |
| 13 | the `wire` key dropped from `wire draw`'s JSON | the same test | fails on "the noun's key carries the contract" |
| 14 | every JSON cost part written as `0` | `contract::tests::the_total_is_the_sum_of_the_parts_it_is_printed_beside` and the two `routed` goldens | fail. The integration check of the same name **passes**: it watches the text form, which reads `Cost::parts` directly. Two checks of one property over two forms, and only one form was broken |
| 15 | `DrawArgs::corners` returning an empty list, so `--via` is ignored | `a_drawn_wire_answers_in_the_contract_and_in_the_mutation_result` | fails: the escape rule refuses the straight line that is left |

**Row 9 is a finding about the instrument, and it is case 1.** Deleting the
`wire draw` section left `agent_doc_covers_every_command` green, because the
`[routing]` section names both verbs in prose and the check tests for the
string anywhere in the document. It is not blind — 9b removed the last mention
and it failed with the predicted message — but it is **weaker than its name
suggests**: it proves a verb is named somewhere, not that it is documented.
`agent_doc_covers_every_verb_flag` is what actually holds a verb's section in
place. PROPOSED, and not fixed here because the check is the delta view (T17)'s
and outside this lane: **`agent_doc_covers_every_command` should assert a
heading, not a mention.** Recommendation: record as a chore; it costs one line
and it removes a false sense of coverage from the milestone's exit criteria.

##### Two things a reviewer should check rather than take on trust

- The `routed` golden's identifiers come from `Identifiers::for_document`, so
  they are derived from the probe's bytes and the request seed. If a probe or
  the seed changes, the golden moves. That is a golden working, not a golden
  breaking.

  **This paragraph was wrong, and it is left standing because how it was wrong
  is the finding.** Corrected beneath it, 2026-08-15, after the merge failed —
  see "The merge failed on the two `routed` goldens" below. The mechanism named
  here is right; the inference is not. The seed is not only "the probe's bytes
  and the request seed": `edit::wire::draw` builds it as
  `format!("{} wire {} to {}", target.path.display(), …)`, so it **starts with
  the file's absolute path**. The golden therefore moves between one checkout
  and the next, which is not a golden working. I had read that line while
  studying the file and did not read it again while reasoning about the golden.
- `crates/kicli/src/route/report.rs` is byte-identical to `9b010b3`. The freeze
  held and no hook fired.

#### The merge failed on the two `routed` goldens, 2026-08-15 — cause, fix, and why the lane was green

The orchestrator merged this lane, ran the six gates on the merged result, and
the **test** gate failed on `a_drawn_route_matches_the_golden` and
`a_moved_terminal_matches_the_golden`. The merge was backed out. Fixed on top
of the original commits in `978ad2e`; the lane is green again and the contract
test was run three times in a row to show it.

**The cause, measured rather than inferred.** `edit::wire::draw` seeds each new
identifier with

```
format!("{} wire {} to {}", target.path.display(), from.name, to.name)
```

and `Identifiers` hashes that seed with SHA-256. **The identifiers are not
random** — `edit::insert`'s own module doc says so outright ("derived from a
seed rather than read from a random source, so … a test is repeatable"). They
are a function of the file's **absolute path**. Recomputing the hash by hand
outside the test reproduces both sides exactly:

| Seed path | First identifier |
|---|---|
| `…/.claude/worktrees/agent-a8e75981ddd6a58db/target/tmp/wire-contract/routed-golden/probe.kicad_sch` | `ebb43fde-5c3c-4534-8776-e35e3f8aefaf` — the committed golden |
| `/Users/james/code/kicli/target/tmp/wire-contract/routed-golden/probe.kicad_sch` | `9d618919-5152-4a4e-8860-23f4945012d3` — what the merged run produced |

All three of the orchestrator's observed values are what the main-checkout path
predicts, to the digit.

**One correction to the report, on the evidence.** The report says "I ran it
twice and got two different sets, so this is not … anything about my checkout".
The hash is a pure function of the seed, so one checkout at one path cannot
produce two sets; two different sets means two different paths. The conclusion
it was used to support — that freezing these values is wrong — is right anyway,
and by a stronger argument: the values are *stable* per checkout and *wrong*
everywhere but one, which is worse than random, because random fails loudly on
the second run and this failed only on somebody else's machine.

**Why the lane reported six green gates, plainly.** The gates were green, on
byte-for-byte the state that was committed — `git status --porcelain` was empty
and `git diff --stat HEAD` was empty before `cargo xtask check` ran, and the
golden-regeneration scaffold had been removed before either commit. Nothing was
refreshed inside the checked run. **The green was real and it was worthless**,
because the check asserted a property of this worktree's location rather than a
property of the contract, and this worktree is where it ran.

The falsification discipline did not catch it, and it is worth being exact
about why. Rows 2, 3, 4, 5 and 14 of the table above all broke the renderer and
all failed these same two goldens, so the goldens **were** shown capable of
failing. That is necessary and not sufficient: a check can be falsifiable and
environment-dependent at the same time, and the procedure as written only tests
the first. **Every break was made in the source; none was made in the
environment.** PROPOSED, as a lesson for the skill rather than a fix here:
`falsification-control` should name environment variation — path, clock,
locale, run order — as a break worth making against any check that consumes a
generated value. Recommendation: add it as a worked example, this being one.

**The reproduction, in this worktree.** Changing only the probe's scratch
directory name — which changes the absolute path in the seed, exactly as
another checkout does — makes the pre-fix commit `dec0c0a` fail with the
orchestrator's two tests and no others. The control that the experiment varied
anything: under the changed path the drawing's written identifiers are
`fa9bd366…`, `6ebfadf1…`, `9b63e57e…` against `ebb43fde…`, `d42bd368…`,
`85a91ae2…` under the original. With the fix in place the same changed path
passes all 13.

**The fix.** `matches_golden` compares both forms through
`without_generated_identifiers`, which replaces each **distinct**
identifier-shaped string with `<id-1>`, `<id-2>` … numbered in first-appearance
order. The count and the ordering are still asserted; the shape is asserted
separately on the **real** values by `what_was_added_is_named_by_identifier`,
which now checks the full `8-4-4-4-12` shape and that no two segments share an
identifier, so normalising hides nothing. `added` was not dropped, and
`wire draw` was not changed to produce fixed identifiers — generating fresh
ones is correct behaviour.

**Which goldens carried the hazard.** Measured by grep for the identifier
shape, not assumed: `routed` (3) and `routed_adjusted` (3) carried it;
`labels`, `blocked` and `invalid` carried **none** — their `added.wires` is
empty and their only hex-looking string is the hand-written eight-character
`da5aa983`, which is shorter than the shape the normaliser matches and is still
compared verbatim.

##### Falsification of the normalisation, which is now load-bearing

| What was broken | Which assertion caught it | Observed |
|---|---|---|
| the golden given two identifiers where three belong | `a_drawn_route_matches_the_golden` | fails on "the JSON form of routed" |
| the golden given the three in the wrong order (`<id-2>, <id-1>, <id-3>`) | the same | fails on "the JSON form of routed" |
| the probe's scratch path changed, so every identifier differs | **nothing — and that is the fix working.** All 13 pass. The anti-vacuity control is the written file: its identifiers really did change | passes |
| the same path change, against the pre-fix commit `dec0c0a` | `a_drawn_route_matches_the_golden`, `a_moved_terminal_matches_the_golden` | both fail — the merge failure, reproduced here |
| `what_was_added_is_named_by_identifier` is what holds the shape now, so: the normaliser made to emit `<id>` for every identifier | the **ordering** check | see below |

**The collapsing normaliser hides one of the two properties, not both.** The
brief predicted it would hide both. Measured, it does not: with `<id>` for
everything, dropping an entry still changes the count and still fails. What it
destroys is the **ordering** — with three identical tokens a swap is not merely
undetected, it is unrepresentable, so there is no golden to write that would
fail. That is precisely the property per-distinct numbering exists to keep, and
it is why the placeholders are numbered rather than uniform.

### T17 — `sch view --view delta`, the command M3 left unwired (Lane B) ✅

The machinery exists and the semantics are ruled; only the verb is missing.
`view::delta` compares two snapshots, `view::snapshot` takes, writes and reads
one, and every mutation already updates `@last-write`.

The delta answers **"what has touched this file since kicli last wrote it?"** It
is empty immediately after a mutation, by design, because that mutation's own
result already reported what it changed. The command's help says so, in those
words, because the spec itself expected the other reading.

Two things the command must state rather than let a caller assume. A comparison
against a **file** degrades to hashes and names, because a snapshot file does
not hold the old values an in-memory comparison prints — so the output says
which form it is. And a project kicli has never written has no snapshot: that is
a structured error naming `@last-write`, not an empty delta.

`--against <name>` compares against a named snapshot; the default is
`@last-write`. The output obeys the §7.4 budget model like the other two views.

- **Check:** `cargo test --test delta_view` — a mutation followed by a delta
  reports nothing changed; an edit written to the file behind kicli's back, the
  way an editor makes one, reports exactly that object; the JSON twin carries
  the same content as the text form.
- **Check:** `cargo test --test command_surface` — `--view delta` parses, and a
  project with no snapshot exits the code its table row names.
- **Check:** `cargo test agent_doc_covers_every_command` — `AGENT.md` documents
  the verb and the distinction it draws with the mutation result.

#### Implemented and merged, 2026-08-15 — NOT TICKED, awaiting tick review

The lane's own six gates passed, the merged result passes the six gates, the
corpus run and the environment-gated run with the netlist oracle at 35 of 35.
It is **not ticked**: the tick-review practice adopted this session requires a
reviewer with fresh context reading the entry and the diff, and the session hit
its token limit before one ran. The next session dispatches the reviewer against
this entry and commit `71bb1d6`, and ticks only on APPROVE.

**The entry was right, and that is itself the finding: the machinery existed and
only the verb was missing.** `view::delta` and `view::snapshot` were not
modified. This was wiring, output shape and documentation.

**The task text yields to measured reality on the degraded form.** The entry
says a comparison against a **file** "degrades to hashes and names". That was
true before `spec/SPEC.md` §7.3's 2026-08-13 amendment added the display column;
it is not true of a snapshot file this version writes. The existing check
`a_delta_against_a_saved_state_reads_like_one_against_a_design` already shows a
file-based delta reading identically to a design-based one. The degradation is
real but narrower: a snapshot written **before** the column existed carries four
columns, and then a line can name the object and say `edited` and no more. So
the header prints `compared=values|hashes`, measured from the state actually
read, and `a_delta_against_a_state_of_hashes_alone_says_so` covers the narrow
form. A caller never has to assume which form it holds.

Other decisions, each recorded where the code makes them: a saved state covers
one sheet, because a mutation writes one file, so the header names the sheet and
`--sheet` naming any other path is refused rather than answered; the JSON twin
carries `from`, `to`, `sheet`, `scope`, `compared`, `changed[]` — with the same
four keys the mutation result uses — and `unchanged`; `--include-power`,
`--uuids` and `--against` say on stderr when they do not apply, on the
`--variant` precedent, rather than being ignored in silence.

**Every check was shown capable of failing**, each by breaking the code it
watches and watching it fail:

| Check | What was broken | Result |
|---|---|---|
| `a_delta_right_after_a_mutation_reports_nothing_changed` | the saved state read as empty | fails, every object reported as added |
| its anti-vacuity control | `Delta::between(&saved, &saved)` — reports nothing, ever | the check above **passes**; the control below fails |
| `a_delta_reports_an_edit_made_outside_kicli` | the same "reports nothing ever" break | fails: `[]` against the expected edit line |
| `a_delta_says_which_state_it_compared_and_how_much_that_state_holds` | the header keys dropped | fails |
| `a_delta_against_a_state_of_hashes_alone_says_so` | `compared_form` always answers `values` | fails |
| `a_delta_larger_than_the_budget_falls_back_to_a_summary` | the fallback removed | fails |
| `the_delta_json_twin_carries_what_the_text_carries` | JSON `detail` emitted empty | fails on the line diff |
| `a_delta_with_no_saved_state_exits_the_code_its_row_names` | `ExitCode::File` for `ExitCode::Operation` | fails, 4 against 1 |
| `the_delta_view_help_says_which_question_it_answers` | "by design" removed from the help | fails |
| `agent_doc_states_what_the_spec_requires_it_to_state` | the distinction sentence reworded | fails |

**The vacuity control earned its place on the day it was written.** The
"reports nothing changed" check passes against an implementation that reports
nothing ever. Its control — the same harness reporting an edit made behind
kicli's back — is what makes the pair evidence. This is the exact failure the
standing rule exists to catch, caught.

**Six calls the entry did not decide. Proposed at the moment of the claim, and
all six PROMOTED by the advisor's ruling on the interrupted stop (R7), on the
evidence recorded below.** The scope excursions in (3) and (4) were promoted
with the note that proposing them was correct where staying silent would not
have been.

1. **A missing saved state exits 1 (`Operation`), and a malformed one exits 4
   (`File`).** Evidence: §6.1's row for 1 reads "a well-formed request that
   could not be completed"; `locate.rs` already uses `Operation` for "holds no
   KiCad project" and `File` for a named file that will not read, and
   `code_for_snapshot` already uses 4 for a snapshot that will not parse.
   Absent and malformed are different failures. Recommendation: accept.
2. **`--sheet` disagreeing with the saved state is refused, exit 1.** The
   alternative is answering about a sheet the state does not cover.
   Recommendation: accept.
3. **`tests/agent_doc.rs` gained three assertions**, outside the lane's listed
   scope, about ten lines. `agent_doc_covers_every_command` checks command names
   only, so it would pass with the distinction deleted from `AGENT.md`.
   Recommendation: keep — it is what makes the entry's third check capable of
   failing.
4. **`cli.rs` was edited, one line, outside the lane's listed scope and
   unavoidable**: the dispatch had to carry the new flag. The arm now passes the
   whole verb, so a future `sch` flag never touches `cli.rs` again.
   Recommendation: accept.
5. **The budget fallback is skipped when the summary would be larger than the
   lines.** Measured: at a 120-byte budget a two-line delta produced a 238-byte
   summary against a 180-byte full form. A fallback that costs more bytes than
   it saves is the opposite of what the budget is for. `view/scope.rs` has the
   same latent property for the index and was **not** touched, being out of
   scope. Recommendation: accept, and record the `scope.rs` twin as a chore.
6. **The current state carries no stamp.** kicli reads no clock of its own,
   `now()` is private to `cli/edit.rs`, and this state is compared and never
   written, so no stamp is ever printed. Commented where the code does it.

#### Tick review, 2026-08-15 — REJECT on the first pass, and the defect was the lane table's

The first tick reviewed under the practice, and it rejected. The verdict is
recorded here in full rather than summarised away, because a rejection the
orchestrator paraphrases into agreement is not a rejection.

**The reviewer's gap:** the diff modifies `crates/kicli/tests/command_surface.rs`
(62 lines, two new test functions), which is outside the file list Lane B was
given, and which the entry's six recorded decisions do not name as a scope
excursion. Two excursions were declared and ruled; this was a third, undeclared.

**The finding is real and it lands on the lane table, not on the implementer.**
This entry's own second check reads `cargo test --test command_surface` — the two
functions the reviewer flagged ARE that check. Writing them is executing the
task, not exceeding it. What is genuinely wrong is that the Phase 2 lane table
listed the files Lane B owns and omitted a file two of Lane B's own tasks are
required by their entries to write. The implementer had a check it could not
satisfy without touching a file its brief did not grant, and the brief never said
what to do about that.

Corrected above: `tests/command_surface.rs` joins Lane B's row and is held by one
task at a time, like `AGENT.md`, because the verb surface (T16) names the same
check. The reviewer's brief was also wrong — it presented the lane file list as
the answer to "does the diff exceed the entry's stated scope", when the entry's
scope is what the entry's checks require. Both are fixed before the re-review.

**Rejection one of two on this task**, counted, not waived. The re-review ran
against corrected inputs.

**Verdict on re-review: APPROVE.** A fresh reviewer checked every factual claim
in this entry against the source as it stands — the two exit codes, the `--sheet`
refusal, the fallback skip, the reused timestamp, the one-line `cli.rs` dispatch,
the three assertions inside `agent_doc_states_what_the_spec_requires_it_to_state`
rather than inside `agent_doc_covers_every_command`, and that `view::delta` and
`view::snapshot` are untouched. All held. **This is the tick.**

**The APPROVE was traced, not measured, and the reviewer said so unprompted.**
Its tool grant was read-only, so it could run nothing; it reported that plainly
instead of letting the brief's claim of a shell stand as evidence. A reviewer
that distinguishes "the table says it fails" from "I made it fail" is worth more
than one that blurs them, and the distinction is now in its definition.

**The three weighted rows were then measured separately, in a scratch copy
outside the repository, against a green baseline** — `command_surface` 17
passed, `delta_view` 11 passed, `agent_doc` 5 passed, before any break and again
after every revert. Each break made alone:

| Break | Check | Observed |
|---|---|---|
| `Delta::between(&saved, &saved)` | `a_delta_right_after_a_mutation_reports_nothing_changed` | **passes** — vacuous on its own, exactly as the entry claims |
| the same break | `a_delta_reports_an_edit_made_outside_kicli` | **fails**: `left: []` against `right: ["~ S R1.Value \"10k\" -> \"22k\""]` |
| `compared_form` always `values` | `a_delta_against_a_state_of_hashes_alone_says_so` | **fails** on the header line |
| `ExitCode::File` for the absent state | `a_delta_with_no_saved_state_exits_the_code_its_row_names` | **fails**: `left: 4`, `right: 1`, the predicted numbers |

**The anti-vacuity pair discriminates in both directions, and that is now
measured rather than asserted.** The entry's centrepiece claim was that its own
first check is vacuous and only the control makes the pair evidence. It is true.

**Carried, and it bites the moment the wire verbs land.**
`agent_doc_covers_every_command` asserts every noun and verb pair appears in
`AGENT.md`. When `kicli wire connect|draw|delete` merges, that check fails on
the merged result until `wire` is documented there. The verb surface (T16) owns
`AGENT.md` after this task, and it owes both its own `[routing]` section and
these three verbs.

---

## Phase 3 — join (orchestrator, main checkout)

### T18 — `wire connect <pin> <pin>`, and the junctions it emits

The router behind the verb. A route that terminates on the interior of an
existing same-net wire emits a junction; one that terminates at an existing
endpoint does not, because KiCad renders a corner there.

**Carried in from T7 — the target-cell exception, where it is measured against
KiCad.** T8's walk excepts the two ends and T10's A\* repeats the rule; this is
the task that routes to a real sheet pin and so shows it works on a drawing the
tool reads back. The terminus cell sits under the sheet body, which is a hard
block, so without the exception the route is refused at the very point it was
asked to reach.

- **Check:** `cargo test a_route_joins_the_two_pins_it_names` — the extractor
  agrees after the write, and the report's claimed net is the extractor's net.
- **Check:** `cargo test a_terminus_on_a_wire_interior_emits_a_junction` — and
  its pair, `a_terminus_at_an_endpoint_does_not`. The junction's causal role is
  measured by removing it and re-measuring, on T4's helper.
- **Check:** `KICLI_TEST_KICAD_CLI=1 cargo test kicad_agrees_about_the_route` —
  the netlist of the written file carries the join kicli reported, and no other
  net changed.

#### PARKED, 2026-08-21 — interrupted, uncommitted draft on `lane-t18`

**State: nothing committed. Branch `lane-t18` is at its base `b3a53d1`.** The
worktree holds an uncommitted draft touching `crates/kicli/src/cli/args.rs`,
`crates/kicli/src/cli/edit/wire.rs` and `crates/kicli/src/edit/wire.rs`, plus an
untracked `crates/kicli/tests/edit_wire_connect.rs`. The lane's last words were
"Now the test file", so the checks are the part that does not exist.

**As far as the record is concerned this task is not started.** Per CLAUDE.md, a
parked lane's uncommitted draft is **reference, not resumption**: a fresh
implementer starts from this entry, and any line adopted from the draft passes
the falsification discipline as if newly written. Nothing in that draft has been
shown capable of failing, and an unfalsified draft from a context that is gone
carries no evidence standing whatever it asserts.

**One thing the next implementer needs that is not in the entry above.**
`crates/kicli/src/cli/edit/wire.rs` is also touched by the parked handle chore
(C1), which removed a private `fn handle` at `:352`. Whichever lands first, the
other resolves against it.

**And the base moved.** `b3a53d1` no longer holds: `a2d27b4` merged the typed
label shape (C3), which changed `Probe::label_of_kind`'s signature at all 34 call
sites. Any new probe drawing this task writes uses `LabelKind`, not strings.

### T19 — `wire connect <pin> <net>`

The common agent request — "connect `U1.7` to `+3V3`" — and first-class, not a
composition (C13). The target set is every grid point on the net's wires plus
its pins; the shape fast path is tried against the nearest target first, and A*
runs with a multi-source heuristic.

- **Check:** `cargo test connecting_to_a_net_takes_the_nearest_terminal` — a net
  reachable at two points yields the cheaper, and the report says which point it
  joined.
- **Check:** `cargo test a_net_is_addressed_by_name_or_by_handle` — both forms
  resolve, and an ambiguous name is refused with the candidates listed.

### T20 — The re-route calibration gate, at 15 % ✅

#### Tick review, 2026-08-21 — APPROVE. TICKED

Reviewed from the entry and the diff `491c254..lane-t20`, merged `1af87e5`. The
reviewer pinned the range at both ends and confirmed it held.

**Measured by the reviewer rather than read** — recorded because the entry's
central claim is one nobody should take on trust:

- **The baseline reproduces byte-for-byte** in a checksum-verified scratch copy:
  82 pins, 40 strands, 2 skipped, 1126 → 1126, **+0.0 %**, both ruling reports
  0/0. The shape mix was **recomputed from the output rather than accepted**:
  **12 I, 22 L, 6 Z = 40**, 74 segments, exactly as claimed.
- **The fixture is what its recipe builds.** Under `KICLI_TEST_KICAD_CLI=1`, the
  committed sheet is genuinely what the recipe plus `kicad-cli sch upgrade
  --force` produces, and its netlist oracle is current.
- **The honesty guard fires.** Breaking the costing symmetry skips 82/82 pins and
  panics `assert_skipped_pins_are_a_minority` — **from the skip-guard assertion,
  not the tolerance one**, confirming the entry's claim that a broken symmetry
  makes strands *uncostable* rather than mis-priced. The tolerance assertion was
  shown to fail independently by doubling the re-routed cost.
- **The tautology claim is confirmed independently.** The reviewer perturbed
  `w_turn` from 6 to **6000 — a thousandfold** — and the fixture's deviation
  stayed at **exactly +0.0 %**.
- **The withdrawn claim is accurate, not a retreat.** Removing both pin sorts
  fails the two order checks while the tolerance checks do not notice; restoring
  them and reversing the file's object order produces an identical report.
- **The two zeroes are backed by working counters.** Both region predicates
  assert their true *and* false cases, so the zeroes are results rather than
  silence.

**Taken on the entry's word, and said so:** the `ampli_ht` corpus arm, because the
corpus was absent from the reviewer's scratch copy — *"that arm did not run for me
rather than failing"*. The orchestrator's merged run is the authority for it.

**Scope:** no file under `crates/kicli/src/` at all; the only hotspot is the
`MANIFEST` this lane was granted. Nothing exceeds the stated scope.

The PROPOSED finding this task produced — that the gate measures agreement rather
than calibration — is **strengthened, not weakened, by the approval**: the gate
does what its procedure says, and its procedure cannot answer the question the
exit criteria name. That is PROPOSED 9 in `tasks/reports/M4-phase3.md`.



The objective measure of whether the weights are right. Re-route every net of a
known-good sheet from scratch and assert the total cost is within 15 % of the
original.

The procedure is stated here because the gate is meaningless without it:

1. Take each net with two or more pins on one sheet and no bus involvement.
2. Cost the net's **existing** wires with the same cost function, against the
   sheet with that net's own wires removed — so the original and the re-route
   see the same obstacles.
3. Re-route: build a spanning tree over the net's pins by Manhattan distance,
   ties broken by `(x, y)`; route each tree edge in order; add each result to
   the obstacle map before the next edge.
4. Sum both over the sheet. Assert the deviation is within 15 % **in either
   direction**. A result far under the human's cost is evidence of divergence
   and not a win: the likely cause is a cost function that fails to price
   something the person was avoiding, such as a crossing or a label the route
   runs through. Cheap is an anomaly to explain, never a number to bank.
5. Report the per-net deviation, and **name every net skipped and why**. Assert
   the skipped pins are under a quarter of the sheet's pins, so the gate cannot
   pass by skipping the hard nets.

Two sheets. A purpose-built in-repo fixture gates the default run — tidy, at
least 20 nets and 40 segments, a mix of I, L and Z shapes, canonicalised with
`kicad-cli sch upgrade --force`. The tidy hand-drawn demo sheet `ampli_ht`
measured in `research/wire-routing.md` §2 gates the corpus run.

If either sheet falls outside 15 %, the task reports the deviation and parks it.
It does not move a weight: the weights are M5's too.

**Two T7 rulings name this gate as their trigger to revisit, so it reports on
both whatever the deviation is.**

1. **The border region.** The window is clipped to the paper and nothing is
   deducted for the border and the title block, because a wire there is legal in
   the file and kicli invents no boundary the editor does not draw. If calibrated
   routes enter the border region, the deduction comes back **as a cost, not as
   a wall**. Report how many re-routed segments land inside it.
2. **Bus entries.** They are left out of the obstacle map, being diagonals the
   lattice cannot describe. If calibrated routes thread unrealistically close to
   one, that treatment reopens. Report any re-routed segment that passes within
   1 G of a bus entry.

- **Check:** `cargo test re_routing_a_known_good_sheet_costs_what_the_original_did`.
- **Check:** `cargo test --features corpus re_routing_a_demo_sheet_costs_what_the_original_did`.

#### PARKED, 2026-08-21 — interrupted, uncommitted draft on `lane-t20`

**State: nothing committed. Branch `lane-t20` is at its base `aa96263`.** The
worktree holds a modified `crates/kicli/tests/fixtures/MANIFEST`, an untracked
`crates/kicli/tests/fixtures/sch/routing/` and an untracked
`crates/kicli/tests/route_calibration.rs`. The lane's last words were "Now let me
generate the fixture", so the purpose-built sheet — the bulk of this task — was
not made.

**As far as the record is concerned this task is not started**, on the same
reasoning as the join above: the draft is reference, not resumption, and no part
of it has been falsified.

**The fixture is the long pole and the reason this lane was dispatched first.**
It needs at least 20 nets and 40 segments, a mix of I, L and Z shapes,
canonicalised with `kicad-cli sch upgrade --force`, and genuinely tidy — a
fixture that is not tidy makes the gate measure the fixture rather than the
weights.

**The MANIFEST ownership grant stands for whoever picks this up.** This lane was
made the sole designated owner of `crates/kicli/tests/fixtures/MANIFEST` for the
session, as an explicit exception to the merge-hotspot rule. Re-grant it with the
next dispatch or the lane will correctly stop and report.

**The base moved.** `aa96263` no longer holds: `b3a53d1` changed
`routing.label_threshold`'s default to 300 G = 381 mm, and `a2d27b4` changed
`Probe::label_of_kind` to take a typed `LabelKind`. Both bear on this task — the
first because a re-routed net over 381 mm is proposed as labels rather than
drawn, which the calibration procedure must account for or explain.


#### Done, 2026-08-21

**Both sheets are inside 15 %, and neither weight was touched.** The
purpose-built fixture re-routes at **+0.0 %**, `ampli_ht` at **+1.1 %**. Nothing
is parked for James on the tolerance. What is raised is a PROPOSED item about
how much this gate can tell anybody about the weights, below.

**The unit of measurement is the strand, not the net, and that is the one
substantive reading of the task text.** Step 1 says "each net with two or more
pins on one sheet". A net is often not a drawing: a ground net is one net and a
dozen separate two-pin strands, joined by the value of a power symbol rather
than by any wire, and there is nothing to re-route between two pieces nobody
drew as one. So the gate works on the **connected piece of drawn wire** — the
thing a person sat down and drew — found with KiCad's geometric merge rules
alone (shared connection point; a junction joins every wire through its
position). The name-based rules are deliberately left out, because a name is not
a drawing. On `ampli_ht` this raises the measurement from a handful of nets to
35 strands covering 80 of 87 pins; net-grained selection would have skipped every
power net and failed the quarter-of-the-pins assertion outright.

**Both sides go through one function.** A person's wiring is a *tree* of
segments, not a path, so it cannot be handed to `Tally::of_path` as it stands.
Both the drawn strand and the re-routed one are reduced to the same thing — the
set of unit grid edges the drawing covers — and costed by `cost_of_drawing`.
Length is the number of distinct unit edges; a **corner** is a grid point where
exactly two edges meet and they are perpendicular, which is what KiCad draws a
bend at (three or more meeting is a branch carrying a junction dot; two collinear
is one straight run KiCad split into two records); crossings, text steps and
crowded steps are counted by entering each point once, along the edge that
reached it, walking out from the drawing's smallest point — `Tally::of_path`'s
rule that a walk does not enter the point it starts from, generalised from a path
to a tree. Neither side gets arithmetic of its own, which is what makes this a
comparison rather than two measurements.

**Step 2's "the same obstacles" is implemented through the seam the adapter
already has, plus one extension the task text does not mention.** The strand's
own **wires** are named through `Routed`, which the map treats as free to enter —
the same verdict as removing them, and `Treatment::Terminate` costs nothing. Its
own **junctions, no-connects and labels** have no such seam and are removed from
the typed item list before `SheetObjects::read`. That is not decoration: a
junction on the strand's own wire is a hard block standing exactly where the
person's wire runs, so leaving it in refuses the person their own drawing and
denies the router the same ground. `ampli_ht` carries 13 junctions and the
measurement is impossible without this.

**Four smaller decisions the entry did not settle, recorded where they were
made:**

1. **The window.** The original is costed over a window sized to the whole
   strand; each re-routed edge gets the request-sized window the router really
   builds — the bounding box of its two terminals, inflated by `routing.margin`,
   clipped to the page. The strand window contains every edge window, so both
   sides see the same verdicts; asking the router for a wider window would be
   asking it an easier question than the verb asks.
2. **Every pin of the strand is a terminal**, not only the two ends of the edge
   being routed. A re-route of a whole strand owns all of its pins, and leaving
   the others as foreign pins with 1 G halos would refuse routes the person drew.
3. **`Approach::of` is not used.** It moves a terminal off a four-way point by
   reading `wire_ends_at` on the schematic, and the schematic still holds the
   strand's own wires — so it would settle against a drawing that is, for this
   measurement, not there. After the strand's wires are named as the route's own,
   its pins carry no wire ends at all.
4. **`routing.label_threshold`, which moved to 300 G = 381 mm under this lane
   (`b3a53d1`).** A connection whose best path is over it is **proposed as a pair
   of labels, not drawn**. A proposal draws no wire, so there is no wire cost to
   set against the person's, and costing it as zero is exactly the "cheap is an
   anomaly" failure this gate warns of. Such a strand therefore leaves the
   measurement, named with `Trigger::reason`, and its pins count against the
   quarter like any other skip. **Neither sheet triggers it**: the longest
   re-routed edge on either is well under 381 mm.

##### The fixture

`crates/kicli/tests/fixtures/sch/routing/calibration.kicad_sch`, A4, **82 pins,
40 measured strands, 74 measured segments** — over the 20 and 40 the task asks
for. Four bands: straight runs and L-shapes to ground; four staggered chips
joined by six Z-shapes; three groups of a chip and a chip turned on its side,
joined by L-shapes; and one chip feeding a bus through two bus entries, which is
the part the gate is expected to decline. Shape mix: 12 I, 6 Z, 22 L.

**The fixture is drawn by a recipe in the test file, and the drawing rule was
fixed before the router was ever consulted**: every connection is the shortest
orthogonal path with the fewest corners that honours both pins' escape
directions, laid clear of the symbol bodies. **A fixture adjusted until the
router agrees with it measures nothing**, so the rule came first and the number
second; no wire was moved after a measurement. It is canonicalised with
`kicad-cli sch upgrade --force`, and
`the_calibration_fixture_is_what_the_recipe_builds` compares the committed bytes
against a fresh build under `KICLI_TEST_KICAD_CLI` — a fixture and a recipe that
have drifted apart are two claims about one drawing.

**Its expectations are verified against KiCad, not hand-asserted.** The oracle is
`calibration.netlist`, KiCad's own answer;
`the_calibration_fixture_partitions_as_kicad_says` holds kicli's partition to it
and `the_calibration_oracle_is_current` re-exports and compares. Both MANIFEST
rows are added under this lane's ownership grant.

**Two limitations of the fixture, recorded rather than hidden.** It draws no
crossing, so `w_cross` reaches its numbers only through `ampli_ht`'s two
crossing strands; and its labels and its one free text sit clear of every route,
so `w_text` is in the map but not in any measured cost. Both are visible in the
table as `xing 0` and `text 0` on every fixture row.

##### The reported output, in full

```text
=== re-route calibration: calibration.kicad_sch ===
page 2970022 × 2100072 iu, 82 pins, 40 strands measured, 2 skipped
weights: w_len 1, w_turn 6, w_cross 20, w_text 12, w_near 2; margin 101600 iu, u_max 76200 iu, label_threshold 3810000 iu

strand                 pins segs |   orig step turn xing text |  route step turn xing text | deviation
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     32   22    1    0    0 |     32   22    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
GND                       2    2 |     22   12    1    0    0 |     22   12    1    0    0 |   +0.0 %
BAND1_0                   2    1 |     24   20    0    0    0 |     24   20    0    0    0 |   +0.0 %
BAND1_1                   2    1 |     24   20    0    0    0 |     24   20    0    0    0 |   +0.0 %
#n10                      2    3 |     44   28    2    0    0 |     44   28    2    0    0 |   +0.0 %
#n11                      2    3 |     44   28    2    0    0 |     44   28    2    0    0 |   +0.0 %
#n2                       2    2 |     48   38    1    0    0 |     48   38    1    0    0 |   +0.0 %
#n1                       2    2 |     32   22    1    0    0 |     32   22    1    0    0 |   +0.0 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
#n7                       2    1 |     24   20    0    0    0 |     24   20    0    0    0 |   +0.0 %
#n8                       2    1 |     24   20    0    0    0 |     24   20    0    0    0 |   +0.0 %
#n12                      2    3 |     44   28    2    0    0 |     44   28    2    0    0 |   +0.0 %
#n13                      2    3 |     44   28    2    0    0 |     44   28    2    0    0 |   +0.0 %
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
#n3                       2    2 |     48   38    1    0    0 |     48   38    1    0    0 |   +0.0 %
#n4                       2    2 |     32   22    1    0    0 |     32   22    1    0    0 |   +0.0 %
BAND1_2                   2    1 |     24   20    0    0    0 |     24   20    0    0    0 |   +0.0 %
#n9                       2    1 |     24   20    0    0    0 |     24   20    0    0    0 |   +0.0 %
#n14                      2    3 |     44   28    2    0    0 |     44   28    2    0    0 |   +0.0 %
#n15                      2    3 |     44   28    2    0    0 |     44   28    2    0    0 |   +0.0 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
GND                       2    2 |     28   18    1    0    0 |     28   18    1    0    0 |   +0.0 %
GND                       2    2 |     24   14    1    0    0 |     24   14    1    0    0 |   +0.0 %
#n5                       2    2 |     48   38    1    0    0 |     48   38    1    0    0 |   +0.0 %
#n6                       2    2 |     32   22    1    0    0 |     32   22    1    0    0 |   +0.0 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %

total: original 1126, re-routed 1126, deviation +0.0 % (tolerance ±15 %)

skipped strands, with the reason for each:
  #n16 (1 pins): a wire end at 81.28,172.72 meets a bus entry
  #n17 (1 pins): a wire end at 81.28,182.88 meets a bus entry
skipped pins 2 of 82 on the sheet; the budget is under 1/4

ruling report 1 — the border region (T7: clipped to the paper, nothing deducted for the border and title block).
  re-routed segments landing inside it: 0
  the same count for the wires the person drew, as the control: 0

ruling report 2 — bus entries (T7: left out of the obstacle map, being diagonals the lattice cannot describe).
  bus entries on the sheet: 2
  re-routed segments passing within 1 G of one: 0

test re_routing_a_known_good_sheet_costs_what_the_original_did ... ok

=== re-route calibration: ampli_ht.kicad_sch ===
page 2970022 × 2100072 iu, 87 pins, 35 strands measured, 2 skipped
weights: w_len 1, w_turn 6, w_cross 20, w_text 12, w_near 2; margin 101600 iu, u_max 76200 iu, label_threshold 3810000 iu

strand                 pins segs |   orig step turn xing text |  route step turn xing text | deviation
PIEZO_IN                  2    1 |     16   12    0    0    0 |     16   12    0    0    0 |   +0.0 %
GND                       2    2 |     15    3    1    0    0 |     15    3    1    0    0 |   +0.0 %
#n4                       3    3 |     18   12    0    0    0 |     18   12    0    0    0 |   +0.0 %
GND                       2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
#n3                       3    4 |     40   28    1    0    0 |     39   25    1    0    0 |   -2.5 %
GND                       2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
+12V                      2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
-VAA                      2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
GND                       2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
#n32                      2    1 |      8    4    0    0    0 |      8    4    0    0    0 |   +0.0 %
#n35                      2    2 |     38   16    1    0    0 |     38   16    1    0    0 |   +0.0 %
S_OUT+                    3    5 |    104   86    2    0    0 |    103   85    2    0    0 |   -0.9 %
+12V                      2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
-VAA                      2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
#n31                      2    1 |     11    7    0    0    0 |     11    7    0    0    0 |   +0.0 %
GND                       2    2 |     14    4    1    0    0 |     14    4    1    0    0 |   +0.0 %
HT                        2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
#n21                      2    1 |      6    2    0    0    0 |      6    2    0    0    0 |   +0.0 %
#n9                       3    3 |     17   11    0    0    0 |     17   11    0    0    0 |   +0.0 %
GND                       2    1 |      6    2    0    0    0 |      6    2    0    0    0 |   +0.0 %
#n27                      2    1 |      8    4    0    0    0 |      8    4    0    0    0 |   +0.0 %
#n26                      2    2 |     19    9    1    0    0 |     19    9    1    0    0 |   +0.0 %
HT                        2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
#n25                      2    1 |      8    4    0    0    0 |      8    4    0    0    0 |   +0.0 %
#n10                      3    3 |     22   16    0    0    0 |     22   16    0    0    0 |   +0.0 %
#n22                      2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
#n1                       4    5 |     28   20    0    0    0 |     28   20    0    0    0 |   +0.0 %
#n19                      2    1 |      8    4    0    0    0 |      8    4    0    0    0 |   +0.0 %
GND                       2    1 |      3    1    0    0    0 |      3    1    0    0    0 |   +0.0 %
HT                        2    1 |      8    4    0    0    0 |      8    4    0    0    0 |   +0.0 %
#n13                      3    4 |     67   35    1    1    0 |     67   35    1    1    0 |   +0.0 %
#n14                      3    4 |     59   27    1    1    0 |     70   32    2    1    0 |  +18.6 %
GND                       2    1 |     10    6    0    0    0 |     10    6    0    0    0 |   +0.0 %
PIEZO_OUT                 3    5 |     44   26    2    0    0 |     42   24    2    0    0 |   -4.5 %
GND                       2    2 |     16    4    1    0    0 |     16    4    1    0    0 |   +0.0 %

total: original 630, re-routed 637, deviation +1.1 % (tolerance ±15 %)

skipped strands, with the reason for each:
  #n5 (3 pins): the drawn wire is refused by U201 at 110.49,55.88
  Vpil_0_3,3V (4 pins): the drawn wire is refused by U201 at 110.49,64.77
skipped pins 7 of 87 on the sheet; the budget is under 1/4

ruling report 1 — the border region (T7: clipped to the paper, nothing deducted for the border and title block).
  re-routed segments landing inside it: 0
  the same count for the wires the person drew, as the control: 0

ruling report 2 — bus entries (T7: left out of the obstacle map, being diagonals the lattice cannot describe).
  bus entries on the sheet: 0
  re-routed segments passing within 1 G of one: 0

test re_routing_a_demo_sheet_costs_what_the_original_did ... ok


```

##### The two T7 rulings, reported whatever the deviation

**1. The border region — no change is needed, and the counter is shown able to
say so.** T7 clipped the window to the paper and deducted nothing for the border
and the title block. The region is defined here from KiCad's own default drawing
sheet: 10 mm margins on all four sides (`ds_data_model.cpp:69`, and the
`(left_margin 10)…` of `defaultDrawingSheet`,
`drawing_sheet_default_description.cpp:125`, tag 10.0.5), plus the title block,
`(rect (start 110 34) (end 2 2))` anchored at the margin box's bottom-right
corner. **Re-routed segments landing inside it: 0 on both sheets. The control —
the same count over the wires the person drew — is also 0 on both.** So the
deduction does not come back, as a cost or as anything else. Because both
answers are zero, `the_border_region_is_the_band_kicad_draws` shows the counter
answering `true` for four points inside the band and the title block and `false`
for points clear of them: "no route enters the border" and "the counter is
broken" do not read the same here.

**2. Bus entries — no change is needed, and this was measured rather than
assumed.** T7 left bus entries out of the obstacle map, being diagonals the
lattice cannot describe. `ampli_ht` has none, so the fixture was built with a
bus and **two** bus entries on purpose, to give the question something to be
asked of. **Re-routed segments passing within 1 G of one: 0.** The two strands
that feed the bus are declined by name — "a wire end at 81.28,172.72 meets a bus
entry" — so no route was ever planned through that region.
`a_bus_entry_halo_is_one_grid_step` shows the proximity test answering both ways.

##### Falsification

Committed good state first: `df97f59`, then `90d3322`. Every break was applied
to the working copy, run, and restored from a checksummed copy —
`md5 eb283db488c72c73b1ec0408c5aaa252` after each restore.

| Break | Result |
|---|---|
| The re-routed side's steps scaled by 5/4 | fixture **fails at +15.7 %**, demo passes at under 15 % |
| The same at 3/2 | fixture **fails at +33.8 %**, demo **fails at +28.4 %** — the **+15 %** side, both sheets |
| The original side's steps scaled by 3/2 | fixture **fails at −25.2 %**, demo **fails at −20.6 %** — the **−15 %** side, both sheets |
| Skip every strand of more than one segment | **both fail**: 58 of 82 and 45 of 87 skipped pins against the 1/4 budget |
| **The costing symmetry**: cost the original against the full sheet, its own wires no longer named as the route's own | **both fail**: 82 of 82 and 67 of 87 skipped pins. The person's own wire runs collinear along itself and is refused |
| Remove `pins_of`'s sort alone | **no failure** — the strand's own pin sort still carries it |
| Remove both pin sorts | **both order checks fail**; the tolerance checks do **not** notice |
| Remove the strand sort's total-order key | no failure — the earlier key is total on these sheets too |

**The costing-symmetry break is caught by the skip guard and not by the
tolerance**, and that is the finding it produces: with the strand's own wires
left in the map the strands do not mis-price, they become uncostable, and it is
`assert_skipped_pins_are_a_minority` that says so. The quarter-of-the-pins
assertion is not a nicety beside the tolerance; on this break it is the only
thing standing between a broken gate and a green one.

**One claim was withdrawn under its own control.** The first run-order break
reversed the pin list **after** `pins_of` had sorted it, and the report's rows
re-ordered — which looked like an order-dependence defect and was written up as
one. Reversing the *file's* object order, which is the variation that actually
happens, changed nothing, and re-running the break against the earlier sort key
showed it passing. So there was no defect: the break was breaking the sort rather
than varying the environment, and the code comment that claimed otherwise was
rewritten before it was committed. The strengthened key is kept as a total order
by construction rather than by luck, and the check that guards it is falsified
above by removing both pin sorts.

##### Environment variation

| Variation | Result |
|---|---|
| Reversed object order in the file (`Sheet::with_objects_reversed`) | **whole report identical**, both sheets — now a standing check, `the_report_does_not_depend_on_the_order_the_file_lists_its_objects` and its demo twin |
| Run from `/` rather than the worktree, with `TZ=Asia/Tokyo LC_ALL=tr_TR.UTF-8` | **whole report identical**, both sheets |
| `--test-threads=1` against `--test-threads=4` | 9 passed either way |

The comparison is of the **whole report**, not of the totals: the first
variation produced totals that agreed and rows that did not, and a report whose
rows move under file order is a report no reader can diff.

##### PROPOSED — this gate measures agreement, not calibration

**Measured, 2026-08-21.** The procedure costs both drawings with the same
weights while the router optimises exactly that objective, so the deviation is
close to a fixed point of the weights. Sweeping every weight to extremes —
`w_near` 0 to 20000, `w_turn` 0 to 10000, `w_cross` 1 to 100000, `w_text` 0 to
200, `w_len` 0 to 20, `margin`, `u_max` — **moves neither sheet outside ±15 %**.
The demo sheet ranges from −6.4 % to +8.2 %; **the fixture reads +0.0 % under
every single perturbation.** A gate that passes for all weights cannot be the
objective measure of whether the weights are right, which is what Q3 asked it to
be.

**The instrument that does answer Q3 is one line away**, and it was run: let the
router **choose** under the perturbed weights and **score** both drawings under
the defaults, so the person's own drawing is the standard. Then every
perturbation is worse or equal, which is the result Q1 wanted:

| Router chooses under | fixture | `ampli_ht` |
|---|---|---|
| the defaults | +0.0 % | +1.1 % |
| `w_near = 0` | **+1.5 %** | **+4.6 %** |
| `w_near = 60` | +0.0 % | +1.5 % |
| `w_turn = 0` | +0.0 % | **+1.7 %** |
| `w_turn = 60` | +0.0 % | +1.1 % |
| `w_len = 40` | +0.0 % | **+6.0 %** |
| `margin = 1 G` | +0.0 % | **+6.0 %** |
| `w_cross = 1` or `400`, `w_text = 0` | +0.0 % | +1.1 % |

So `w_near`, `w_turn`, `w_len` and `margin` are each shown to be doing work, and
the defaults are at least a local optimum against a real person's drawing — but
only in **sign**, not in magnitude: `w_turn = 60` and `w_turn = 6` are
indistinguishable here, and `w_cross` and `w_text` are not exercised by either
sheet at all. **Q1's "`w_turn = 6`" is confirmed to be better than 0 and not
confirmed to be better than 60.**

**The proposal, for James.** Keep the ±15 % gate as the regression check it
genuinely is — it catches a router that stops reproducing a tidy drawing — and
add the fixed-standard sweep above as the calibration measurement, in M5 where
the weights may move. **No weight was moved here.** The gate is not adjusted
either: changing what it measures mid-milestone would be re-litigating Q3, and
this is a proposal rather than a change for that reason.

##### Two findings about the router, from the sheets themselves

1. **`ampli_ht` refuses two strands, and the reason is the body box, not the
   weights.** `#n5` and `Vpil_0_3,3V` are declined with "the drawn wire is
   refused by U201 at 110.49,55.88" and "…at 110.49,64.77". `U201` is an
   `LM358N`, whose body box under `symbol_boxes` is the union of the graphic
   **and the pin segments** — KiCad's own `GetBodyBoundingBox` — so it spans the
   full x −12.7…12.7 of the pin tips. The person drew a wire running vertically
   down the op-amp's own input pin column, which is inside that box and which the
   router treats as a hard block. 7 of 87 pins, well inside the quarter, and
   recorded rather than worked around.
2. **`ampli_ht`'s one disagreement is `#n14`, at +18.6 %** — 32 steps and 2
   corners where the person drew 27 and 1. It is the only strand on either sheet
   over the tolerance individually, and the total absorbs it.

##### Measurements

| Command | Result |
|---|---|
| `cargo test re_routing_a_known_good_sheet_costs_what_the_original_did` | pass, **+0.0 %**, 40 strands, 74 segments, 2 of 82 pins skipped |
| `cargo test --features corpus re_routing_a_demo_sheet_costs_what_the_original_did` | pass, **+1.1 %**, 35 strands, 7 of 87 pins skipped |
| `cargo test --features corpus --test route_calibration` | 9 passed |
| `KICLI_TEST_KICAD_CLI=1 cargo test --test route_calibration` | 6 passed, including the fixture rebuild and the oracle refresh |
| `cargo xtask check` | all six gates pass: fmt, clippy, test, doc, deny, clean |

**The corpus was there and `kicad-cli` was there**, both really run in this
worktree: the corpus test ran rather than skipping, and the two `kicad-cli` tests
rebuilt the fixture and re-exported the netlist. Per `CLAUDE.md` neither counts
toward done from a lane worktree — they were run to **make** the measurements
this task owes, and the orchestrator's merged run is what gates.

**No source file under `crates/kicli/src/` was touched.** The gate is written
entirely against the router's existing public surface.

### T21 — The whole loop, end to end, against KiCad

One test that does what an agent does with wires: read a view, connect two pins,
connect a pin to a net, draw one explicit polyline, and ask KiCad what it made.
It is the only test that proves the milestone rather than a task.

- **Check:** `KICLI_TEST_KICAD_CLI=1 cargo test --test wire_loop` — the netlist
  of the written file carries exactly the partition kicli reported, net for net
  and pin for pin; the before-partition is non-empty, which is the control;
  `kicad-cli`'s rule check reports no fault that does not name the wires just
  drawn; and P1 and P2 hold on the file the four writes produced.

### T22 — The agent documentation

`AGENT.md` gains the wiring half of the loop: the four statuses and what an
agent does with each, the cost breakdown read as a decision ("this route costs
20 for one crossing — move `R7` instead"), the label proposal and
`--auto-labels`, and the exit codes for `blocked` and `invalid`. A worked
example connects two pins, reads the cost, moves a symbol and connects again for
a cheaper route.

- **Check:** `cargo test agent_doc_covers_every_command` — now covering the
  three wire verbs.
- **Check:** `cargo test --test agent_doc` — the worked example's commands parse
  and the flags it names exist.

---

## Chores — pickable by any session at a task boundary

A chore is not a task: it has no milestone goal behind it and blocks nothing. It
is written down because a debt nobody owns is a debt nobody pays.

**Owners, ruled at the checkpoint-1 review, 2026-08-15 (advisor ruling, item 6).**
A debt nobody owns is a debt nobody pays, and C3–C6 were all filed without one.
Every open chore now names who does it and when. The ordering constraint that
forces most of this table: **C3 and C5 both edit
`crates/kicli-probe/src/drawing.rs`**, and the probe crate belongs to no lane, so
they are sequenced rather than parallel, exactly like any merge hotspot.

| Chore | Owner | When |
|---|---|---|
| C1 — one name for the handle | unassigned; any session at a task boundary | open |
| C2 — the index fallback's cost | unassigned; `chore-runner` when picked up | open |
| C3 — `label_of_kind` takes a fragment | `lane-implementer`, in Lane B's worktree | **after C5**, same file; not before the M4 close |
| C4 — a sheet pin whose angle disagrees | **M5**, with the rule catalogue | **closed for M4 by being carried**, 2026-08-21 |
| C5 — handle-addressable probe drawings | `chore-runner`, in Lane B's worktree | **this checkpoint, before the verb surface (T16)** |
| C6 — `JOINING`'s rustdoc | `chore-runner`, in Lane B's worktree | **this checkpoint**, with C5 — it edits `edit/wire.rs`, a Lane B file | ✅
| C7 — `agent_doc` asserts a mention | `lane-implementer`, holding `AGENT.md` alone | not this checkpoint — it needs `AGENT.md`, held by the verb surface (T16) |
| C8 — the identifier seed goes project-relative | `lane-implementer`, one lane across the three `edit/` verbs | the M4 close, after the measurement its entry makes first |

### C1 — One name for the eight-character handle

`Uuid::short` is the handle rule with its rustdoc. Four private copies of the
same eight characters remain: `edit::net`, `edit::mark`, `view::connectivity`
and `view::snapshot`. Two of them shorten keys that are not identifiers, so the
replacement is read-and-decide rather than search-and-replace: a key that is not
a `Uuid` keeps a shortener of its own, under a name that says so.

- **Check:** `cargo test --test the_handle_has_one_name` — a workspace-reading
  test over `crates/`: no file but the one that defines it declares a function
  that shortens an identifier to eight characters. Its control asserts
  `Uuid::short` **is** found, so a sweep that read nothing cannot pass.
  - *Corrected 2026-08-21, and left standing above so the correction is
    visible.* That sentence is not what the check enforces, and writing it as
    though it were is how the first attempt got a REJECT: "declares a function"
    and "shortens an identifier" are both judgements about **names**, and names
    are what the instrument was shown to be blind to. What it enforces is
    stated at its true width under `Reworked again, 2026-08-21` — every
    eight-wide cut reachable by four named mechanisms, each accounted for with
    a reason. *Amended a second time: the pointer above first read "Reworked,
    2026-08-21", whose claim — "in any spelling `std` offers" — was itself too
    wide and drew REJECT 2. A gloss corrected once can still be wrong, and the
    correction chain is left visible for the same reason the original is.* A
    check's gloss that nobody compares to the check is the same defect as the
    threshold gloss this milestone already paid for.
- **Check:** `cargo xtask check` — the six gates, and no golden moves, because
  this changes no output.

#### Done, 2026-08-20

Lane `lane-c1`, based on `aa96263`. Base verified as the lane's first action:
`git log --oneline -1` reported `aa96263`, `git status --short` reported
nothing. No fast-forward was needed.

**The count was four; it is five.** Task text yields to measured reality. The
entry names `edit::net`, `edit::mark`, `view::connectivity` and
`view::snapshot`. A fifth private copy exists, in
`crates/kicli/src/cli/edit/wire.rs:352`, spelled differently — named `handle`
rather than `short`, and `uuid.get(..8).unwrap_or(uuid)` rather than
`chars().take(8)` — which is presumably why an eye counting `fn short` missed
it. The measurement is the sweep's own first (red) run, which listed exactly
five and nothing else:

```
crates/kicli/src/cli/edit/wire.rs:353 uuid.get(..8).unwrap_or(uuid) [in `fn handle(uuid: &str) -> &str {`]
crates/kicli/src/edit/mark.rs:567     uuid.chars().take(8).collect() [in `fn short(uuid: &str) -> String {`]
crates/kicli/src/edit/net.rs:452      uuid.chars().take(8).collect() [in `fn short(uuid: &str) -> String {`]
crates/kicli/src/view/connectivity.rs:30 uuid.get(..8).unwrap_or(uuid) [in `fn short(uuid: &str) -> &str {`]
crates/kicli/src/view/snapshot.rs:535 uuid.chars().take(8).collect() [in `fn short(uuid: &str) -> String {`]
```

**Scope deviation, recorded rather than hidden.** `cli/edit/wire.rs` is not on
the dispatch brief's IN list, because the brief's list was derived from the
count of four. The check the chore names — *no file but the definer declares a
function that shortens an identifier* — cannot pass with that file untouched,
so the choice was between the named check and the derived list. The named check
won; the edit there is four lines (delete the helper and its unit test, call
`left.junction.short()`). It is not a merge hotspot and was not on the live-lane
list given at dispatch. **PROPOSED: accept the one extra file. Recommendation:
accept — the alternative is a second dispatch to delete four lines. Reversal is
cheap: `git revert` of the wire hunk leaves the sweep red, which is the honest
state if the ruling goes the other way.**

**The read-and-decide, which is the substance of the chore.** Each of the five
was read at its call sites before anything was changed.

- `edit::net` — **identifier.** Both call sites pass `Uuid.0`
  (`label.uuid.0`, `symbol.uuid.0`). Folded into `Uuid::short`.
- `edit::mark` — **identifier.** Three call sites, all `Uuid.0`
  (`line.uuid.0`, `junction.uuid.0`). Folded.
- `view::connectivity` — **identifier.** Three call sites. Two are plainly
  `Uuid.0`; the third took a `String` out of a row tuple, and that string is
  `symbol.uuid.0.clone()` at the point the row is built. The row now carries a
  `Uuid` instead of a `String`, so the type says what the value is. Folded.
- `cli::edit::wire` (the fifth) — **identifier.** One call site,
  `left.junction.0`, where `StrandedJunction::junction` is typed `Uuid`.
  Folded.
- `view::snapshot` — **both, which is the case the chore was written for.**
  Six call sites shorten `Uuid.0` and were folded. Three shorten a *snapshot
  key*: `handle_of_key`'s two branches and `other_object`'s `handle`. A
  snapshot key is an identifier only when the object has one — an object kicli
  cannot name is keyed by `format!("{token}@{data}")` over a content hash
  (`snapshot.rs:960`), and a field is keyed by owner-dot-name
  (`snapshot.rs:738`). Those three keep a shortener of their own, `short_key`,
  whose rustdoc says why it is not `Uuid::short`. The distinction is visible at
  the call site by type: code holding a `Uuid` calls `.short()`; code holding
  an untyped key string calls `short_key`. `handle_of_key`'s own rustdoc
  claimed "the file holds identifiers", which was already wrong about the
  content-key case, and was corrected.

**Coverage that moved rather than vanished.** Three of the deleted copies
carried unit tests of the short-input case (`short("short") == "short"`).
`Uuid::short`'s doctest only covered the ordinary cut, so the edge cases now
live with the rule: `a_handle_shorter_than_the_handle_is_the_whole_of_it`
(short, empty, exactly eight, nine) and `a_handle_cuts_characters_rather_than_bytes`
(multibyte — `chars().take(8)` and `char_indices().nth(8)` agree, but a byte
slice would panic). `handle_of_key`'s test gained a content-key case.

**What the sweep forbids, and what it deliberately does not.** Two arms, both
over every `.rs` under `crates/`, with the definer and the sweep file itself
excluded by path:

1. No function whose *parameter list* names an identifier (`uuid`, `kiid`,
   `identifier`) may cut to eight characters. This is the chore's wording —
   "declares a function" — and it is what lets `short_key(key: &str)` stand
   while `handle(uuid: &str)` falls.
2. Shipped code (any file under a `src/` directory) additionally may not cut an
   identifier *inline*, judged from the text to the left of the cut. Nothing in
   `src/` does today; the arm is there because a future inline copy is the same
   drift wearing no declaration.

Deliberately out: `crates/kicli-probe/tests/drawing.rs:73,130` reads uuids out
of raw file text and cuts them inline inside a test body. That is not a
declared shortener, and the probe's handle usage belongs to the
probe-handle chore (C5), not here. Recorded so a later reader knows it was seen
and not missed.

**Gates.** `cargo xtask check` in the lane worktree: `fmt`, `clippy`, `test`,
`doc`, `deny`, `clean` all pass. **No golden moved** — the `clean` gate reports
"the gates changed no file outside target/", and `git diff --stat -- '*.golden'`
is empty. That is the expected result: no output text changed, because every
fold is arithmetic-for-arithmetic on the same input.

**Falsification.** Good state committed at `2c4f0fc` before the first break; the
strengthening below is `ff80d05`. Restores were checksummed
(`md5 -q crates/kicli/tests/the_handle_has_one_name.rs` = `fb3c6a9b…` at every
restore) and `git status --short` was empty before each new break.

| # | What was broken | Which assertion caught it |
|---|---|---|
| 1 | A private `fn short(uuid: &str) -> &str` reintroduced in `view/connectivity.rs`, above `natural_key` | `no_second_function_shortens_an_identifier`, "These shorten an identifier somewhere else", naming `connectivity.rs:30` |
| 2 | `edit/net.rs:407` folded back to an **inline** cut — `let uuid: String = label.uuid.0.chars().take(8).collect()` — declared in no helper of its own | the same assertion, via the second (shipped-code) arm alone: the enclosing `fn edits_for(hierarchy, net, from)` names no identifier, so arm 1 could not have caught it |
| 3a | `Uuid::short`'s signature drifted to `-> String` with a `chars().take(8)` body — compiles, every caller still builds | `the_one_rule_is_where_it_is_claimed_to_be`, "declares the one shortener" |
| 3b | `DEFINER` pointed at `model/version.rs`, i.e. the rule sought where it is not | both tests: "holds the identifier type", **and** the sweep then flagging `items.rs:48 .nth(8) [in \`impl Uuid pub fn short(&self) -> &str {\`]` |
| 3c | `sources()` pointed at `target/nothing-is-here`, i.e. a sweep that reads nothing | "the workspace sources were found: 0", and the definer read panicking on a file it could not find |
| 4 | Both `.short()` calls in `edit/net.rs` replaced with `.0.clone()` — a fold that deletes a copy *and* its callers, printing whole identifiers | `the_one_rule_is_where_it_is_claimed_to_be`, "crates/kicli/src/edit/net.rs calls the one rule rather than a copy of it". The sweep itself passed, which is the point of this control |
| 5 | `SHORTENINGS` emptied, i.e. the instrument made blind | "the sweep read source: it found cuts to eight characters that are not identifiers", and the definer control's "cuts to eight characters" |

**A break that found a real hole, recorded because the check changed because of
it.** Break 3b was run twice. The first run failed the presence assertion as
expected but left the *sweep* green with `items.rs` no longer excluded — meaning
the definer's own declaration was not classified as an identifier shortener, and
the path exclusion was decoration. Cause: `Uuid::short` is a **method**, so its
parameter list is `&self` and names nothing. An identical copy declared as a
method on any other type would have passed forever. The classification now reads
the enclosing `impl` line for methods (`ff80d05`), the function's own name stays
out of it (`fn uuid_from(seed: &str)` in `edit::insert` slices hex to lay out a
UUID's dashes and must not be flagged for it), and 3b re-run against the
strengthened sweep fails **both** tests. This is the falsification-control
skill's case 2 — control blind over innocent code — caught by not accepting a
green as good news.

**Environment variation, per the skill's break class.** The sweep consumes a
generated value: the file list it walks. The whole worktree was copied to
`/private/tmp/claude-501/.../scratchpad/second-directory` — a different absolute
path, fresh `target/`, recompiled there (`strings` on the test binary shows
`scratchpad/second-directory/crates/kicli` baked in as `CARGO_MANIFEST_DIR`) —
and both tests pass there. Positive proof it reads the tree it is *in* rather
than the one it was written in: an offending
`crates/kicli/src/only_in_the_copy.rs` added **only to the copy** fails the sweep
in the copy while the lane worktree stays green.

**Run order.** `sources()` sorts, so a filesystem handing back a different order
gives the same answer. Demonstrated rather than asserted: with
`found.reverse()` added after the sort in the copy, both tests still pass, and
the planted offender is still caught under the reversed order — so the green is
order-independence, not order-blindness.

#### PARKED, 2026-08-21 — interrupted mid-rework, after one REJECT

**State: rework in progress on branch `lane-c1`, five commits, not merged.**
`d9ea182` `17ee61b` `e4b91ae`. The lane's own gates were green at `d9ea182`;
the two commits after it are the rework and their gate state is unverified.

**The tick review REJECTed the first attempt, and the gap is the instrument.**
The sweep classified by **name**, against the closed list
`IDENTIFIER_WORDS = ["uuid", "kiid", "identifier"]`. The reviewer planted two
copies of the rule in a verified scratch copy and both passed with zero
offenders found: a free function whose parameter is named `id`, and a method on
a type named `Ident`. A genuine reintroduction of the fifth-copy defect lands
clean under either spelling, so the entry's unqualified claim — *no file but the
definer declares a function that shortens an identifier* — is not what the sweep
enforces.

**The lane had already found this class and stopped one step short.** Its break
3b found the classifier blind to methods, diagnosed it correctly as the skill's
case 2, and fixed it **for the definer's own spelling (`impl Uuid`)** rather than
for the class. Every break in its seven-row table names its offender literally
`uuid` — *a falsification table built from the instrument's own vocabulary tests
the instrument against itself.* That is the lesson worth keeping whatever happens
to this chore.

**Direction given, and a fresh implementer should start from it rather than from
the draft.** Not a longer word list — that is the same instrument with a longer
blind spot. **Classify by the cut, not by the name**: find the eight-character
slice itself anywhere under `crates/`, then allow exactly the permitted sites —
the definer, the deliberately-retained `short_key`, and anything else with **the
reason recorded**, because an allowlist without reasons is a blind spot with a
comment.

**The rework was itself mid-correction when the session ended, and its last
finding is the useful one.** The lane reported: *"Confirmed: three std spellings
pass silently — the same failure class one level down. Deriving the list from
std's API instead of my own imagination."* So the cut-based instrument had the
identical defect at the next level — a hand-written list of slice spellings — and
the fix in flight was to derive the spellings from `std`'s API rather than from
memory. That correction is unverified.

**The count in this entry is wrong and the correction stands regardless.** There
are **five** private copies, not four. The fifth is `cli/edit/wire.rs:352`,
spelled `fn handle(uuid: &str)`, which is why an eye counting `fn short` missed
it — the same name-blindness that the sweep then inherited.

**Scope, recorded for the ruling it needs:** the lane wrote to
`crates/kicli/src/cli/edit/wire.rs`, outside the brief's IN list, because the
list was derived from the count of four. Disclosed by the lane in its first
paragraph. See PROPOSED 4 in `tasks/reports/M4-phase3.md`.

**What is verified and must not be redone:** the presence control fails red when
the definer is unfindable (measured by the reviewer); no golden moves; the
identifier / non-identifier distinction is genuinely preserved rather than folded
— `short_key` remains a separate, named, rustdoc'd helper.

#### Reworked, 2026-08-21 — after REJECT 1, on the merged tree

Resumed on `lane-c1` after the session was cut by a usage limit. Worktree
verified first: `git log --oneline -1` reported `e4b91ae`, `git status --short`
reported nothing. `main` had moved to `491c254`, so the branch was brought
forward with `git merge main` rather than left on a stale base.

**Merged forward twice.** `main` moved again during the rework, from `491c254`
to `88d8e71`; the second merge was clean and touched only other lanes' sections
of `tasks/M4.md`, `tasks/M5.md`, `tasks/dogfood.md` and the phase report. The
gates and both named checks were re-run after it, not assumed. The first merge
is the one that conflicted:

**Merge conflict, and how it was resolved.** `tasks/M4.md` conflicted under this
chore: this branch's `Done, 2026-08-20` evidence against main's
`PARKED, 2026-08-21` note recording the REJECT. **Both kept**, in the order they
happened — the Done section, then the parked note that rejects its classifier.
The parked note is the record of the interruption and is not the lane's to
delete; the Done section is left standing rather than edited, because a
superseded claim with its refutation beside it is the record, and a silently
corrected one is not. Everything else merged clean. `a2d27b4` (typed
`LabelKind` in `Probe::label_of_kind`) landed underneath and touches many test
files; the sweep was **re-run against the merged tree** rather than assumed —
green, and both probe-test accounted-for sites still match, which the staleness
control would have caught if they did not.

**The REJECT was right.** The sweep classified by name, against
`IDENTIFIER_WORDS = ["uuid", "kiid", "identifier"]`. The reviewer planted
`fn short(id: &str)` and `impl Ident { fn short(&self) }`; both passed with zero
offenders. The claim the entry made was not the claim the instrument enforced.

**The new rule: classify by the cut, not by the name.** Find every expression
under `crates/` that takes the first eight characters or bytes of a string, then
require each one to appear in `ACCOUNTED` with the reason it is not a second
copy of the handle rule. Nothing is judged by what it is called. The
identifier/non-identifier distinction the chore rests on survives intact — it
stops being guessed from names and starts being stated in prose, by a person, at
the site.

##### Where the vocabulary comes from, which is the part that took three tries

The instrument failed three times in this chore, each time for one reason: **it
was built from its author's vocabulary, so its own falsification table was
spelled the way it expected and could not see the gap.**

1. Classified by **name**, closed list of three words. The reviewer walked
   `fn short(id: &str)` through it.
2. Reworked to classify by the **cut**, with the slice spellings written from
   memory. Plants using `split_off(8)`, `drain(8..)` and `split_at_mut(8)`
   passed in silence — measured, in a verified copy, before the fix.
3. Fixed (2) by grepping `std` — **with a hand-written alternation of method
   names**, which is the same closed list one level up. Caught here, in this
   entry's own review of it, not by a reviewer.

The test of done is not "the list is longer". It is: **can a reader who has
never seen this repository check the list against something outside it?** The
answer is now yes. `CUTS` is exhaustive over three external enumerations, and
the commands that produce them are recorded on the constant:

```text
SRC=$(rustc --print sysroot)/lib/rustlib/src/rust/library
awk '/^impl str \{/,0' "$SRC"/core/src/str/mod.rs | grep -oE 'pub (const )?fn [a-z_0-9]+' | sed -E 's/.*fn //' | sort -u
awk '/^impl String \{/,/^impl FromUtf8Error/' "$SRC"/alloc/src/string.rs | grep -oE 'pub (const )?fn [a-z_0-9]+' | sed -E 's/.*fn //' | sort -u
grep -oE '^\s+fn [a-z_0-9]+' "$SRC"/core/src/iter/traits/iterator.rs | sed -E 's/.*fn //' | sort -u
```

Every method the three enumerations returned was considered. Those that can
yield a prefix at a literal width of eight are in `CUTS`; the rest are listed
here with the reason they cannot, so the residue is inspectable rather than
implied:

- **In `CUTS`, from `str`:** `get`, `get_mut` (four range spellings each way),
  `split_at`, `split_at_checked`, `split_at_mut`, `split_at_mut_checked`,
  `floor_char_boundary`, `ceil_char_boundary`; plus the `core::ops::Index`
  forms `[..8]` and `[..=7]`, which are an operator rather than an inherent fn.
- **In `CUTS`, from `String`:** `truncate`, `split_off`, `drain`,
  `replace_range`.
- **In `CUTS`, from `Iterator`:** `take`, `nth`, `zip`, and the two array forms
  `next_chunk::<8>` / `array_chunks::<8>`.
- **Rejected — pattern-directed, so they cannot express "the first eight" with
  a numeric literal at all:** `split`, `splitn`, `rsplit`, `rsplitn`,
  `split_once`, `rsplit_once`, `split_terminator`, `rsplit_terminator`,
  `split_inclusive`, `split_whitespace`, `split_ascii_whitespace`, `lines`,
  `lines_any`, `matches`, `rmatches`, `match_indices`, `rmatch_indices`,
  `find`, `rfind`, `contains`, `starts_with`, `ends_with`, `strip_prefix`,
  `strip_suffix`, `strip_circumfix`, `trim_prefix`, `trim_suffix`, and every
  `trim*`. Note `splitn(8, …)` counts **pieces**, not characters.
- **Rejected — no prefix in the result:** `len`, `is_empty`, `is_ascii`,
  `is_char_boundary`, `parse`, `as_bytes`, `as_ptr`, `as_mut_ptr`, `as_str`,
  `as_ascii`, `substr_range`, `eq_ignore_ascii_case`, `encode_utf16`, every
  `escape_*`, every `from_utf8*`/`from_utf16*`, `make_ascii_lowercase`,
  `make_ascii_uppercase`, `into_bytes`, `into_boxed_str`, `into_raw_parts`,
  `leak`, `clear`, `new`, `pop`, `remove`, `remove_matches`, `insert`,
  `insert_str`, `push`, `push_str`, `replace_first`, `replace_last`, `retain`,
  `extend_from_within`, and the whole capacity family (`capacity`, `reserve`,
  `reserve_exact`, `shrink_to`, `shrink_to_fit`, `try_reserve`,
  `try_reserve_exact`, `try_with_capacity`, `with_capacity`).
- **Rejected — feeders, not cuts:** `chars`, `char_indices`, `bytes`,
  `into_chars`. They produce the iterator that `take`/`nth`/`zip` then cut, and
  the cut is what is matched.
- **Rejected — `Iterator` methods that take no width or take it from the wrong
  end:** `skip`, `skip_while`, `step_by`, `advance_by` (yields the *suffix*),
  `take_while`, `scan`, `position`, `rposition`, `map_windows`, and the
  consumers, comparators and folds (`fold`, `collect`, `count`, `sum`,
  `product`, `max*`, `min*`, `cmp*`, `eq*`, `partial_cmp*`, `is_sorted*`, …).

**PROPOSED — two judgement calls in that classification, flagged rather than
buried.** `retain` and `take_while` *can* cut to eight with a captured counter,
and `advance_by(8)` on a `by_ref()` iterator can bracket a prefix. All three are
excluded because their eight is not in the expression — they are the same class
as a `const LEN = 8`, and including them would mean matching every `retain` in
the workspace. **Recommendation: exclude, as recorded, and treat them as part of
the residual claim below rather than as coverage.**

##### The claim, at the width the instrument actually supports

> No expression anywhere under `crates/` takes the first eight characters or
> bytes of a string, **at a literal width of eight, in any spelling the standard
> library offers for doing so**, except at the sites in `ACCOUNTED` with the
> reason each is there.

That is narrower than "the handle rule has one home", and it is written at this
width in the module rustdoc as well as here. What it cannot see: a width behind
a `const`, a width computed at run time, a hand-rolled loop with its own
counter, or a macro that expands to a cut.

**PROPOSED — the residual needs a different instrument, not a longer sweep.**
Closing those gaps means reading the compiled MIR rather than source text — a
clippy-style lint, which is separate work with its own dependency and its own
gate. **Recommendation: accept the narrowed claim for this chore, and file the
lint as its own item if the guarantee is ever wanted at full strength.** The
alternative — quietly claiming the wider thing — is what this chore has now been
rejected for once.

##### The allowlist, and the reason for each entry

Six sites. `ACCOUNTED` carries each reason as a string, and the test asserts the
reason is longer than sixty characters and prints the whole set on failure,
because an allowlist without reasons is a blind spot with a comment.

| Site | Reason it is not a second copy of the rule |
|---|---|
| `model/items.rs`, `impl Uuid` + `fn short` | The rule itself. Every identifier handle in kicli comes from here. |
| `view/snapshot.rs`, `fn short_key` | Deliberately retained, deliberately not named for an identifier. A snapshot key is an identifier only when the object it names has one: an object kicli cannot name is keyed by a hash of its contents (`snapshot.rs`, `other_object`), a field by its owner and its name. Folding it into `Uuid::short` would make that rule's rustdoc a lie about what it governs. |
| `view/snapshot.rs`, `impl Encoding` + `fn finish` | Eight **bytes** of a SHA-256 digest, which is the width of a `ContentHash`. It shortens a hash, not a string, and no caller could type it back. |
| `edit/insert.rs`, `fn uuid_from` | Not a shortening at all: it slices a 32-character hex digest into the five groups a UUID is written in, and `[0..8]` is the first group. The result is longer than what went in. |
| `kicli-probe/tests/drawing.rs`, `fn a_probe_drawing_yields_distinct_handles_for_each_object` | A probe test reading identifiers out of raw file text and cutting them inline. It **is** a copy of the rule, and is named as one here rather than excused: the probe crate is another lane's, and the probe's handle usage belongs to the probe-handle chore (C5). Accounted for so it is visible, not so it is approved. |
| `kicli-probe/tests/drawing.rs`, `fn sibling_probes_of_different_series_have_no_colliding_handles` | The same inline copy, a second time, in the same file. Same owner, same chore (C5). |

The last two are the difference between this instrument and the previous one.
The name-based sweep did not report them at all — they sat in a category it had
been told to ignore. Here they cannot be silent: they are either offenders or
they are written down.

##### Falsification, extended with the cases the lane did not choose

Good state committed at `ff887aa` before any break. All plants were made in a
**copy** of the worktree at
`/private/tmp/claude-501/…/scratchpad/evasion`, verified byte-identical to
`lane-c1` by `diff -r` before and after, because two of the plant sites
(`view/scope.rs`) are outside this task's write scope. The copy is at a
different absolute path with its own `target/`, so it doubles as the
second-directory run the environment-variation rule requires.

| # | Plant | Chosen by | Result |
|---|---|---|---|
| A | `pub fn short(id: &str) -> &str { id.get(..8)… }` in `view/scope.rs` | **the reviewer**, verbatim | FAILS — offender at `scope.rs:187`. Passed the old sweep silently |
| B | `impl Ident { pub fn short(&self) -> &str {…} }`, planted **in the definer's own file** | **the reviewer**, verbatim | FAILS — `items.rs:62 … [in \`impl Ident pub fn short(&self)\`]`. The hardest case for a file-keyed allowlist, and the `impl Uuid` requirement is what refuses it |
| C | `String::split_off(8)` | **std's API list** | FAILS. Passed the hand-written cut list silently |
| D | `String::drain(8..)` | **std's API list** | FAILS. Passed the hand-written cut list silently |
| E | `str::split_at_mut(8)` | **std's API list** | FAILS. Passed the hand-written cut list silently |
| F | `String::replace_range(8.., "")` | **std's API list** | FAILS |
| G | `String::truncate(8)` | **std's API list** | FAILS |
| H | `str::split_at_checked(8)` | **std's API list** | FAILS |
| I | `str::floor_char_boundary(8)` | **std's API list** | FAILS |
| J | `Iterator::zip(0..8)` over `chars()` | **std's API list** | FAILS |
| K | `fn short_key(key: &str)` in `view/scope.rs` — borrowing another file's justification by sharing its name | lane | FAILS. An `ACCOUNTED` entry is keyed by file *and* declaration, so a name cannot be inherited |
| L | `short_key` **deleted** from `snapshot.rs` and its callers folded onto `Uuid::short`, entry left behind | lane | FAILS — "these accounted-for sites no longer exist". This is the *opposite* failure, and it matters: it is the sweep refusing the fold the chore forbids, not just refusing a copy |
| M | An entry's reason cut to `"Not an identifier."` | lane | FAILS — "carries a reason, not a label" |
| N | The definer drifts to `-> String` with a `chars().take(8)` body — compiles, all callers build | lane | FAILS — presence control, "declares the one shortener" |
| O | `DEFINER` repointed at `model/version.rs`: the rule sought where it is not | lane | FAILS **both** tests — presence control, *and* the sweep then flagging `items.rs:48` as unaccounted-for. The path exclusion is load-bearing, not decoration |
| P | `sources()` pointed at `target/nothing-is-here`: a sweep that reads nothing | lane | FAILS — "the workspace sources were found: 0" |
| Q | Both `.short()` calls in `edit/net.rs` replaced by `.0.clone()` — a fold that deletes a copy *and* its callers | lane | FAILS the call-site control. The sweep itself passes, which is why that control exists |

Ten of the seventeen rows are spelled by somebody other than the lane — two by
the reviewer, eight by `std`. That ratio is the point: the previous table's
seven rows all named their offender literally `uuid`, so it could only ever have
confirmed the instrument to itself.

**Run order.** With `found.reverse()` after the sort in the copy, the clean tree
still passes and plant A is still caught, so the green is order-independence
rather than order-blindness.

**Restores.** After every break the file was restored and the tree re-verified:
`diff -r` byte-identical to `lane-c1`, `git status --short` empty in the lane
worktree, and `md5 -q crates/kicli/tests/the_handle_has_one_name.rs` =
`efdb4cec…` throughout.

**Gates.** `cargo xtask check` on the merged tree: `fmt`, `clippy`, `test`,
`doc`, `deny`, `clean` all pass. **No golden moved** — `clean` reports "the
gates changed no file outside target/", and `git diff --stat` over `*.golden`
against the merge base is empty. Expected: the rework touches only the test.

**Not redone, per the reviewer's instruction:** the presence control's design,
the golden check, and the identifier/non-identifier distinction were all
verified at review and were not reopened. `short_key` is still a separate,
named, rustdoc'd helper; rows L and M above only add controls *around* it.

#### Reworked again, 2026-08-21 — after REJECT 2

Second rejection, escalated to a PROPOSED item in the session report per
CLAUDE.md's tick-review rule. **Merged forward a third and a fourth time** —
`88d8e71` → `1af87e5` (the calibration gate) before the work, `1af87e5` →
`984545c` (the agent-doc and net-count items) after it, `main` having moved
again during the write-up. Both clean, no conflict, and the gates and both
named checks re-run after each rather than assumed. Four merges forward in one
chore is the measurement behind PROPOSED 8: a lane must expect `main` to move
*during* its work, not only before it.

**The gap: `format!("{:.8}", uuid)`.** A complete second copy of `Uuid::short`'s
behaviour, planted by the reviewer in a byte-identical scratch copy, and the
sweep passed it with zero offenders.

**Why the disclosed limitations did not cover it, which is the part worth
keeping.** The previous entry named four escapes — a `const`-hidden width, a
runtime width, a counter loop, and "a macro that expands to a cut". `{:.8}` is
none of them: the eight is right there in the text, and `core::fmt` precision is
exactly as std-offered as `chars().take(8)`. The claim said *"in any spelling the
standard library offers"* while the derivation enumerated `str`, `String` and
`Iterator` **methods**. `core::fmt` is not a method, so no amount of care inside
that derivation could have reached it.

**This is the same defect a fourth time, one level down, and the level is the
point.** The record:

1. classified by **name**, closed list of three words — reviewer walked
   `fn short(id: &str)` through it;
2. classified by the **cut**, spellings written from memory — `split_off(8)`
   walked through it;
3. derived the spellings by grepping `std` — **with a hand-written alternation
   of method names**, the same closed list one level up; caught by this lane;
4. derived them from **three method enumerations** — but *which three lists to
   enumerate* was still this lane's choice. `{:.8}` walked through it.

The lane's own note after (3) said the rule is "the derivation must bottom out
in an enumeration nobody in the loop wrote". It did bottom out in `std`. **The
vocabulary moved outside the author's head; the taxonomy did not.** That is the
generalisation, and it is why the fix below is a statement about the boundary
rather than another list.

##### `core::fmt`, derived from the grammar rather than the macro list

std states the format-spec grammar in its own documentation source, and it was
transcribed from there, not recalled:

```text
sed -n '/format_spec :=/,/parameter :=/p' \
  "$(rustc --print sysroot)/lib/rustlib/src/rust/library"/alloc/src/fmt.rs

format_spec := [[fill]align][sign]['#']['0'][width]['.' precision][type]
precision   := count | '*'
count       := parameter | integer
parameter   := argument '$'
type        := '?' | 'x?' | 'X?' | 'o' | 'x' | 'X' | 'p' | 'b' | 'e' | 'E'
```

A literal precision of eight is therefore `.8`, optionally followed by one
`type`, then the closing brace — eleven spellings, one per `type` plus the bare
form. Two consequences of deriving from the grammar rather than from a list of
macros:

- **The macro list is not needed.** Matching the *spec* covers `format!`,
  `write!`, `writeln!`, `print!`, `eprint!`, `panic!`, the `assert!` family,
  `todo!`, `unreachable!` — and any macro taking a format string that std has
  not written yet. The macro enumeration was run
  (`grep -ohE '^macro_rules! [a-z_]+' … core/src/macros/mod.rs alloc/src/macros.rs std/src/macros.rs`)
  and is recorded here, but the matcher does not depend on it, which is the
  stronger position.
- **The grammar's optional `ws` before `}` is handled for free**, because lines
  are whitespace-stripped before matching, so `{:.8 }` — legal per
  `format := '{' [argument] [':' format_spec] [ws]* '}'` — is the same cut as
  `{:.8}`. Verified as plant V rather than assumed.

##### A fifth enumeration, found by this lane while doing the fourth

Enumerating to fix `{:.8}` surfaced a gap nobody had pointed at: **`[T]`**. A
byte prefix is reached by `as_bytes()` or `into_bytes()`, and `str`, `String`
and `Iterator` do not cover slice methods. `uuid.as_bytes().chunks(8).next()` is
a cut and was invisible. Added, with the same mechanical derivation
(`awk '/^impl<T> \[T\] \{/,…' core/src/slice/mod.rs`), contributing
`split_off(..8)` (the slice signature, a range, unlike `String`'s),
`first_chunk::<8>`, `split_first_chunk::<8>`, `as_chunks::<8>`,
`array_windows::<8>`, `chunks(8)`, `chunks_exact(8)`, `windows(8)`.

**Recorded as a scope judgement, since the instruction was "two things and
nothing else":** this is a third. It was included rather than filed because
shipping a sweep while knowing where its next hole is, is precisely the defect
under correction. **PROPOSED: keep it. Recommendation: keep — it is the same
taxonomy branch (method calls) completed, not a new mechanism, and it cost eight
list entries. Reversible by deleting that block of `CUTS`.**

##### The restated claim, and the boundary as the claim's own subject

> No expression anywhere under `crates/` takes the first eight characters or
> bytes of a string, at a literal width of eight, **by any of the four
> mechanisms enumerated below**, except at the sites in `ACCOUNTED` with the
> reason each is there.

The four: (1) `str`/`String` methods, plus the `core::ops::Index` range forms;
(2) `[T]` methods, reached by `as_bytes`/`into_bytes`; (3) `Iterator` adaptors
over `chars`/`char_indices`/`bytes`/`into_chars`; (4) `core::fmt` precision in a
format spec, in any macro that takes one.

Outside it, **named rather than implied**: a width that is not the literal 8 in
the expression (`const LEN = 8`, a runtime width, and `core::fmt`'s own indirect
precisions `{:.*}`, `{:.1$}`, `{:.n$}` — grammar-legal, but binding a
`$`-parameter to its argument means parsing the macro call); a hand-rolled
counter loop, or `retain`/`take_while` closing over one; a user macro expanding
to any of the above; a helper from a crate outside `std`; anything visible only
after monomorphisation or const evaluation.

**And the residual said plainly: a fifth mechanism may exist.** Three of the
four were found by somebody else pointing at a gap, and the fourth by this lane
tripping over it. What is claimed is that these four are covered exhaustively
and that the boundary is written down — **not** that the taxonomy is complete. A
reader who finds a fifth is finding a real defect in this sweep, and the module
rustdoc says so, so they meet an honest sentence rather than one that promised
more than it delivered.

**PROPOSED — the stronger guarantee needs a different instrument.** Everything
in the "outside it" list is beyond a reader of source text; it needs a lint over
compiled MIR. **Recommendation: accept the bounded claim for this chore and file
the lint separately if the full guarantee is ever wanted.** Carried forward
unchanged from the previous rework, and now with a fourth demonstration of why a
source-text sweep cannot be argued into completeness.

##### Falsification, the new rows

Good state committed at `183d01d` before any break. All plants in a copy at
`/private/tmp/claude-501/…/scratchpad/fmt`, `diff -r` byte-identical to
`lane-c1` before and after, at a different absolute path with its own `target/`
— so it is also the second-directory run.

| # | Plant | Chosen by | Result |
|---|---|---|---|
| R | `pub fn short(uuid: &str) -> String { format!("{:.8}", uuid) }` | **the reviewer**, verbatim | FAILS. Passed the previous sweep with zero offenders |
| S | `format!("{0:.8}", uuid)` — positional argument | std's grammar | FAILS |
| T | `format!("{handle:.8}", handle = uuid)` — named argument | std's grammar | FAILS |
| U | `format!("{:->12.8}", uuid)` — fill, align and width before the precision | std's grammar | FAILS |
| V | `format!("{:.8 }", uuid)` — the `ws` the grammar permits before `}` | std's grammar | FAILS |
| W | `write!(out, "{:.8}", uuid)` — a different macro | std's grammar | FAILS, and without the matcher knowing any macro names |
| X | `assert!(…, "the handle {:.8} is empty", uuid)` — a panic message | std's grammar | FAILS |
| Y | `format!("{:.8?}", uuid)` — a `type` after the precision | std's grammar | FAILS |
| Z | `uuid.as_bytes().chunks(8).next()` — the slice branch | **this lane's own enumeration** | FAILS |
| O' | `DEFINER` repointed at `model/version.rs` | lane | FAILS both tests — presence control re-confirmed against the extended vocabulary, not assumed to survive it |

**Negative control, which is new and which the boundary statement needs.** A
claim that names its gaps is only honest if the gaps are real. The four
disclosed-outside forms were planted and **must pass** — `format!("{:.*}", 8, uuid)`,
`format!("{:.1$}", uuid, 8)`, `uuid.get(..LEN)` behind `const LEN: usize = 8`,
and a hand-rolled `for … if index >= 8 { break }` loop. All four pass. So the
"outside it" list describes the instrument as it actually is, in both
directions: the covered forms fail, and the uncovered forms are exactly the ones
named.

**Gates.** `cargo xtask check` on the thrice-merged tree: all six pass. **No
golden moved** — `clean` reports no file changed outside `target/`, and
`git diff --stat main..HEAD` over `*.golden` is empty.

**Not redone, per the reviewer's instruction:** the derivation commands'
re-runnability, the reason-length control, row L, the five source-file folds and
the `tasks/M4.md` diff were all measured at review and were not reopened.

### C2 — Does the index fallback cost more than what it replaces?

The delta view (T17) measured its budget fallback and found the summary larger
than the lines it stood in for: 238 bytes against 180, at a 120-byte budget. A
fallback that costs more than it saves is the opposite of what a budget is for,
so the delta view skips it in that case. `view/scope.rs` has the same shape for
the index and was left alone, being outside that task's scope.

Measure it with the same instrument. If the index fallback can exceed what it
replaces, it takes the same skip.

**This chore is chore-runner eligible only while it stays mechanical.** The
measurement is mechanical. Deciding what to do about a case the measurement does
not settle is not — stop and report instead.

- **Check:** `cargo test` — a check that names the budget at which the summary
  would exceed the lines, with the measurement recorded. If the property does
  not hold for the index, the check records that it was looked for and not
  found, because a chore that measured nothing must not read as a chore that
  found nothing.
- **Check:** `cargo xtask check` — the six gates.

### C8 — The identifier seed becomes project-relative

**Provenance: James's ruling, checkpoint-2 review, 2026-08-20, promoting
PROPOSED 13 of `tasks/reports/M4-phase2.md`.** Read that item before starting:
it carries the measurement that widened this from one verb to three, and the two
questions a ruling had to weigh.

Three write verbs build their identifier seed from the document's **absolute**
path:

| Verb | Site |
|---|---|
| `label add` | `crates/kicli/src/edit/label.rs:294` |
| `wire draw` | `crates/kicli/src/edit/wire.rs:382` |
| `text add` | `crates/kicli/src/edit/text.rs:98` |

`edit::insert`'s module doc justifies seeding **rather than randomising** — "one
command run twice over one design produces one file and a test is repeatable" —
and says nothing about the path. The path choice was made once and never argued.
A project-relative seed serves the one recorded justification **strictly
better**: repeatable across checkouts as well as across runs. It is what the T16
golden defect cost a merge over.

**One chore across all three sites, not three chores.** A convention that ends
up split between verbs is worse than either convention, and every future write
verb inherits whichever one stands.

**Its first act is a measurement, and the finding is recorded before the change
lands.** Whether any committed fixture or golden depends on the absolute-path
values is unmeasured — the fixtures predate the convention being load-bearing.
If something does depend on them, that is a finding to record and report, not a
golden to refresh in passing.

**Not chore-runner eligible until that measurement comes back clean.** The
measurement is mechanical. Deciding what to do about a fixture that depends on
the old values is not.

- **Check:** the measurement, recorded — which committed fixtures and goldens
  contain identifier-shaped values, and which of those change when the seed
  changes. A measurement that found nothing must not read like a measurement
  that was not run.
- **Check:** `cargo test` — a check that the same drawing at two different
  absolute paths writes the same identifiers, with its control asserting the two
  paths really did differ, so a test that varied nothing cannot pass. This is
  the environment-variation break class the falsification-control skill now
  names, applied as the check itself.
- **Check:** `cargo xtask check` — the six gates.

## Milestone exit criteria

M4 is complete when all of the following pass on a clean checkout:

```sh
cargo xtask check
cargo test
cargo xtask corpus
cargo test --features corpus
KICLI_TEST_KICAD_CLI=1 cargo test --features corpus
```

with these gates green:

| Gate | Assertion |
|---|---|
| probe harness | one home for the builder, the oracle and the scratch copy; no test file carries a copy; the crate is a dev-dependency only |
| determinism | 100 runs and a shuffled item order yield byte-identical output |
| no floating point | none under `src/route/`, and no hash-map iteration feeds a decision |
| escape | every route leaves and enters along the pin's own direction |
| collinear block | a wire is never drawn along another net's wire |
| four-way | the router never creates one; it offsets by 1 G and reports it |
| one knob | `routing.label_threshold` is the only threshold, read by the router and by `KI-LBL-001` when M5 arrives |
| refusals | a diagonal, an off-grid vertex, a blocked escape: each exits its documented code and writes nothing |
| calibration | the in-repo sheet and `ampli_ht` re-route within 15 %, with the skipped nets named |
| netlist oracle | still 35 of 35 demo hierarchies |
| round trip | P1 and P2 hold on every fixture after every wire write |
| locality | a wire command changes only its own object's lines |
| KiCad agrees | the netlist of every written file carries the partition the route reported |
| mutation | `cargo-mutants` run once over `crates/kicli/src/route/`, every survivor triaged and recorded — see below |

**Mutation testing is a milestone-exit check, standing from the M4 close.**
Provenance: James's ruling, on the advisor's recommendation, 2026-08-15,
re-affirmed at the checkpoint-1 review and recorded here in the exit section
where the criteria live, not only beside the chores. The procedure, the scoping,
the two triage classes, the four counts it reports, and the skill it creates at
the M4 run are all in **"Mutation testing, at the close"** below. It runs after
every M4 task is ticked and the gates above are green, and **nothing about it
runs before then**. It is a batch run, hours-scale; it is not a per-commit gate
and it never becomes one.

### C3 — `label_of_kind` takes an s-expression fragment and calls it a shape

Found by the candidate shapes task (T9), 2026-08-15, which hit it inside an
environment-gated oracle and lost a debugging cycle to it.

`Probe::label_of_kind(head, shape, text, at)` interpolates `shape` **verbatim**
into the item text (`crates/kicli-probe/src/drawing.rs:318`). So `shape` is not a
shape — it is a complete s-expression fragment or the empty string. A caller who
passes `"input"`, which is what the parameter name invites, writes
`(hierarchical_label "IN" input …)`. KiCad writes `(shape input)` and **does not
read the bare token**; kicli's own reader is lenient and does, so the probe and
the oracle disagree about the same file and the oracle is the one telling the
truth. The rustdoc says "Draw a label of any of the three kinds" and never says
the argument is a fragment.

**T9's report of the blast radius is wrong, and the correction is recorded here
rather than propagated.** It reported that five callers in
`tests/net_probe_rules.rs` "pass over a file KiCad reads differently". Measured:
those callers pass `""` (plain labels, correct) or the full `"(shape input)"`
(correct — `net_probe_rules.rs:123-127`). **No existing test is compromised.**
The defect is a footgun that bit the one caller who read the parameter name and
believed it, which is a real defect and a smaller one.

**PROPOSED: this is a task, not a chore-runner item.** The mechanical half — make
the parameter a typed shape and build the fragment inside the helper, so the
illegal state is unrepresentable per `ENGINEERING.md` — is small. Deciding
whether the lenient reader that accepted the bare token is itself a defect is
not, and it is the more interesting question: kicli read a file KiCad would not.
Recommendation: fix the probe API as a chore, and open the reader question as its
own item rather than settling it in passing.

**The obvious fix breaks every correct caller, and this is the trap to record
before anyone starts.** Found by T9's tick reviewer, 2026-08-15. If the helper
starts writing `(shape …)` around its argument, then every existing caller —
which already passes the parenthesised form — double-wraps to
`(shape (shape input))`. The callers are `net_probe_rules.rs:124`, `:148`,
`:598`, `:622` (unchanged since M4 T2, `bddcbb7`), `edit_wire.rs:100` and
`edit_wire_sheet_pin.rs:148`. So the change is not "make the helper wrap"; it is
"make the argument a type that cannot be either form", and the callers move with
it in the same commit.

- **Check:** `cargo test` — a probe writing a hierarchical label of a given shape
  produces exactly one `(shape …)` form, asserted on the item text: neither the
  bare token nor a double wrap can be written by any caller.
- **Check:** `KICLI_TEST_KICAD_CLI=1 cargo test` — the `net_probe_rules` oracle
  tests still pass **against KiCad**, not only against kicli's lenient reader.
  That distinction is the whole point of this chore: kicli's reader accepting a
  file is not evidence KiCad does.
- **Check:** `cargo xtask check` — the six gates.

#### Done, 2026-08-20 — on branch `lane-c3`, base `aa96263`

**Commit:** `c95c0a0` — the helper and every caller together, per the trap
above. Base verified before anything was touched: `git log --oneline -1` in the
lane worktree read `aa96263`, `git status --short` was empty, so no
fast-forward was needed.

**What was built.** The kind and its shape are now one type in
`crates/kicli-probe/src/drawing.rs`. `LabelKind::Local` carries no shape;
`LabelKind::Hierarchical(LabelShape)` and `LabelKind::Global(LabelShape)` carry
one; `LabelShape` is the five tokens KiCad writes and its `token()` is the only
place they exist as text. `label_of_kind` builds the `(shape …)` list itself and
`strand_of_kind` takes the kind through, so no caller passes text for either
half.

**PROPOSED: the `head` argument was folded into the same type, which is more
than "a typed shape".** The reason is a second illegal state with a live caller,
below: `("label", "input")` was representable and was being written. A local
label has no shape at all (`research/sch-format.md` §3.3, "A `label` (local) has
no `shape`"), so pairing the head with the shape makes the migration of that
caller a consequence of the type rather than a judgement of taste.
Recommendation: keep it. Reversing it costs one enum and re-opens the state.

**The bare token had a second, live caller, and the entry's account of the blast
radius needs one line added rather than corrected.** The correction recorded
above — that every caller passes `""` or the full `"(shape input)"` — was true
of `tests/net_probe_rules.rs`, which is what it was measured against. It was not
true of the tree: the crowded drawing of the determinism probe
(`tests/route_determinism.rs:135`, as of `aa96263`) passed
`label_of_kind("label", "input", "SENSE", …)` and wrote
`(label "SENSE" input (at 85.09 95.25 0))`. The footgun bit twice, not once.
That drawing's label is an obstacle rather than a connection, so nothing it
measures moved; it is now `LabelKind::Local`, which is the form KiCad writes.

**The caller list, verified rather than trusted.** `git grep -n -e label_of_kind
-e strand_of_kind aa96263 -- '*.rs'` finds **34 call sites in eight test files**,
where the entry names six sites in three. Four of the six are still exactly where
they were — `net_probe_rules.rs:124`, `:148`, `:598` and `:622` each name the
`"hierarchical_label"` line of the call above it, and `:148` is the
`bundled_members` helper, which reaches `label_of_kind` at `:164`.
`edit_wire.rs:100` is unchanged. **One has moved:**
`edit_wire_sheet_pin.rs:148` is now `:204`. **Two are missing:**
`route_shapes.rs:647`, which passes `"(shape input)"` correctly, and
`route_determinism.rs:135`, the bare-token caller above.

The entry's list held the sites passing a **shape**, which is what it was about.
The other twenty-six pass `""`: twenty in `net_probe_rules.rs`, and one each in
`route_labels.rs`, `route_cost.rs`, `route_determinism.rs` and two in
`route_obstacles.rs`. **Every one of them is forced by the signature** — a
literal `""` cannot survive a typed parameter, under any design of the type — so
the change reaches five files the chore's scope did not name:
`route_labels.rs`, `route_obstacles.rs`, `route_cost.rs`, `route_determinism.rs`
and `route_shapes.rs`. Nothing was touched that the signature did not force, and
nothing in `crates/kicli/src/**` was touched at all. `route_labels.rs` also
imports `kicli::model::items::LabelKind`, so the probe's type is spelled in full
at its one call site there rather than aliased.

**Check evidence.**

- `cargo test --workspace`: **581 passed, 0 failed** over 77 result lines. Four
  of those are new, in `crates/kicli-probe/tests/drawing.rs`, which is where the
  builder's own properties already live.
- `cargo xtask check`: all six gates pass (fmt, clippy, test, doc, deny, clean).
  Run in a lane worktree, so it does not count toward done — the orchestrator's
  merged run does.
- `KICLI_TEST_KICAD_CLI=1 cargo test --workspace`: **581 passed, 0 failed**, with
  **`kicad-cli` actually present** — `kicad-cli version` → `10.0.5`. The 16
  `net_probe_rules` probes pass against KiCad, not only against kicli's reader.
  **The gate was shown to bite**, because "the gated test did not run" and "the
  gated test passed" are different facts: ungated the 16 probes finish in
  `0.01s`, gated in `0.76s`, and gated with `KICLI_KICAD_CLI=/nonexistent/kicad-cli`
  all 16 panic at `oracle.rs:76`. Environment-gated, so this is a measurement
  the lane made, not a check the lane passed.

**Falsification.** Good state committed first (`c95c0a0`), and the file
checksummed back to `d4de1058b360505ac30d61d87c7d7eb20be01433d68b925c7a3c6dbf6fe98cd8`
after every restore, with `git status --porcelain` empty. The narrow assertions
sit **before** the whole-line one on purpose: with the exact-line `assert_eq!`
first, every break tripped it and the three specific assertions could never
fire, so they were three checks that could not fail. Reordered, each break names
the form it wrote.

| Break, in `Probe::label_of_kind` | Assertion that caught it |
|---|---|
| `format!(" {}", shape.token())` — the bare token, which is what the old parameter name invited | `"a bare shape token is never written beside the name"`, `tests/drawing.rs:188`. Also `a_global_label_wears_its_own_shape` and `every_shape_writes_the_token_kicad_writes`. |
| `format!(" (shape (shape {}))", …)` — the double wrap the obvious fix would have caused | `"a shape list is never wrapped in another"`, `tests/drawing.rs:192`, and the same two other checks |
| `format!(" (shape {0}) (shape {0})", …)` — two lists, which neither assertion above sees | `"exactly one shape list"`, `tests/drawing.rs:196`. `every_shape_writes_the_token_kicad_writes` stayed green: it asserts the token appears as its own list, not that it appears once, so this is a break it does not claim to watch. |
| `LabelKind::Local => Some(LabelShape::Input)` — a local label wearing a shape, the second illegal state | `a_local_label_wears_no_shape_at_all`, `tests/drawing.rs:219`: `(label "IN" (shape input) …` against `(label "IN" …` |

**Evidence for the reader question, which is carried forward and was not acted
on.** Measured 2026-08-20 with `kicad-cli` 10.0.5, on a copy of
`tests/fixtures/sch/nets/` outside the repo with the one `(shape input)` in
`nets_channel.kicad_sch` replaced by a bare `input`:

- **KiCad reads the two files differently.** Netlist export gives 32 nets from
  the file KiCad wrote and 36 from the bare-token file; `/CH_A_IN`, `/CH_B_IN`,
  `/channel_a/CHAN_LOCAL` and `/channel_b/CHAN_LOCAL` are gone from the second.
  The label stops naming anything, and the nets it joined fall apart.
- **kicli reads them identically.** `kicli sch view --view connectivity` on both
  projects diffs to nothing at all — same `nets=14`, same 47 lines. No warning,
  no refusal.

So kicli's reader takes a bare token as a shape and reports connectivity KiCad
does not agree with, silently, on a file KiCad would reject the meaning of. That
is the reader question with a number on it. **Not acted on here**, per the split
this entry proposes: the mechanical half is above, and whether the leniency is
itself a defect is the separate item.

### C4 — A sheet pin whose angle disagrees with its position is nobody's

Found by `wire draw` (T14) when the sheet-pin rule was measured, 2026-08-15, and
written down here because that entry named an owner — "whichever task owns the
sheet-pin lint" — that does not exist. **No task owns this.** `spec/SPEC.md`
§11.4's rule catalogue has no code for it, and M4 scores no drawing at all, so
there was nothing in the milestone to attach it to. Unowned and recorded as
unowned is the honest state; it stays that way until a session picks this up.

**The gap, measured rather than argued** (T14's third arm, kicad-cli 10.0.5):
KiCad puts a port's connection point on the edge its angle names, keeping the
along-edge coordinate. `SheetPin::at` is the file's value. On a file KiCad wrote
the two agree and nothing is owed; on a hand-written or generated file where
they disagree, `Terminal::of_sheet_pin` gives the router a terminal at a place
KiCad does not connect, and a wire drawn to it is dangling in the tool while
kicli reports it routed.

**PROPOSED: report the disagreement, do not correct it silently** — T14's
recommendation, unchanged. A router that moved the port would be editing the
drawing to suit itself. The alternative reading is that the reader should hand
back the position KiCad uses, which is defensible and is what makes this a
decision rather than a typo.

**Not chore-runner eligible.** Detecting the disagreement is mechanical;
choosing between reporting it and correcting it is not, and neither is deciding
whether it earns a `KI-…` code in §11.4 or stays a router-level refusal.

**PROPOSED owner, under the checkpoint-1 owners ruling: M5, with the rule
catalogue.** The two open questions are both M5's by subject — whether this earns
a `KI-…` code is a §11.4 decision, and §11.4 is what M5 builds. Naming an M4 task
would mean opening a scoring surface this milestone deliberately has none of.
Recommendation: accept, and carry the entry forward into M5's file at the M4
close rather than leaving it in a closed milestone. The alternative — an M4 task
that refuses at the router without a rule code — is defensible but decides the
larger question by default, in the milestone least equipped to.

#### CARRIED INTO M5, 2026-08-21 — closed for M4

The PROPOSED owner above is accepted and executed: this entry now also lives at
`tasks/M5.md`, "Carried in from M4 C4". **The M4 chore is closed by being carried,
not by being done** — nothing was implemented, and the reason is the one the
PROPOSED gives: both open questions are §11.4 decisions and §11.4 is what M5
builds.

The entry stays here in full rather than being replaced by a pointer, because the
measurement it carries — T14's third arm, against kicad-cli 10.0.5 — was made in
M4 and belongs with the task that made it. M5 carries the decision; M4 keeps the
evidence.

- **Check:** `cargo test a_port_whose_angle_disagrees_with_its_position_is_reported`
  — a probe drawing with one port written on an edge its angle does not name is
  reported as a fault naming that port, and a drawing where every angle agrees
  is not. The disagreeing drawing already exists as a recipe: it is the
  reflected arm of `tests/edit_wire_sheet_pin.rs`.
- **Check:** `KICLI_TEST_KICAD_CLI=1 cargo test` — the fault kicli names is one
  KiCad agrees with: the netlist for that drawing leaves the stub at the written
  position unconnected, which is the third arm's measurement re-used as the
  oracle rather than kicli's own arithmetic.
- **Check:** `cargo xtask check` — the six gates.

### C7 — `agent_doc_covers_every_command` asserts a mention, not a heading

Found by the verb surface (T16), 2026-08-15, while falsifying its own
documentation check. **Measured, not suspected: deleting the entire
`kicli wire draw` section from `AGENT.md` left the check green**, because the
`[routing]` prose elsewhere in the file names the verb, and a mention is all the
check looks for. What actually held the section in place was
`agent_doc_covers_every_verb_flag`.

**This check has been relied on all milestone**, including by three tasks that
were sequenced around `AGENT.md` precisely because it was believed to enforce
documentation. It does enforce something — a verb cannot vanish from the file
entirely — but not what its name claims, and the gap is the whole difference
between "documented" and "mentioned somewhere".

**Not chore-runner eligible as a single mechanical sweep.** Tightening the check
is mechanical; deciding **what counts as documented** is not. A heading per
command is the obvious answer and is probably right, but `AGENT.md` groups verbs
under shared headings today — `kicli field move`, `kicli field rotate`,
`kicli field justify` share one — so a naive heading-per-command rule fails on
the existing document, and whether to split those groups is a judgement about
the document's shape rather than about the check.

The check is the delta view (T17)'s by origin; T16 found this and left it alone
as outside its lane, which was correct.

- **Check:** `cargo test agent_doc_covers_every_command` — with the section for
  one command deleted, the check **fails**; with it present, it passes. The
  falsification is the whole point of this chore, because the defect is a check
  that could not fail in the case it exists for.
- **Check:** `cargo xtask check` — the six gates, on the unchanged `AGENT.md`, so
  a tightened check is shown to pass on the document as it stands rather than
  demanding a rewrite of it.

#### Done, 2026-08-21

**Commit:** `tests: a command is documented by a heading with a body, not a
mention (M4 C7)` — one commit on `lane-doc`. It is deliberately not cited by
hash: the falsification below ran against a checkpoint commit that this one
amended, so a hash written here would name a commit that no longer exists. The
state under test is identified by checksum instead, which is stronger. Touched `crates/kicli/tests/agent_doc.rs` only; `AGENT.md` is byte-unchanged by
this commit, which is what the second check asked for.

**What counts as documented, and why.** The rule is: *the command's name appears
as its own backtick-delimited span in a heading line, and the section that
heading opens carries a body.* Three decisions inside that, each made against
the document as it stands rather than against a preference:

- **A heading, not a mention.** This is the whole defect. Prose can name a verb
  for a dozen reasons that have nothing to do with documenting it — `[routing]`
  named `wire draw` while its section was gone.
- **One heading *per command name*, not one section per command.** A naive
  heading-per-command rule fails on this document, exactly as the entry warned:
  six headings are shared, covering sixteen of the twenty-eight commands
  (`field move`/`rotate`/`justify`; `field show`/`hide`; four `text` verbs;
  three `label` verbs; two `junction`; two `noconnect`). **PROPOSED: leave the
  groups alone.** They group verbs that differ by one word of behaviour and
  share a usage block, and splitting them would make the document longer and
  worse to read to satisfy a check — the check should describe the document's
  shape, not impose one. What the rule does insist on is that a grouped heading
  *names every verb it covers*, which is the failure mode grouping actually has
  (break 2a below).
- **Backticked, and exactly.** Delimitation is not decoration: an undelimited
  substring match would let a future `kicli wire draw-arc` heading answer for
  `kicli wire draw`. Backticks are already the universal convention here — all
  28 commands, no exceptions — so this costs the document nothing today. It does
  mean a heading spelled `### kicli wire draw` is refused (break 5); the failure
  message says so in as many words.

**The body floor is measured, not chosen.** `SUBSTANCE = 80` non-whitespace
characters. Measured across all 28 commands on the unchanged document: the
smallest real section is `kicli sym delete` at **135**, the largest
`kicli wire draw` at **4391**; a heading with one sentence under it measures
about **50** (break 3 measured 15). The floor sits in the gap, so it catches a
stub without demanding a word count from a genuinely terse command.

**A parser defect found on the way, and fixed.** The section reader skips fenced
blocks. It has to: `AGENT.md` prints view samples inside fences and a kicli view
comments itself with `#`, so `# scope project  sheets=3 sym=23 pwr=4 nets=14`
reads as a level-1 heading to a naive line scanner and truncates the section it
sits in. Verified by printing the parse — 36 headings, none of them from inside
a fence. It is also an evasion route in its own right (break 4).

**Falsification.** Every break was made against a good state committed as a
checkpoint before anything was broken, and `shasum -a 256` was run on both files
after every restore: each restore returned `AGENT.md` to `968e790b…` and
`agent_doc.rs` to `97567621…`, without exception. One change was made after the
breaks were finished — the wording of the failure message, sharpened so that the
unbackticked-heading case (break 5) reads as the instruction it is — so the
committed test hashes `2d266df0…`, and the six-gate run below is on that state.
The checkpoint commit was amended into the commit above and no longer exists;
the checksums are what identify the state, which is the point of taking them.

Rows 1 and 2a run the **old** check (`git show 491c254:…/agent_doc.rs`) against
the *same* broken document as the new one, so each is an A/B on a single input
rather than two stories told side by side.

| # | The break | Old check | New check |
|---|---|---|---|
| 1 | The whole `kicli wire draw` heading and section deleted (118 lines, 424–541) | **ok** — the defect, reproduced | **FAILED**: "no heading naming `kicli wire draw`" |
| 2a | `kicli field rotate` dropped from the shared heading, body untouched | **ok** — the usage block still spells the verb | **FAILED**: "no heading naming `kicli field rotate`" |
| 2b | The whole shared `field move`/`rotate`/`justify` section deleted (13 lines) | FAILED | **FAILED**, on `kicli field move` first |
| 3 | `kicli sym delete`'s heading kept, body replaced by "Deletes a symbol." | — | **FAILED**: "has only 15 characters under it, which documents nothing" |
| 4 | Section deleted, a heading naming it planted **inside a fenced block** | — | **FAILED** — the fence skipping is load-bearing |
| 5 | Heading respelled as a level-3 heading with the name unbackticked, body intact | — | **FAILED** — deliberate, see above |
| 6 | Heading kept, body replaced by 2 lines of "TODO: … the routing section explains most of it" | — | **ok** — see below |

**Break 6 stayed green, and that is a limit, not a no-op.** The break was real:
118 lines replaced by two. The check passed because the replacement clears the
80-character floor, and no textual check can judge whether a body is *about* the
command it sits under. Recorded rather than papered over, because the lesson of
this very entry is that a green break is a finding about the instrument. Raising
the floor would not close it — it would only make the evasion longer — so the
floor stays where the measurement put it. **The residual hole is: a section that
exists, is long enough, and says nothing useful.** That is a tick-review
question, not a test one.

**A spelling the rule refuses on purpose**, flagged for whoever writes the next
command section: the name goes in the heading, in backticks, spelled exactly as
the binary spells it.

**Check evidence:**

- `cargo test --test agent_doc` on the unchanged `AGENT.md`: **6 passed, 0
  failed**.
- `cargo xtask check` at the good state: **all six gates pass** (fmt, clippy,
  test, doc, deny, clean).

**Carried, not done here.** The check still only guards one direction: a heading
naming a command the binary does not have would pass, which is the drift the
module's own doc comment calls "worse than no document". Out of this chore's
stated scope; recorded so it is not rediscovered.

**Expected failure, as of this lane's base `491c254`.** The verb surface lane
is adding
`kicli wire connect`; `agent_doc_covers_every_command` will fail on the merged
result until that verb has a heading with a body in `AGENT.md`, which is that
task's job. Not accommodated here, per the lane brief. Re-verify against the
merged tree rather than trusting this note.

### C6 — `JOINING`'s rustdoc claims a number it does not share

Found by `wire delete` (T15)'s tick review, 2026-08-15, verified at source before
filing.

`crates/kicli/src/edit/wire.rs:98-104` documents `JOINING` as "the same one
`crate::edit::mark` refuses a four-way junction against, read from the other
side". `JOINING` is 3; `mark.rs:33`'s `FOUR_WAY` is 4. They are different
boundaries on the same quantity — how many wire ends meet at a point — and both
are fed by the one `wire_ends_at`, which is what the designation ruling was for.
The relationship is real; the stated identity is not.

Fix the sentence to say what is true: two boundaries on one measurement, from
opposite directions, sharing an implementation rather than a value. Say it
without naming a number, so the prose cannot go stale if either boundary moves.

**Chore-runner eligible.** It is a documentation correction with no behaviour
attached, and the gates prove nothing broke.

- **Check:** `cargo xtask check` — the six gates, `cargo doc` included, which is
  where a broken intra-doc link to `mark` would show.
- **Check:** `cargo test` — unchanged counts; this touches no behaviour.

#### Done, 2026-08-15

**Commits:**
- `23a2504` — Fix JOINING rustdoc to not claim an identity with FOUR_WAY
- `9d14515` — C6 correction: Clarify JOINING's rustdoc to distinguish measurement from boundary

**Check evidence:**

- `cargo test`: **345 tests passed** (unchanged before, during, and after). No behaviour changed.
- `cargo doc --no-deps`: **clean, no errors or warnings**. Intra-doc link to `crate::edit::mark` verified in both versions.
- `cargo xtask check`: **all six gates pass** (fmt, clippy, test, doc, deny, clean).

**Rustdoc evolution:** First revision stated "shared with mark's refusal boundary, measured from opposite directions", which still suggested the boundary itself was shared — reintroducing the ambiguity. Correction clarifies: JOINING and FOUR_WAY are two different thresholds on one shared measurement (how many wire ends meet at a point), both calling the same implementation. The measurement and implementation are shared; the boundaries are distinct. Neither number is named, so prose stays current if either moves.

### C5 — No probe drawing and no fixture can be addressed by a handle

Found by `wire delete` (T15), 2026-08-15, which had to work around it to test its
own addressing rule.

`Probe::uuid` writes `00000000-0000-4000-800{series}-{n:012}`
(`crates/kicli-probe/src/drawing.rs:94-100`). The identifier varies only in its
last twelve digits, so **every object of every probe drawing shares the
eight-character handle `00000000`**. `Uuid::short` cannot tell any two of them
apart.

**Measured, and wider than the finding as reported.** The committed fixtures are
numbered the same way: across `crates/kicli/tests/fixtures/sch/`, all 151 `uuid`
atoms share the same eight leading characters. So neither instrument — probe
drawing nor committed fixture — can exercise a command addressed by handle.

**No existing test is wrong, and that is the point.** Nothing currently addresses
an object by handle in a probe drawing, so nothing is silently passing. What the
defect removes is a whole class of test that later verbs need: `wire delete`
addressed its segment by identifier and could only test the handle path by
rewriting one identifier in the drawing's text after the harness wrote it, saying
so at that point. Every verb that takes a handle will need the same workaround
until this is fixed.

**Chore-runner eligible.** The change is mechanical — vary the leading digits and
keep them stable per object — and its check states its own success condition. The
fixtures are the judgement call: regenerating them moves goldens, so this chore
covers the probe crate only, and the fixtures are named here as a known second
half rather than swept in silently.

- **Check:** `cargo test` — a probe drawing of several objects yields several
  distinct eight-character handles, with a control asserting the count of
  distinct handles equals the count of objects, so a generator that varied
  nothing cannot pass.
- **Check:** `cargo xtask check` — the six gates, and no golden moves, because
  the probe crate writes no committed fixture.

#### Done, 2026-08-15

**Commits:**
- `278c678` — Probe UUID generation varies per object so handles are distinct
- `c31de4b` — C5 correction: Fold series into leading UUID digits; widen control to multi-sheet case

**Check evidence:**

- `cargo test -p kicli-probe --test drawing a_probe_drawing_yields_distinct_handles_for_each_object`: **pass**. Within one drawing, 12 objects yield 12 distinct handles.
- `cargo test -p kicli-probe --test drawing sibling_probes_of_different_series_have_no_colliding_handles`: **pass**. Across parent + two named_child_of siblings (3 drawings total), 18 objects yield 18 distinct handles.
- `cargo xtask check`: **all six gates pass** (fmt, clippy, test, doc, deny, clean). No golden files moved.

**First pass missed:** Initial fix varied leading digits per object but not per series. When `named_child_of` creates siblings, each starts its own `next_uuid` counter at 0. So left's first object (series 2) and right's first object (series 3) both got handle `00000001`. Defect was filed to remove — it survived in multi-sheet case.

**Correction:** Folded series into leading field. UUID format changed from `{:08x}` to `{:02x}{:06x}`, encoding series in first 2 hex digits and next_uuid in next 6. Now series 1 objects have handles starting `01xx...`, series 2 `02xx...`, etc. No collision possible across siblings.

**Falsification proof — contrast between narrow and wide control:**
- Reverted to series-blind leading digits `{:08x}` (only next_uuid, no series)
- Narrow control (within-drawing): 12 objects per sibling, 12 distinct handles per sibling — still PASSED
- Wide control (across-siblings): 18 objects total, 9 unique handles due to collision — FAILED
- Restored: both controls now PASS
- This contrast proves the widened control watches what the narrow one cannot.

### Mutation testing, at the close — standing from M4

**Provenance: James's ruling, on the advisor's recommendation, 2026-08-15.**

After every M4 task is ticked and all gates above are green, run `cargo-mutants`
once, scoped to `crates/kicli/src/route/`. Install with
`cargo install cargo-mutants`, and use its file-filtering options per its current
documentation rather than per anyone's memory of them.

**It is a batch verification run, hours-scale. It is NOT a per-commit gate and it
never becomes one.** The six gates stay fast because a gate a developer dreads is
a gate a developer skips.

**Why it exists.** It mechanises the falsification-control rule after the fact.
Hand falsification proves a check can fail **at birth** — the implementer breaks
the code and watches the check go red. Nothing keeps proving it afterwards. A
surviving mutant is a check whose coverage **decayed** since: the code changed
underneath it, or a refactor made it vacuous, and no one was watching. The two
instruments answer different questions and neither replaces the other.

**Triage every survived mutant, recorded in the milestone report**, in two
classes:

- **Genuine coverage gap** — the mutant changes behaviour and no test notices.
  File each as a chore or a task, with the mutant quoted verbatim. **Do not
  silently iterate the tests to zero survivors during the close.** Fixing them is
  ruled work, not close-out tidying; a survivor count driven to zero by
  unrecorded test edits destroys the very evidence the run exists to produce.
- **Benign survivor** — message wording, logging, genuinely equivalent code.
  Recorded with the reason it stands.

**Report four counts:** generated, killed, survived-genuine, survived-benign.

**At the M4 run only, also create `.claude/skills/mutation-run/SKILL.md`**: the
procedure, the scoping, the triage classes, and **the M4 run's actual results as
the worked example**. Future milestones widen the scope by ruling, not by
default.

Nothing about this runs before the M4 close.

M4 ships the first commands that route. Nothing in it scores a drawing.
