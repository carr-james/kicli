# Formatting is rustfmt's opinion, not ours (ENGINEERING.md).
# Stable options only, so `cargo fmt` works on the stable toolchain.
edition = "2024"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
