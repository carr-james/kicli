[package]
name = "xtask"
description = "Workspace automation for kicli. Not published."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# xtask uses the standard library only. Keep it that way: it must run before
# anything else builds.
[dependencies]
